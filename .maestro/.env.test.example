# Copy to .maestro/.env.test (gitignored) and fill in with two numbers from the target deploy's
# OTP_TEST_PHONES allowlist (docs/PILOT-READINESS.md § "Vendor-free QA testing"). Never commit real
# phone numbers or API keys here.
#
# Local usage (no CI):
#   set -a && source .maestro/.env.test && set +a
#   maestro test .maestro/customer --env CUSTOMER_PHONE="$CUSTOMER_PHONE" --env RIDER_PHONE="$RIDER_PHONE" --env API_URL="$API_URL"
#   maestro test .maestro/rider    --env CUSTOMER_PHONE="$CUSTOMER_PHONE" --env RIDER_PHONE="$RIDER_PHONE" --env API_URL="$API_URL"

CUSTOMER_PHONE=+263771234567
RIDER_PHONE=+263770000002
API_URL=https://lyniago.lyniafinance.com
