# React Native / Expo autolinking and Metro resolve dependencies by walking node_modules and don't
# follow pnpm's symlinked store reliably. A hoisted layout (a flat node_modules) is what RN expects,
# so EAS cloud builds and Metro find the Expo native modules + workspace packages. (Layout-only; the
# lockfile is unchanged.)
node-linker=hoisted
