# Refactoring routine — 2026-08-03 (LC loop R, fifteenth run)

Fifteenth run of the recurring refactoring routine (`docs/ROUTINES.md` → "Refactoring routine"),
run by the temporary LC loop R sprint. Branch `claude/lc-r-20260803b`, based on `main` @ `475c5b1`
(the fourteenth run's RF-19 PR and the day's other merged lanes, including an "LC steer" pass that
merged four stalled PRs across other lanes).

## Phase 0 — orient

- `docs/REFACTOR-LEDGER.md`: the fourteenth run (RF-19) left the debt register with **no actionable
  OPEN rows** and ran its self-disable check — but this firing arrived anyway (the trigger disable
  from that run either hadn't taken effect yet or this firing was already queued), so Phase 0 still
  applies in full: re-verify the gate, check for an in-flight lane PR, and — per priority-order step
  (b), since there was no actionable OPEN row for step (a) — recompute the hotspot map before
  concluding there's genuinely nothing to do.
- `docs/ROUTINES.md` § "Refactoring routine" and `docs/KNOWN_BUGS.md` read for doctrine/dedup.
- `list_pull_requests` on `unnfazzed/Lynia` (open) returned two open PRs (#531 LC-B, #532 LC-C —
  both different lanes), no `claude/lc-r*`/`claude/refactor-*` PR in flight — no in-flight refactor
  PR to babysit, clean branch to build on.
- Gate: fresh checkout needed `pnpm install` + `apps/api`'s `prisma generate` (the generated Prisma
  client isn't checked in — the same one-time gap every prior run has noted, not a code defect)
  before `pnpm typecheck && pnpm build && pnpm test` went green on clean `main`: 96 API test files /
  1516 API tests, 101 mobile suites / 713 mobile tests, plus admin/merchant/shared suites all green.

## Phase 1 — hotspot recompute (priority-order step b)

Re-ran the 45-day churn scan (`git log --since="45 days ago" --name-only`) across `apps/`/`packages/`,
excluding tests/config/lockfiles/changelogs, and reconciled the top ~30 hits against the ledger's
existing hotspot map:

- **`apps/mobile/app/rider/(tabs)/index.tsx`** (hotspot map rank 8) — the ledger already tracked this
  screen at `apps/mobile/app/rider/index.tsx`, 955 lines, "bid-adjacent, needs a design pass; not
  scoped." It has since moved under a `(tabs)` route group and grown to **1212 lines**, still churning
  (5 commits/45d). Read the full file: it's the rider board — online-toggle, GPS/location, KYC-gate,
  and the bid-compose flow (`makeOffer` mutation, the `selected`/`fare`/`eta`/`offerMode` compose card)
  all in one function over ~20 pieces of state. Six JSX sections are already hoisted to local consts
  shared between two return branches (`activeJobBanner`, `onlineToggleCard`, `sentOffersSection`,
  `selectedCard`, `trailingFooterContent`, `boardBanner`) — a plausible RF-18-style presentational-
  extraction seam — but `selectedCard` **is** the bid-offer compose UI itself, the same bucket as the
  hotspot map's SENSITIVE `order/[id].tsx`/`rider/job.tsx` row ("bid acceptance UI"). Per the RF-05b/
  RF-18 precedent (a design note before any extraction PR on an entangled or sensitive screen),
  triaged into the register as **RF-22, OPEN — needs a design pass first**, not executed this run.
- **`apps/mobile/app/send.tsx`** — new to the map, 958 lines, 4 commits/45d. The customer parcel-
  request compose screen (`createOrder` — pre-bid, not an agreed-price/bid-acceptance mutation, so
  lower sensitivity than RF-22). Large enough to warrant its own RF-18-style design pass before
  extraction. Triaged as **RF-21, OPEN — not scoped**; not attempted this run — one non-mechanical,
  design-pass-requiring screen (RF-22) was already this run's hotspot finding, and picking a second
  large screen in the same firing would exceed the "one refactoring concern" rule.
- Spot-checked the rest of the recompute's top candidates for standard-menu items (dead code,
  duplication, misplaced logic, `any`) and found them already clean or too small to matter:
  `apps/api/src/admin/admin.controller.ts` (378 lines, pure routing/guard delegation — no misplaced
  logic), `apps/merchant/app/lib/use-queue-poll.ts` (110 lines, already-lean single-flight polling
  hook), `apps/merchant/app/lib/reachability.ts` (178 lines — has a tiny 3-line `nextProbeDelayMs`
  formula literally duplicated in `apps/mobile/src/net/reachability.ts`, but the surrounding stores
  are structurally different classes, and the shared 3-line pure function would need its own
  explicitly-labeled `packages/shared` public-surface PR per the RF-07 precedent — not worth a
  dedicated PR for 3 lines; noted here, no register row), `apps/merchant/app/components/
  KitchenConnectionProvider.tsx` (183 lines, clean context provider), `apps/merchant/app/(app)/
  hours/page.tsx` (232 lines — this is where the RF-20 duplication below was first spotted). A repo-
  wide grep for `: any`/`as any`/`<any>` outside tests returned zero real hits (all matches were the
  English word "any" inside comments) — type-safety is clean, nothing to do there.
- That last spot-check (`hours/page.tsx`) surfaced a genuine duplication: **11 catch-block call sites
  across 4 merchant pages** (`hours`, `menu`, `shop`, `statement`) repeating the identical
  `if (isSessionExpiredError(err)) { signOut(); return; }` 3-line block before each site's own
  (distinct) fallback-message `setXxx` call. Small, mechanical, single-concern, non-sensitive (auth-
  session housekeeping, not bid/order/agreed-price/KYC logic), and all 11 sites are covered by
  existing per-page test files that specifically exercise the session-expiry branch. Executed this as
  the run's one increment — see Phase 2.

## Phase 2 — RF-20: merchant session-expiry catch-block dedup

Added one helper to `apps/merchant/app/lib/api-client.ts`, next to the `isSessionExpiredError` it
wraps:

```ts
/** Signs out and reports `true` on a session-expiry error, so a catch block can early-return with
 *  `if (redirectIfSessionExpired(err, signOut)) return;` instead of repeating the check + call. */
export function redirectIfSessionExpired(err: unknown, signOut: () => void): boolean {
  if (!isSessionExpiredError(err)) return false;
  signOut();
  return true;
}
```

Every one of the 11 call sites (`hours/page.tsx` ×3, `menu/page.tsx` ×4, `shop/page.tsx` ×3,
`statement/page.tsx` ×1) now reads:

```ts
if (redirectIfSessionExpired(err, signOut)) return;
setXxx(err instanceof ApiError ? err.message : "<unchanged per-site fallback message>");
```

`statement/page.tsx`'s one site has an additional `cancelled` guard checked *before* the session
check (an unmount-race guard on its `refresh()` effect) — left exactly where it was, ahead of the new
helper call, so that behavior is unchanged. Doc comments explaining *why* a given catch block exists
(the D-D0e/LC-D04 comments on `menu/page.tsx`'s `onCreateStarterCategory`/`onClearOos`, the LC-D04
comment on `hours/page.tsx`'s `onToggleBusy`) were kept in place immediately above the new one-line
check, since they explain the catch block's existence in general, not the session-expiry branch
specifically.

**Verification:**
- `pnpm typecheck && pnpm build && pnpm test` (full monorepo) — green pre and post: 96 API test
  files / 1516 API tests, 101 mobile suites / 713 mobile tests, 24 merchant test files / 168 merchant
  tests, 9 admin / 9 shared test files — every count identical to the pre-change baseline. The 4
  touched pages' existing test suites (`hours` 5, `menu` 7, `shop` 3, `statement` 1) pass unmodified,
  including `menu/page.test.tsx`'s dedicated "MenuPage session-expiry on a mutation (LC-D##)" describe
  block (3 cases: starter-category create, "back in stock", category delete-via-sheet, each asserting
  `signOut` is called exactly once and no inline error text renders).
- `pnpm lint` (oxlint) — 0 new warnings; 1 pre-existing unrelated `admin-orders.service.spec.ts`
  shadow warning (same baseline every prior run has noted); `apps/merchant` lint itself 0 warnings/0
  errors.
- `pnpm run depcruise` — 0 new violations; 4 pre-existing unrelated mobile orphan-module infos
  (`render-count.ts`, `wallet-ledger.ts`, `use-now.ts`, `food-rider-job.ts` — all mobile files this PR
  never touches).
- Public surface: no controller/route/DTO/socket-event touched; `packages/shared` untouched;
  `redirectIfSessionExpired` is a new named export of `apps/merchant`'s own internal
  `app/lib/api-client.ts`, not a `packages/shared` export, so the routine's public-surface-diff rule
  (which only binds `packages/shared`/API routes/socket events) doesn't apply to it.
- Diff size: 5 files / 23 insertions / 48 deletions (well under the ~400-line guideline).

## What was deliberately skipped

- RF-21 (`send.tsx`) and RF-22 (`(tabs)/index.tsx` rider board) — both triaged, neither executed;
  see Phase 1 for why (need a design pass, and RF-22 in particular touches bid-compose UI).
- The `nextProbeDelayMs` 3-line duplication between `apps/mobile/src/net/reachability.ts` and
  `apps/merchant/app/lib/reachability.ts` — genuine but too small to justify its own labeled
  `packages/shared` public-surface PR (RF-07 precedent); not registered as an RF row, just noted here
  so a future run doesn't re-discover it from scratch.
- No second refactor PR this run (the routine allows up to 3 per run) — RF-20 was the only
  same-run-executable finding; RF-21/RF-22 both need their own design-note increment first, which is
  itself a separate firing's worth of work per the RF-05b precedent.

## Self-disable check

The debt register is **not** exhausted after this run: RF-21 and RF-22 are both new actionable-ish
OPEN rows (though each needs a design-note increment before a mechanical extraction PR can follow,
mirroring RF-05b's two-step precedent). Per the mission brief, self-disable only triggers when there
are no actionable OPEN rows *and* the hotspot recompute surfaces nothing new — this run's recompute
did surface something new (RF-21, RF-22), so the lane stays active. No trigger-disable action taken
this run.

## Ledger

`docs/REFACTOR-LEDGER.md` updated in this PR: hotspot map row 8 updated (path/size/status, now
cross-referenced to RF-22), two new hotspot rows 16 (RF-20, DONE) and 17 (RF-21, OPEN) added after
row 15, debt register gets three new rows (RF-20 DONE, RF-21 OPEN, RF-22 OPEN), new completed-log
entry at the top. This report replaces the fourteenth run's same-date report per the docs retention
policy — that run's findings (RF-19) are preserved in the ledger's hotspot map, debt register, and
completed log, which are the durable record.
