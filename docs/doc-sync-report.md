# Documentation Sync Report

Generated by the doc-reconciliation routine. Incremental pass against the marker in
`docs/.last-doc-sync` (previously `a456040`, this run's HEAD `243aaed`, 2026-07-10). That range (50
files, ~29 commits) added three undocumented-until-now docs (`DEPLOYMENT-AUTOHEAL.md`,
`GCP-PROVISIONING-REVIEW.md`, `UX-USABILITY-REVIEW-2026-07-09.md`), touched deploy/observability
workflows and Terraform, and landed a wave of admin/mobile fixes (rider `on_hold` recovery,
scheduler-guard audience pinning, cancel-strike surfacing, and more). Verified every checkable claim
in the changed/new docs against the actual code — `.github/workflows/*`, `infra/terraform/*`,
`apps/api/src/**`, `apps/mobile/**` — and, motivated by the `on_hold` fix landing in this window,
cross-referenced older review docs (`FRAUD-REVIEW.md`, `JOURNEY-BUGS.md`) that reference the same
code paths. `JOURNEY-BUGS.md` turned out to be badly out of date — most of its P1/P2 findings from
2026-07-05 have since been fixed but were never marked as such across at least one earlier doc-sync
pass — so this run did a full reconciliation of that file rather than staying strictly incremental.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 6 files (28 individual claims) |
| CODE_BUG | 0 |
| ORPHAN | 0 |
| AMBIGUOUS | 0 |
| Claims checked and confirmed correct | 90+ |

---

## STALE_DOC — fixed this run

### `docs/JOURNEY-BUGS.md` (major reconciliation — 22 findings)
This 2026-07-05 UX bug-hunt report had drifted badly: **R1–R10 (rider), C2–C12 (customer), and S1**
were all marked open/partially-fixed but are now genuinely fixed in code, each verified line-level
(spot examples: `markUndelivered` now exists end-to-end for R1; "Cancel job" now gates on
`RIDER_CANCELLABLE` for R2; `SupportCallRow` now covers the KYC/suspended/on_hold/banned gates for
R4; `PRESENCE_ESCALATION_MS` staleness check now exists for C4; the delivery-code card now gates on
`isActive` for C6; `signOut()` now clears the query cache + device state for S1 — full list in the
file). Updated the status table, the "New surfaces" summary line, and each per-finding section to
✅ FIXED with the current file:line evidence. Left genuinely-open items (C1 partial, and 5 P3 polish
bullets: "Continue verification" silent no-op, all-items-unticked dead-end, compose-card not
dismissed on expiry, auction-clock stuck display, `requested`-status blank screen, StatusPill always
grey) marked open with corrected citations. Kept the original narrative/summary prose intact as
historical context per a footnote, rather than rewriting the report from scratch.

### `docs/FRAUD-REVIEW.md`
- **P3 bullet, "`on_hold` recovery dead-end"** — described self-clear as impossible with no admin
  escape either. `POST /admin/riders/:id/clear-hold` (`admin.controller.ts:188`,
  `admin-riders.service.ts` `clearHold`) shipped this cycle (commit `23b3e0a`), giving admins a real
  "Clear hold" action — previously an `on_hold` rider had no admin action at all (only `suspended`
  riders got Lift/Ban). Reworded to reflect the admin-side fix while correctly keeping the
  self-service gap open (P3 item #17 in the remediation list is still accurate and untouched).

### `docs/GCP-PROVISIONING-REVIEW.md`
- **§5 OTel-collector row** — claimed "a normal release deploy **drops the sidecar**." This is now
  the opposite of both the current code and this same document's own §3 P0 diagnosis: `release.yml`'s
  "Guard against an orphaned collector sidecar" step (added this cycle) exists specifically because
  `gcloud run deploy --image` **preserves** existing sidecars — that's the confirmed root cause of the
  2026-07-08→07-10 prod outage documented earlier in the same file. Corrected the row and pointed at
  the new guard.
- **§3 hardening follow-up, Dockerfile citation** — said `apps/api/Dockerfile:19`; the
  `--frozen-lockfile=false` flag is actually on line 28 (19 is an unrelated comment). Fixed the line
  number; the "3 of 6 workspace manifests copied" reasoning was already correct.

### `docs/LAUNCH-DEPLOYMENT-STRATEGY.md`
- **§0.5 status table, "Staging stack" row** — said staging was still pending founder apply+arm.
  Independently confirmed via the GitHub Actions API that `deploy-staging.yml` has run green on every
  `main` push since 2026-07-08 (run #4 onward) — staging is applied and armed, matching
  `GCP-PROVISIONING-REVIEW.md` §2's finding. Reworded the row (the in-repo Terraform *default* is
  still `staging_enabled=false`, which is accurate and untouched — only the "still pending" framing
  was stale).

### `docs/LAUNCH-READINESS.md`
- **LR11 status cell** — said the load harness "needs a staging stack + OTEL live (founder) to run,"
  implying staging didn't exist yet. Same staging-is-live evidence as above; reworded so the only
  remaining blocker named is OTEL (LR9), not the staging stack itself.

---

## Confirmed correct / no action (90+ claims checked)

- **`docs/DEPLOYMENT-AUTOHEAL.md`** — all 8 claim groups (staging-gate ordering, same-image
  promotion, unarmed-mode pass-throughs, automatic retry + backoff, the "gate-only-failure isn't
  retried" exclusion, the escalation issue's contents + auto-close, manual overrides, and all 6
  tunable defaults) verified exactly against `release.yml`, `deploy-staging.yml`,
  `deploy-autoheal.yml`, `rollback.yml`, `gcp-diagnose.yml`. Zero mismatches.
- **`docs/UX-USABILITY-REVIEW-2026-07-09.md`** — all 8 numbered findings' claimed fixes verified
  present in code (history subtitle, KYC-status footer, cancelled-terminal reason, role-toggle
  confirm, admin stuck-note, OTP-mismatch banner, cancel-strike copy, tappable earnings rows), plus
  both sanity checks (deferred `expo-image-manipulator`, no accumulated-positions array for #9).
  Zero mismatches (two trivial ±2-line citation drifts, not worth editing).
- **`docs/GCP-PROVISIONING-REVIEW.md`** — remaining claims (WhatsApp OTP 503 + boot-guard, Prisma
  connect/env-validation crash timing, Cloud Scheduler / push-provider / P2-deferral gaps, the
  verify script) all confirmed against code beyond the two fixes above.
- **`docs/OBSERVABILITY.md`, `docs/LAUNCH-DEPLOYMENT-STRATEGY.md` OTA row, `docs/plans/UX-FIX-DESIGN-GAP-PLAN.md`**
  — these were already corrected by the developer in the same commits that changed the underlying
  code (sidecar-preservation note, EAS project-linked OTA status, honest 2-hour cooldown copy);
  spot-checked against current code and confirmed still accurate.

---

## Worth knowing, not a discrepancy

- `docs/JOURNEY-BUGS.md` C1 (map-pin fallback) is now a *better* partial fix than before — a
  `ComposeMap.tsx` timeout+fallback UI landed (`map-fallback.ts`, new this cycle) — but genuinely
  still incomplete: `coordsOk` still requires a real lat/lng, and the fallback's "type your landmark"
  affordance doesn't satisfy that for a keyless build's drop-off pin (only address search or "use my
  location," and the latter only sets pickup). Left the doc's 🟡 partial status as-is since that's
  still accurate — not upgraded to fixed.
- `docs/FRAUD-REVIEW.md` P3 remediation item #17 ("on_hold self-recovery path") remains open and
  accurate — the fix this cycle is admin-initiated recovery, not rider self-service.
- `docs/LAUNCH-EXECUTION-RUNBOOK.md` §8e (staging-stack runbook) was flagged by
  `GCP-PROVISIONING-REVIEW.md` alongside the two docs fixed above, but its content is a procedure
  ("run these commands to arm it"), not a status claim — still valid instructions regardless of
  whether they've already been run once. No edit needed.

## Method note

Re-running this routine with no further code/design changes should produce zero additional doc
edits (determinism requirement) until the next real drift.
