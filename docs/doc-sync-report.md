# Documentation Sync Report

Generated by the doc-reconciliation routine. Incremental pass against the marker in
`docs/.last-doc-sync` (previous marker `243aaed`, this run's HEAD `37f0f3f`, 2026-07-11). That range
(11 commits) landed the 2026-07-10 UX-review pass (`e44df1f`), a `confirmItems` CAS-guard fix
(`395b700`), and a one-shot EAS/Play arming script (`0c8898d`) — plus a dependency bump and merge
commits with no doc-relevant content. Traced every code change in range to `file:line` citations in
the docs, verified each against the current source, and fixed what had actually drifted.

While tracing `order-lifecycle.service.ts` and `rider/index.tsx`/`rider/job.tsx` citations (both
files touched in this window), found a substantial amount of **pre-existing** line-citation drift in
`MOCKUP-ALIGNMENT-REVIEW.md` — predating this window, not caused by the commits reconciled here — that
this incremental pass did not attempt to fix (see AMBIGUOUS below).

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 3 files (10 individual claims) |
| CODE_BUG | 0 |
| ORPHAN | 0 |
| AMBIGUOUS | 1 (flagged, not edited) |
| Claims checked and confirmed correct | 20+ |

---

## STALE_DOC — fixed this run

### `docs/JOURNEY-BUGS.md`
- **R5 (on_hold copy).** Quoted the old `gates.ts` on-hold message ("Complete a few clean trips to
  recover it...") as still current. `e44df1f` rewrote that copy (`apps/mobile/src/logic/gates.ts:87`)
  to drop the self-recovery language entirely — it now just points to support. Updated the finding to
  quote the real current copy instead of the retired one.
- **R6 citation.** Pointed at `rider/index.tsx:256-262`, which (even before this window, checked
  against the `243aaed` baseline) held unrelated warm-resume code, not the "card the rider must
  manually tap" the finding describes. Retargeted to the actual `activeJob` card block
  (`rider/index.tsx:391-409`), content-verified against the current file.
- **R1, R8 citations** (`rider/job.tsx:216` → `:223`, `rider/job.tsx:264-281` → `:271-288`). Both
  citations were accurate against the `243aaed` baseline (content-matched: the `markUndelivered`
  mutation for R1, the `CancelledHandback` block for R8) but drifted by the `+7`-line shift `e44df1f`
  introduced earlier in the file (job-poll-fallback wiring at old lines 65-71 and 128-134). Updated to
  the current lines, re-verified against the live file.
- **R5's confirmDelivery-adjacent citations were not touched** — confirmed no other finding in this
  file cites `order-lifecycle.service.ts` lines that moved.

### `docs/FRAUD-REVIEW.md`
`order-lifecycle.service.ts` shifted by `+3` lines after the `confirmItems` CAS-guard change
(`395b700`, `:204-230`) and by a further `+2` after the `cancel()` plain-language message change
(`e44df1f`, `:459`) for everything below it. Four citations fell in the shifted region; each was
content-verified against the `243aaed` baseline before applying the offset, then re-verified against
the current file:
- P0-3 `markUndelivered`: `:273-326` → `:276-329`
- P1-6 `rate`/`rateSender`: `:353-365`/`:379-403` → `:356-368`/`:382-406`
- P2-4 `cancelStrikes` reset: `:463-469` → `:468-474`
- P2-6 `confirmDelivery` (OTP withhold): `:221-263` → `:233-275` (retargeted to the function's actual
  current boundaries rather than a straight offset — the original citation already started a few lines
  into the neighboring `confirmItems` function even at `243aaed`)

### `docs/UX-USABILITY-REVIEW-2026-07-10.md`
Cancel-conflict citation `order-lifecycle.service.ts:454` → `:459` — the finding's own fix
(`395b700`, landed in the same window on a separate lane) shifted the line after this doc's line was
written.

---

## AMBIGUOUS — flagged, not edited

### `docs/MOCKUP-ALIGNMENT-REVIEW.md` — pervasive rider/index.tsx and rider/job.tsx line drift
While computing offsets for the `rider/index.tsx` (`e44df1f`, three insertion points, cumulative
`+11`/`+49`) and `rider/job.tsx` (`+7`) changes in this window, checked every `rider/index.tsx:NNN` and
`rider/job.tsx:NNN` citation in this doc against the `243aaed` baseline before trusting an offset. Most
did **not** match their described content even at the baseline commit (e.g. `rider/index.tsx:319-329`
cited for the "A customer picked you!" card actually held `chooseOrder()`'s ETA-seeding logic;
`rider/job.tsx:180-187` cited for `cooldownUntil` discarding actually held OTP-retry haptics code) —
this drift predates the window this pass covers and wasn't introduced by any commit reconciled here.
Given the number of affected rows (roughly a dozen across both files) and that guessing corrected line
numbers without a reliable anchor risks fabricating citations, this pass left them as-is rather than
guess. **Recommend a dedicated non-incremental pass over this doc** (walk every row's citation against
current source, independent of any commit-range diff) rather than folding it into a routine
incremental sync.

---

## Claims verified correct, no edit needed
- `docs/plans/LAUNCH-FIX-ROUND1.md` L4 lane scope (mentions `confirmItems` CAS as planned) — a plan
  doc with no status marker, not contradicted by the fix landing; nothing to reconcile.
- `docs/LAUNCH-READINESS.md`/`FRAUD-REVIEW.md` — no existing claim asserted `confirmItems` was
  unguarded, so the `395b700` fix closes a gap the docs never claimed was open.
- `docs/LAUNCH-DEPLOYMENT-STRATEGY.md` / `docs/LAUNCH-EXECUTION-RUNBOOK.md` EAS/Play arming
  claims — still accurate: `scripts/eas-arm.sh` (added `0c8898d`) automates the scriptable steps but
  still requires a human to run it and complete the Expo-login/Play-Console/token steps, matching the
  docs' "founder arming pending" framing. Verified `--verify` mode and the env vars the script sets
  (`EAS_PROJECT_ID`, `EAS_RELEASE_ENABLED`, `EXPO_TOKEN`) against the actual script content.
- `docs/MOCKUP-ALIGNMENT-REVIEW.md:80` (`addr_map_confirm`, pin-drag landmark auto-fill) — the fix in
  `e44df1f` (`ComposeMap.tsx` `onDragEnd` now calls `setActive()`, re-running reverse-geocode) makes
  this claim newly *true*; it was arguably optimistic before the fix, accurate now.
- `docs/CONCEPT.md:230` ("Rate your rider" as a named journey step) — describes product intent (the
  rating step exists at order completion), unrelated to the `STEP_LABELS.customer.completed` tracking
  UI label bug fixed in `e44df1f`; not the same claim, not stale.
- `docs/FRAUD-REVIEW.md:318` (P3 hardening sweep, "`on_hold` self-recovery path") — a backend-hardening
  TODO item (rate-limiting/abuse angle), distinct from the on-hold UI copy fixed in `e44df1f`; not
  contradicted.
- `docs/MOCKUP-ALIGNMENT-REVIEW.md:92` (`rider_cancelled`, `order-lifecycle.service.ts:502-523`) —
  already matched the current file exactly; no drift despite the same-file line shifts affecting
  nearby citations.
- Dependency bump (`fe62965`, 17 patch-level `@nestjs/*` updates) — no doc cites specific patch
  versions of these packages; nothing to check.

---

Marker updated to `37f0f3f` (this run's HEAD) below.
