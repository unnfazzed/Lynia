# Documentation Sync Report

Generated by the doc-reconciliation routine. Marker-scoped pass: previous marker `cec7f21`
(2026-07-21 02:14 UTC), this run's HEAD `2416ad0` (2026-07-26 05:00 UTC, `main`, after tonight's
bug-hunt/UX/deep-sweep fix routines and the RF-10/RF-11 refactor, session-TTL, Bird SMS-API-port,
Bird delivery-status-webhook, per-device signup-cap, OSV-scanner-migration, and Cloudflare-provider
commits). That range covers 29 commits / 69 files.

Fanned this out across four parallel research passes: (1) auth/OTP/Bird-SMS-webhook/session &
device-id vs `BIRD-SETUP.md`/`SECURITY.md`/`DATA-RETENTION.md`/`PILOT-READINESS.md`/`.env.example`;
(2) admin-audit/order-lifecycle/tracking-gateway/notifications-feed vs
`ARCHITECTURE.md`/`OBSERVABILITY.md`/`REFACTOR-LEDGER.md`/`RF-05-WS-GATEWAY-STATE.md`; (3) mobile
earnings/session/device-state/order-socket vs
`DATA-RETENTION.md`/`PILOT-READINESS.md`/`DESIGN.md`/`APP-SIZE.md`; (4) infra/terraform/CI/MCP/
dependency-policy vs `CLOUDFLARE.md`/`INFRA-HARDENING-ROLLOUT.md`/`SECURITY-OPS.md`/
`LAUNCH-READINESS.md`/`LAUNCH-EXECUTION-RUNBOOK.md`. Verified every flagged claim myself against
current code before editing.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 8 files, 12 claims |
| CODE_BUG (logged, not doc-edited) | 0 |
| ORPHAN | 0 (2 candidates folded into the STALE_DOC fixes below — obsolete env-var names, not dead features) |
| AMBIGUOUS | 4 (all dated-ledger file/symbol drift from later refactors, left untouched) |
| Report-retention stragglers found (pruning blocked, see below) | 13 files across 4 lanes |

---

## STALE_DOC — fixed this run

### Bird SMS arming docs still named the pre-port config (`OTP_CHANNEL=bird`'s Channels-API vars)
`fix(api): port Bird OTP sender to the SMS API` (`2767ecb`) removed `BIRD_WORKSPACE_ID`/
`BIRD_SMS_CHANNEL_ID` from `env.ts` and replaced them with `BIRD_SMS_FROM` — but three docs still
told operators to set the deleted vars:
- `docs/PILOT-READINESS.md:250-252` and `:433-434` — "set the `BIRD_WORKSPACE_ID` /
  `BIRD_SMS_CHANNEL_ID` repo Variables" → now names `BIRD_SMS_FROM` (verified against
  `apps/api/src/config/env.ts:169` and `docs/BIRD-SETUP.md:52`; the "three repo Variables" count
  (`BIRD_SMS_FROM`, `OTP_CHANNEL`, `BIRD_ENABLED`) still holds).
- `docs/BIRD-SETUP.md:176` — "or the workspace/channel IDs are empty" → the actual launch-hygiene
  guard (`.github/workflows/release.yml:244-247`) only checks `BIRD_ENABLED` and `BIRD_SMS_FROM`;
  rewrote to match.
- `docs/BIRD-SETUP.md:181` — "bad channel/workspace id, revoked key, unregistered sender" → the SMS
  API has no channel/workspace concept; rewrote to the actual failure modes logged by
  `apps/api/src/auth/otp-sender.ts:178-180` ("revoked key, no eligible sender for the destination
  (E12003), insufficient wallet balance… unregistered sender").

### `BIRD_WEBHOOK_SECRET` missing from two vendor-secret adoption docs
`secrets.tf`'s `google_secret_manager_secret.vendor` `for_each` (`infra/terraform/secrets.tf:108-115`)
gained `BIRD_WEBHOOK_SECRET` alongside the existing four (for the delivery-status webhook,
`189954c`), but two docs describing that `for_each` list weren't updated:
- `docs/LAUNCH-EXECUTION-RUNBOOK.md:302` — the manual-fallback `for s in …` loop was missing it;
  added.
- `scripts/adopt-vendor-secrets.sh:3-4` (header comment) already had it in the executable
  `SECRETS=(...)` array (line 29) but not in the comment describing what the array imports —
  comment/code drift within the same file; synced.

### `tracking.gateway.ts` grew from 784→828 lines (BH-25's `boardOpChain` serializer)
`fix(api,mobile): rider presence-recovery push + board-room re-scope race (BH-25a/BH-25b)`
(`b7935f3`) added a 6th per-process structure, `boardOpChain` (`tracking.gateway.ts:123-126`), and
grew the file to 828 lines (verified `wc -l`; was 784 at `cec7f21`). Two docs cited the stale count:
- `docs/RF-05-WS-GATEWAY-STATE.md:9` — "784 lines" → 828; the "holds five... structures" table is
  RF-05's own Redis-vs-process classification of the *original* five and still correctly describes
  only those (folding `boardOpChain` into that table would wrongly imply it needs the same
  Redis-placement analysis — it's a same-process synchronization primitive, not routing/presence
  data). Added a short paragraph after the table naming the 6th structure and explicitly scoping it
  out of RF-05's question, instead of listing it as a 6th classified row.
- `docs/RF-05-WS-GATEWAY-STATE.md:62` — "shrinks the 784-line gateway" → 828.
- `docs/REFACTOR-LEDGER.md:25` (hotspot-map row 7) — "grew to 784 lines (was 770)" → "grew to 828
  lines (was 784)".

### Three more hotspot-map line counts drifted, in files this range touched
While fixing the `tracking.gateway.ts` row (`docs/REFACTOR-LEDGER.md`, a persistently-live table,
not a dated report) I checked the other rows for files that changed in this range and found three
more actual-vs-documented drifts, all now fixed:
- Row 5 (`docs/REFACTOR-LEDGER.md:23`) — `session.ts`/`device-state.ts` post-RF-10 split documented
  as "589→65 lines + new ~527"; current `session.ts` is 68 lines (`device-state.ts` is exactly 527,
  already accurate) — `65`→`68`, `~527`→`527`.
- Row 6 (`:24`) — `privacy.service.ts` post-RF-11 documented as "441 lines"; `0a4f05d` (session-TTL
  work) touched the file again after RF-11 landed and it's now 483 lines — `441`→`483`.
- Row 2 (`:20`) — `order-lifecycle.service.ts` documented as "874+ lines"; the file grew
  1041→1058 lines within this range (`git diff --stat` confirms the in-range touch) — `874+`→`1058`.

### CI now runs `terraform validate` — `LAUNCH-READINESS.md` still said it never had
`fix(infra): empty Cloudflare token bricked every terraform command` (`7e655f3`) added a
`terraform-validate` job to `.github/workflows/ci.yml:239-261` (defaults-only, `-backend=false`, no
GCP auth, runs on every PR/push — no `infra/`-path filter). `docs/LAUNCH-READINESS.md:196-200` still
read "it has never run in CI" and separately claimed CI runs it "on PRs touching `infra/`" (both now
false — the first is stale, the second described a path-filter the job doesn't have). Rewrote both
to state the job exists and its actual (unfiltered) trigger scope. `docs/CLOUDFLARE.md`'s own
description of this same job (added in the same commit, `7e655f3`) was already correct — only
`LAUNCH-READINESS.md`, updated by a different commit, had drifted.

---

## CODE_BUG

None found this run — no doc claim across the four lanes reflected a real design/PRD decision that
current code contradicts.

## ORPHAN

None as a distinct class this run — the two Bird Channels-API var names (`BIRD_WORKSPACE_ID`,
`BIRD_SMS_CHANNEL_ID`) that no longer exist in code were folded into the Bird-arming STALE_DOC fixes
above (wording that told operators to configure a deleted var, not a doc describing an abandoned
feature) rather than double-counted as a separate class.

## AMBIGUOUS — flagged, not edited

1. **`docs/KNOWN_BUGS.md:1342`** (IR16-10, dated 2026-07-16) cites `RL.deviceSignup` for the
   per-device signup throttle. `65350ff`/`e2d8d36` (this range) renamed the symbol to
   `rlFrom(env).deviceSignup` / env var `OTP_RL_DEVICE_SIGNUP_MAX` and made the recycle check
   fail-safe on an absent device id — neither commit touched `KNOWN_BUGS.md`. Whether a dated,
   append-only ledger entry should get a follow-up row for a later symbol rename, or stand as an
   accurate-at-the-time historical record, is a policy call outside this lane's remit. Left as-is.
2. **`docs/plans/2026-identity-and-pod-hardening.md:27`** — same `RL.deviceSignup` staleness, same
   caveat (design-plan doc, not covered by this run's assigned doc list, surfaced only because
   `auth.service.ts` comments point at it).
3. **`docs/KNOWN_BUGS.md:1115`** (BH-23) and **`:1626`** (BH-17) cite `apps/mobile/src/auth/session.ts`
   as where `clearDeviceState()`'s sign-out wipe lives. RF-10 (`ca38502`, this range) moved the
   function's definition to `device-state.ts`, re-exported through `session.ts`. The citation still
   resolves correctly (imports from `session.ts` still work) but no longer names the defining file.
   Same append-only-ledger-vs-refactor-drift judgment call as #1 — left as-is.
4. **`docs/REFACTOR-LEDGER.md`** debt-register row `RF-05` (`:51`) cross-references
   `RF-05-WS-GATEWAY-STATE.md` without its own line count, so it inherited the design doc's fix
   automatically — noted for completeness, not a separate edit.

---

## Report retention — stragglers found, pruning still blocked

`docs/ROUTINES.md`'s report-retention policy ("only the most recent report per lane stays on
`main`... **The doc-sync routine prunes any stragglers**") is still violated, and has gotten worse
since the 2026-07-21 sync flagged the same class of problem:

- `docs/PR-HEALTH-REPORT-*.md`: **7** present (`2026-07-19-0220`, `2026-07-19-2010`,
  `2026-07-20-0222`, `2026-07-20-0816`, `2026-07-20-2016`, `2026-07-21-0214`, `2026-07-21-1420`) —
  only `2026-07-21-1420` should remain.
- `docs/UX-USABILITY-REVIEW-*.md`: 3 present (`2026-07-19`, `2026-07-20`, `2026-07-21`) — only
  `2026-07-21` should remain.
- `docs/DEEP-SWEEP-*.md`: 3 present (`2026-07-19`, `2026-07-20`, `2026-07-21`) — only `2026-07-21`
  should remain.
- `docs/WALLET-DATA-AUDIT-*.md`: 3 present (`2026-07-17`, `2026-07-19`, `2026-07-21`) — only
  `2026-07-21` should remain.
- `docs/REFACTOR-2026-*.md`: **new straggler this run** — 2 present (`2026-07-19`, `2026-07-21`),
  was correctly pruned to 1 as of the 2026-07-21 sync. Only `2026-07-21` should remain.
- `docs/BUG-HUNT-*.md`: still correctly pruned to 1 file (`2026-07-25`) — no action needed.

This session's sandbox denies both `git rm` and plain `rm` on these files (`Permission for this
action was denied by the Claude Code auto mode classifier` — a session-level block on file deletion,
independent of file content, same as the 2026-07-21 run hit). Re-flagging for a human or the
PR-health watchdog to prune the 13 straggler files above. This is now the **second consecutive**
doc-sync run this policy has failed to self-execute — worth a human decision on whether the pruning
step needs a different execution path (e.g. the PR-health watchdog's environment, which may not
carry the same classifier restriction) rather than being re-attempted by doc-sync indefinitely.

---

## Determinism note

Re-running this pass against an unchanged `2416ad0` should produce zero further doc edits — every
fixed claim above is now reflected in the tree, and every other claim checked across the four lanes
already matched code or design. The report-retention stragglers are a known, carried-forward
exception to determinism (blocked by sandbox permissions, not a doc-content issue) until a human or
a differently-privileged routine prunes them.
