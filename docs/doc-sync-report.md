# Documentation Sync Report

Generated by the doc-reconciliation routine. Incremental pass against the marker in
`docs/.last-doc-sync` (previous marker `808df80`, 2026-07-13, this run's HEAD `2ffe0d3`, 2026-07-15).
That range (33 commits) includes the WhatsApp inbound webhook + delivery-status observability
(`whatsapp-webhook.controller.ts`), refresh-token lost-response grace (RT-GRACE, migration
`0025_session_rotation_link`), delivery-code rotation-timestamp signal (migration
`0026_delivery_code_rotated_at`), the BH-03..BH-06 bug-hunt fixes, and the UX15-01..15 UX-review
fixes. Fanned this out across four parallel research passes (WhatsApp/auth/session-rotation,
architecture/route-table, fraud/journey/security citation drift, infra/data-retention/observability)
that traced every falsifiable claim in the touched docs to its current `file:line`, then applied the
STALE_DOC fixes below by hand, re-verifying each one against the cited code myself before editing.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 8 files (29 individual claims/citations) |
| CODE_BUG | 0 |
| ORPHAN | 0 |
| AMBIGUOUS | 0 |
| Claims checked and confirmed correct | 60+ |

---

## STALE_DOC — fixed this run

### `docs/ARCHITECTURE.md`
- **§8 refresh-rotation sequence + security bullet**: the diagram and prose described refresh as a
  hard "verify not revoked/expired" gate with no exception. Code now has a 60-second lost-response
  grace (RT-GRACE, KNOWN_BUGS.md DS14-08): a token revoked by its own rotation (not by logout) with an
  un-consumed successor within 60s gets a fresh session instead of a hard 401
  (`apps/api/src/auth/auth.service.ts:308-374,392-409`; migration `0025_session_rotation_link`).
  Updated the sequence diagram and added a bullet describing the grace path.
- **§9 online-gate description**: said `PATCH /riders/online`/`POST /orders/:id/offers` require only
  `kycStatus=verified` + cooldown. Code's actual gate (`rider.service.ts:253-260`, comment: "Full
  online-gate (Q2): kyc + account standing + reliability on_hold + cooldown") also blocks a
  suspended/banned (`accountStatus`) or automated-hold (`onHold`) rider. Corrected the sentence.
- **§16 REST table + auth-exception sentence**: missing the new WhatsApp webhook entirely.
  `apps/api/src/auth/whatsapp-webhook.controller.ts:14` registers unauthenticated `GET`/`POST
  /webhooks/whatsapp` (confirmed no `JwtAuthGuard`, `auth.module.ts:13,17`). Added both route rows and
  added `/webhooks/whatsapp` to the unauthenticated-routes list. Also added the missing
  `POST /admin/riders/:id/clear-hold` row (`admin.controller.ts:212`) — a real route with no table
  entry at all.
- **Master diagram + §1 system-context diagram**: the WhatsApp edge was drawn outbound-only
  ("send code"), but the new webhook is a real inbound path (delivery-status HMAC callback), the same
  shape as the already-drawn Didit webhook edge. Added the inbound edge/legend note and made the
  system-context arrow bidirectional, mirroring how Didit is already drawn.
- **§4 API module map**: the "everything else is a feature lane" diagram was missing six real,
  routed Nest modules (`IssuesModule`, `ReportsModule`, `SosModule`, `PrivacyModule`,
  `SettlementsModule`, `ClientMetricsModule`) — all confirmed present with their own controllers/routes
  already listed in §16. Added them as lanes.

### `docs/FRAUD-REVIEW.md`
Five findings whose underlying code has since been fixed/mooted but which carried no annotation (or a
drifted citation), unlike the doc's own P0-2/P0-3 precedent of marking fixed/moot findings inline:
- **P0-1**: citation-only — `selectOffer`'s self-deal re-check is now at `matching.service.ts:89`
  (was `:79`).
- **P1-1** (refund netting): the weekly-netting mechanic is MOOT (rewritten `settlements.service.ts`);
  independently, the doc's own proposed fix (cap refund to the order's fare) is now implemented —
  `issues.service.ts:223-229`'s `fareCap` check. Added both notes.
- **P1-2** (KYC attempt-lock): **FIXED (verified)** — `rider.service.ts:391`, a vendor auto-decline now
  increments `kycAttempts` inside the same monotonic guard (F-13). Citations corrected: `applyKycResult`
  now `:369`, `adminSetKyc` `:455`, retry lock `:201-211`.
- **P1-4** (OTP verify TOCTOU + unthrottled): **FIXED (verified), both halves** — `auth.service.ts:272`
  increments before comparing (closes the TOCTOU); `auth.controller.ts:34` carries
  `@Throttle({limit:10,windowSec:300})`. Citations corrected throughout.
  (`otp-store.ts:51-56,105-107`).
- **P2-1/P2-2** (admin `adjustFare` no floor / `recordPayment` no proof): both **MOOT** — `adjustFare`
  moved to `admin-orders.service.ts:156-184` with the "PAID week" guard explicitly removed (comment at
  `:163-164`); `recordPayment` no longer exists anywhere in the codebase (confirmed via repo-wide grep).

### `docs/JOURNEY-BUGS.md`
- **R1** citation: `job.tsx:223` → `:345` (the `undeliverM` mutation) after `job.tsx` grew from later
  durable-terminal work.
- **R6** citation: `use-push-registration.ts:52-76` → `:159-177` (the push-token-race guard added ~100
  lines ahead of the cold-start block, DS14-13).
- **"Continue verification" silent no-op** (P3): marked "Still open" — **FIXED (BH-03)**,
  `gates.ts:178-189`'s `resolveKycRetryFeedback` now always returns `openUrl`/`info`/`error`, wired at
  `rider/index.tsx:287-289`. Marked ✅.
- **"Compose card isn't dismissed on mid-compose expiry"** (P3): marked "Still open" — **FIXED**,
  `rider/index.tsx:362-369` clears `selected` when the board reports the order gone. Marked ✅.
- **PickupChecklist all-unticked dead-end** citation: `:78-83` → `:176-179` (narrative unchanged,
  still open — the file grew a photo-upload block ahead of the button).

### `docs/SECURITY.md`
Three citation-only drifts (all confirmed narrative-accurate, only line numbers stale from unrelated
intervening additions — data-retention env vars, key-separation/rotation work, the WhatsApp sender):
- P0-1: `env.ts:57` (default) → `:65`; `:139-144` (prod guard) → `:181-196`.
- P2-3: `token.service.ts:28` → `:50,53` (the actual `{algorithms:["HS256"]}` pins).
- P1-4: `otp-sender.ts:96,109` → `:101,114`.

### `docs/UX-USABILITY-REVIEW-2026-07-12.md`
Four citations that had drifted a **second time** since a prior sync pass already corrected them once
(see the previous report) — confirming the "corrected once, drifted again as more code lands
underneath" pattern this routine watches for:
- Finding #1: `orders.service.ts:641-648` → `:573,690` (`getSnapshot` header / `viewerRole` field).
- Finding #12: `tracking.service.ts:516` → `:539` (`addNotifyRequest`).
- Finding #13: `offers.service.ts:71` → `:76` (`REFUSAL_MESSAGE` throw).
- Finding #14: `rider/index.tsx:766-770` → `:892-914` (the confirm-switch card).

### `docs/DATA-RETENTION.md`
Two real erasure/retention steps the code already performs but the doc never enumerated (found while
confirming the two new nullable columns, `Session.rotatedToId`/`Order.deliveryCodeRotatedAt`, are
correctly *not* PII and so correctly absent — that check surfaced these two unrelated, genuine gaps):
- **`SosEvent.lat/lng`** — scrubbed both on erasure (`privacy.service.ts:127-130`) and on the
  `GPS_RETENTION_DAYS` sweep (`:173-176`), on the same clock as the `order_events` GPS trail, but never
  mentioned in the retention table, erasure bullets, or sweep section. Added a table row + bullets in
  both places.
- **Order-embedded `contactPhone`** — `eraseAccount` also strips `contactPhone` out of the
  `pickup`/`dropoff` JSON on every order the erased profile placed as customer
  (`privacy.service.ts:132-152`, `stripWaypointPhone`), which the "Orders... never deleted; PII
  references anonymise via the profile scrub" line didn't cover (that scrub is order-level, not
  profile-level). Added a table row + erasure bullet.

### `docs/SECRET-ROTATION.md`
§5's vendor-key list named `WHATSAPP_ACCESS_TOKEN`/`DIDIT_API_KEY`/`DIDIT_WEBHOOK_SECRET` explicitly
but only covered the two new WhatsApp webhook secrets (`WHATSAPP_APP_SECRET`,
`WHATSAPP_WEBHOOK_VERIFY_TOKEN`, both added since the marker — `.env.example`, `env.ts:106-107`) via an
implicit "…". Named them explicitly and added a fail-closed callout for `WHATSAPP_APP_SECRET` mirroring
the existing `DIDIT_WEBHOOK_SECRET` one (a rotation mismatch stalls delivery-status observability, not
OTP send).

### `docs/OBSERVABILITY.md`
The new `whatsapp_otp_delivery_failed_total` counter (`metrics.service.ts:39,192-193`, wired from
`whatsapp-webhook.controller.ts:76`) was absent from the metric table. Added it — and, since its
`reason` label is Meta's raw error-title string with no code-enforced cardinality bound (unlike every
other label in the "bounded cardinality" section), documented that honestly as the one exception rather
than silently implying it follows the same bounded pattern.

---

## Areas checked with no discrepancies found

- **`docs/WHATSAPP-SETUP.md`** — every claim (routes, env vars, template/copy-button defaults, OTP TTL,
  webhook verify-token handshake, HMAC signature check, failed-delivery logging/metric name, launch
  boot-guards, smoke-test payload shape) traced exactly to `otp-sender.ts`, `whatsapp-webhook.ts`,
  `whatsapp-webhook.controller.ts`, `env.ts`, `metrics.service.ts`.
- **`docs/ARCHITECTURE.md` §5 ERD** — the two new nullable columns (`Session.rotatedToId`,
  `Order.deliveryCodeRotatedAt`) are absent from the ERD's `Session`/`Order` blocks, but the doc already
  self-scopes ("deliberately scoped to the core offer-loop/lifecycle tables... see schema.prisma for
  the complete model") and already omits many other real columns the same way — consistent with an
  existing, accepted convention, not a fresh divergence. Not edited.
- **`docs/GCP-PROVISIONING-REVIEW.md`**, **`docs/GCP-PENDING-REVIEW-2026-07-13.md`** — every
  Scheduler/OTel/IAM/webhook-related claim still matches current Terraform (`scheduler.tf`, `otel.tf`,
  `iam.tf`) and `release.yml`; neither doc references any of this window's six changed areas at all.
- **`docs/SECURITY.md`** implementation-status table (✅/🟨/⬜ rows, no line citations) — spot-checked,
  still accurate; the RT-GRACE change doesn't undermine the "sessions can be revoked" claim since a
  logout-revoke is explicitly excluded from the grace path.
- **`docs/JOURNEY-BUGS.md`** R2/R4/R5/R9/R10/C1-C12 citations — sampled, no contradictions.
- **`docs/FRAUD-REVIEW.md`** P0-1's underlying fix, P1-3, P1-5, P1-6, P3 section — no drift beyond the
  P0-1 citation noted above.

## Determinism note

Re-running this pass against unchanged code/docs should produce zero further edits — every fix above
is tied to a specific `file:line` read and re-verified during this run (grep + direct file reads, not
taken on the research agents' word alone) and cited in the sections above.
