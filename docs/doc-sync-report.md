# Documentation Sync Report

Generated by the doc-reconciliation routine. Incremental pass against the marker in
`docs/.last-doc-sync` (previously `5e9fdd7`). Before diffing, the designated dev branch
(`claude/compassionate-cori-acpqnd`) was found to be a pure ancestor of `origin/main` — its prior
doc-sync commits had already landed on `main` via PR #151 — so the branch was restarted from
`origin/main` (`a731e01`) per the "already merged" rule, then re-diffed against the marker.

That surfaced 9 real commits since the marker (PR #150, "fix/bug-sweep-2026-07-07", plus one CI
allowlist commit) — all functional bug fixes across `apps/api` and `apps/mobile`, no doc changes.
Each fix was checked against every doc claim that could plausibly describe the old (buggy)
behavior.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 3 |
| CODE_BUG | 0 |
| ORPHAN | 0 |
| AMBIGUOUS | 0 |

Six of the nine new commits (jest `testMatch` fix, `DELIVERY_OTP_MAX_ATTEMPTS` centralization,
rider-job-socket self-heal, foreground-refetch, the admin KYC signed-photo-URL fix, and the
gitleaks allowlist) had no corresponding doc claim anywhere in the repo — grepped for their
specific mechanisms (`testMatch`, `addNotificationResponseReceivedListener`-adjacent terms,
`photoUrl`/"reviewer blind", `connect_error`, `AppState`/foreground, `gitleaks`) with no false
claims found. The remaining three fixes each un-did a claim recorded as an **open gap** in a
tracked backlog/bug-list doc.

---

## STALE_DOC — fixed this run

### `docs/JOURNEY-BUGS.md`
- **R6** — "Getting selected doesn't move the rider to the job — no push deep-link, no socket nav"
  was live-verified against `src/push/push.ts` as of the last sync. PR #150 added
  `pushDestination()` (routes rider-only statuses `assigned`/`completed` to `/rider/job`) wired
  through both the live `addNotificationResponseReceivedListener` and a cold-start
  `getLastNotificationResponseAsync()` check (`src/push/use-push-registration.ts:49-57`). Marked
  "✅ FIXED by PR #150" in the heading, matching this file's existing convention for R7/C1 (both
  already carry the same annotation from a prior PR-#98 status pass) — this doc verifiably
  self-updates finding-by-finding rather than being frozen decision history, so this stays
  consistent with its own established pattern.

### `packages/design/CUSTOMER-JOURNEY-AUDIT.md`
- **F-11** — "Double-broadcast / double-submit... Needs idempotency + a guard state." PR #150 added
  a client-generated `idempotencyKey` (`app/home.tsx`, `useMemo`-stable per compose attempt,
  regenerating only when trip content changes; `src/util.ts` `randomUuidV4`) plus a partial unique
  index on `orders(customer_id, idempotency_key)`; `OrdersService.create` (`orders.service.ts`)
  returns the existing order both on a pre-existing match and on a `P2002` from a concurrent
  replay. Proven against real Postgres in `orders-idempotency.int.spec.ts`. Row annotated
  "✅ Fixed (PR #150)" with the implementing files, text otherwise preserved.

### `packages/design/BACKLOG-PLAN.md`
- Same F-11 item in the Wave-2 backlog table — struck through and marked done with a pointer to the
  CUSTOMER-JOURNEY-AUDIT.md entry, so the two stay consistent.

---

## Confirmed correct / no action

Nothing else in the 34-file diff since the marker touched a documented claim:
- `apps/api/prisma/schema.prisma` idempotency-column migrations (`0020`, `0021`) — no doc describes
  the `orders` table's exact column list at that granularity.
- `packages/shared/src/contracts.ts` `DELIVERY_OTP_MAX_ATTEMPTS`/`OfferStatus` typing — internal
  duplication cleanup, value unchanged (still 5); no doc cites where the constant lives.
- Admin KYC signed-photo-URL fix (`admin-riders.service.ts`, `kyc/page.tsx`) — genuinely
  undocumented behavior gap before the fix; `FRAUD-REVIEW.md`/`BUG-HUNT.md`'s KYC sections don't
  claim the reviewer could see the photo, so nothing was false.
- Push/notification-centre `orderId` wiring, rider-job-socket `connect`/`connect_error` self-heal,
  customer-tracking/rider-board foreground refetch — same: real fixes to previously-undocumented
  gaps, not corrections to an existing false claim.
- `.gitleaks.toml` commit-SHA allowlist — `SECURITY.md`/`CONTRIBUTING.md`/`ARCHITECTURE.md` only
  describe the gitleaks job's existence, not specific allowlist entries.

---

## Method note

Re-running this routine with no further code/design changes should produce zero additional doc
edits (determinism requirement) until the next real drift.
