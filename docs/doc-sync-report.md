# Documentation Sync Report

Generated by the doc-reconciliation routine. Marker-scoped pass: previous marker `d829ef8`
(2026-07-20, set by the 2026-07-20 sync run), this run's base HEAD `cec7f21` (2026-07-21 02:14
UTC, post the night's bug-hunt (23:00) and UX (01:00) fix routines — no deep-sweep-2026-07-21
report or commits were present on `main` as of this run's start, so that lane's output is not yet
part of this reconciliation; re-check next run). That range covers 67 commits / 148 files: the
architecture-hardening roadmap (Phases 1–4 — money-math seam, idempotency inventory, retry-
ownership table, order-lifecycle transitions mirror, core/non-core kill switches, contract
back-compat gate, `dependency-cruiser` import boundaries, admin `jsx-a11y`, semantic colour-token
layer), the app-size program (lean icon imports, JS bundle-size budget, measured APK delta), the
RF-05 WS-gateway design pass, the restore-drill runbook + verification script, mobile Sentry
wiring, `BH-23`/`BH-24` (mobile sign-out bid leak, rider-rating corruption), and `UX21-01`/`02`
(admin error-boundary gap, notify-me feed fallback).

Fanned this out across six parallel research passes (module-graph/idempotency/retry/kill-switch
claims in `ARCHITECTURE.md` + a stale line-number citation in `FRAUD-REVIEW.md`; the app-size
program vs `APP-SIZE.md`; Sentry wiring vs `OBSERVABILITY.md` + the restore-drill runbook vs
`RESTORE-DRILL.md`; the RF-05 design doc vs `tracking.gateway.ts` + `REFACTOR-LEDGER.md`;
`PERFORMANCE.md` wave-2b + `QA-DEVICE-CHECKLIST.md` + `DATA-RETENTION.md` + `LAUNCH-READINESS.md`;
and the new `packages/shared` modules — `money.ts`/`design-tokens.ts` `semantic` layer/
`contract-snapshot.json` — vs `DESIGN.md`/`DESIGN-SYSTEM.md`/`PRICING.md`), then independently
re-verified the two surviving findings myself against current code before editing.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 2 files, 2 claims |
| CODE_BUG (logged, not doc-edited) | 0 |
| ORPHAN | 0 |
| AMBIGUOUS | 0 |
| Report-retention stragglers found (pruning blocked, see below) | 9 files across 3 lanes |

---

## STALE_DOC — fixed this run

### `docs/REFACTOR-LEDGER.md:25` — hotspot-map row contradicted its own debt-register row
Row 7 of the hotspot map still read *"grew to 770 lines (was 708) ... OPEN — RF-05, still needs a
design pass (too large for one PR)"*, while the debt-register row for the same file (`RF-05`, line
51) already reads *"SCOPED — design pass done (`docs/RF-05-WS-GATEWAY-STATE.md`, roadmap 3.6): 3
sequenced PRs..."* — two rows of the same ledger disagreeing about whether the design pass had
happened. Verified against the actual file: `apps/api/src/tracking/tracking.gateway.ts` is
currently 784 lines (`wc -l`), and no commits have touched it since `docs/RF-05-WS-GATEWAY-STATE.md`
was authored (`git merge-base --is-ancestor` confirms the design-doc commit is a descendant of the
last gateway-code commit), so none of the design doc's 3 sequenced PRs have landed yet. Updated the
hotspot-map row's line count (770→784) and status to match the debt-register row: `SCOPED — RF-05
design pass done (...); 3 sequenced PRs planned, none landed yet`.

### `docs/DESIGN.md:47-49` — color-token reference missing the new semantic role layer
`docs/DESIGN.md`'s "Color (light theme, AA+ contrast)" table presents itself as the color-token
reference apps consume (the file states `@lynia/shared`'s `design-tokens.ts` "mirrors" the vendored
CSS tokens `packages/design/tokens/*.css`, and the table enumerates that mirror's contents
one-to-one — every `--ink`/`--accent`/etc. row matches a `color.*` key). `design-tokens.ts:63-96`
added a second, additive `semantic` export (roadmap 4.6 — `text.*`/`surface.*`/`border.*`/
`action.*`/`marker.*`/`state.*`, mirroring `--text-*`/`--surface-*`/`--border-*`/`--action-*`/
`--marker-*`/`--state-*` in `packages/design/tokens/colors.css:40-58`) that the table never
mentions, so the doc no longer matches the artifact it claims to describe. Added a short paragraph
after the existing color table naming the semantic layer, its role names, and that each maps
1:1 onto a raw token already in the table (verified against `colors.css:40-58` and
`design-tokens.ts:72-96` — no new colors, purely a naming layer). Left `docs/DESIGN-SYSTEM.md:38-39`
alone — it's phrased as a historical record of one specific LyniaGo-adoption diff, not an evergreen
"this is the full token list" claim, so it isn't actually false.

---

## Claims checked and confirmed accurate (no edit)

7 items in `docs/ARCHITECTURE.md` (the `AppBootstrapModule` wiring + `GET /app/bootstrap` route,
`POST /riders/heartbeat`'s standing-enforcement parity with `PATCH /riders/online`, all 8 rows of
the new idempotency inventory, all 3 rows of the new retry-ownership table, the
`NOTIFICATIONS_FEED_ENABLED` kill switch) and 1 in `docs/FRAUD-REVIEW.md` (the
`admin.controller.ts:239` `clearHold` line-number citation) all matched current code exactly.
`docs/APP-SIZE.md`'s icon-import, bundle-budget, and measured-APK-delta claims all held (the APK
delta is independently corroborated via real GitHub Actions run IDs cited in the doc, not just
asserted). `docs/RESTORE-DRILL.md` and `scripts/restore-drill-verify.sh` are consistent with each
other and with `docs/LAUNCH-READINESS.md`'s unchecked restore-drill box (the runbook correctly
presents itself as prepared-but-not-yet-executed). `docs/RF-05-WS-GATEWAY-STATE.md`'s factual
claims about the current shape of `tracking.gateway.ts` (784 lines, the five named mutable
maps/sets, the `fetchSockets`-vs-`customerPresence` authority split) all held, and it correctly
never claims any of its 3 sequenced PRs are done. `docs/PERFORMANCE.md`'s wave-2b entries
(`AuctionClock.tsx` extraction + its render-isolation test, `home.tsx`'s focus-gated poll) held.
`docs/QA-DEVICE-CHECKLIST.md`, `docs/DATA-RETENTION.md`'s `evictRiderFromSupply` claim, and
`docs/LAUNCH-READINESS.md`'s restore-drill reference all held. `docs/PRICING.md` makes no claim
about the money-arithmetic mechanism that the new `packages/shared/src/money.ts` seam could
contradict, and the code paths it does describe (`suggestFare`/`quoteFare`/`rankOffers`) already
route through `roundToCents`. No doc anywhere claims the new contract-snapshot back-compat gate
(`scripts/contract-snapshot.mjs`, wired into `.github/workflows/ci.yml:91-94`) exists, so there was
no claim to check there.

**Noted but out of scope (no doc claim contradicted):** `apps/api/src/admin/admin.shared.ts:5-7`
still has a local `Math.round`-based `round()` used only for admin-console fare display, not yet
migrated to the `money.ts` seam — `docs/PRICING.md` doesn't describe admin display formatting, so
this isn't a doc discrepancy, just a leftover migration gap worth a future refactor-routine pass.

**Carried forward, unchanged:** `docs/routines/README.md`'s "Performance watch has no mirror file
yet" note (flagged by the 2026-07-20 sync) is still accurate — `docs/routines/*.md` still has no
`performance-watch.md`. Still correctly not auto-authored (a guessed trigger-prompt body would
itself be a stale doc the moment it diverged from the real live trigger); still needs a human or a
future prompt-audit pass.

---

## Report retention — stragglers found, pruning blocked

`docs/ROUTINES.md`'s report-retention policy ("only the most recent report per lane stays on
`main`... **The doc-sync routine prunes any stragglers**") is currently violated in three lanes —
each routine that landed a new dated report failed to delete its lane's previous one in the same
PR:

- `docs/PR-HEALTH-REPORT-*.md`: 6 present (`2026-07-19-0220`, `2026-07-19-2010`, `2026-07-20-0222`,
  `2026-07-20-0816`, `2026-07-20-2016`, `2026-07-21-0214`) — only the last should remain.
- `docs/UX-USABILITY-REVIEW-*.md`: 3 present (`2026-07-19`, `2026-07-20`, `2026-07-21`) — only the
  last should remain.
- `docs/DEEP-SWEEP-*.md`: 2 present (`2026-07-19`, `2026-07-20`) — only the last should remain.
- `docs/WALLET-DATA-AUDIT-*.md`: 2 present (`2026-07-17`, `2026-07-19`) — only the last should
  remain (WD runs every 2nd day, so `2026-07-19` is current).

This run's sandbox denied `git rm` on these files (session-level classifier block on file
deletion, independent of file content) — flagging for a human or the PR-health watchdog to prune
the 9 straggler files above rather than silently skipping the policy. `docs/BUG-HUNT-*.md` and
`docs/REFACTOR-2026-*.md` are both already correctly pruned to one file each.

---

## Determinism note

Re-running this pass against an unchanged `cec7f21` should produce zero further edits — the two
fixes above are now reflected in the tree, and every other checked claim already matched code.
