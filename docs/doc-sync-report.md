# Documentation Sync Report

Generated by the doc-reconciliation routine. Marker-scoped pass: previous marker `210eb6b`
(2026-07-17 05:47, set by the 2026-07-17 sync run), this run's HEAD `da0c16d` (2026-07-18 05:50).
That range covers last night's three fix routines (bug-hunt `BH-15..17`, UX `UX18-01..05`,
deep-sweep `DS18-01..05`), the wallet & data-lifecycle audit (`WD-021..023`), the `RF-02`
notifications refactor, and two release-please chores — 49 files changed under `apps/` +
`packages/shared/`.

Fanned this out across two parallel research passes (backend/API surface vs `ARCHITECTURE.md` /
`SECURITY.md` / `DATA-RETENTION.md` / `FRAUD-REVIEW.md` / `DESIGN-SYSTEM.md` / `PRICING.md` /
`LOAD-MODEL.md`; mobile/admin surface vs `DESIGN.md` / `CONCEPT.md` / `JOURNEY-BUGS.md` /
`docs/plans/*` / the routines mirror files), then independently re-verified every proposed
finding against the cited diff and current doc text myself before editing anything.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 3 files, 6 individual claims/rows/bullets |
| CODE_BUG (logged, not doc-edited) | 0 |
| ORPHAN | 0 new (prior run's wallet-plan ORPHAN annotation untouched — out of this window's diff) |
| AMBIGUOUS | 0 (last run's routines-mirror item is now resolved — see below) |
| Claims checked and confirmed correct | 7 (FRAUD-REVIEW.md KYC section, SECURITY.md wallet/UUID coverage, DESIGN-SYSTEM.md/PRICING.md/LOAD-MODEL.md, JOURNEY-BUGS.md C12, mobile CHANGELOG, ARCHITECTURE.md §4 module diagram) |

---

## STALE_DOC — fixed this run

### `docs/ARCHITECTURE.md:1199` (Appendix — where to look in the code)
Was missing a row for the in-app notifications feed entirely. Since `44cf079`/`ec7d008` (RF-02),
`GET /notifications/feed` (already listed at `ARCHITECTURE.md:1136`) is served by
`NotificationsFeedService.feedForUser` (`apps/api/src/notifications/notifications-feed.service.ts`),
split out of `NotificationsService` and wired alongside it in `notifications.controller.ts` and
`notifications.module.ts` (both providers exported). Added a second appendix row pointing at the
new file. The §4 module diagram's coarse `NotificationsModule<br/>push + device tokens` label
(line 376) was already coarse pre-split and isn't a claim about internal service boundaries, so
left as-is.

### `docs/DATA-RETENTION.md` — three rows + erasure bullets, all from DS18-01/02 (`da0c16d`)
Verified the actual `PrivacyService.eraseAccount` diff in `da0c16d` (`apps/api/src/privacy/privacy.service.ts`)
before editing:
- **Retention-schedule row, "Orders, ratings, audit log" (line 38)**: claimed PII in these tables is
  resolved only "via the profile scrub" (i.e., by reference). DS18-02 added direct scrubs of
  `ratings.comment` (nulled on the rater's own erasure) and `orders.cancelReason` (nulled when the
  erasing user is either party) — content living *inside* those rows, not just a reference to the
  anonymised profile. Updated the cell to say so.
- **"Order item photo / delivery proof" row (line 41)**: column list didn't include
  `orders.pickup_photo_key`. DS18-01 added it to the same rider-scoped erasure pass as
  `deliveryProofKey` (collected into `itemPhotoKeys`, nulled via the same `updateMany`, GCS object
  purged post-commit — confirmed in the diff and in `pii-manifest.ts`'s new entry). Added the column.
- **New row: "Issue / report free text"**: `issues.description` (opener's erasure, emptied to `""`
  since it's a `NOT NULL` column) and `reports.note` (reporter's erasure, nulled) had no row at all —
  net-new scrub paths added in DS18-02, distinct from the pre-existing `orders.note` entry (the
  manifest is now table-qualified specifically so `orders.note` can't falsely "cover" `reports.note`).
- **Erasure bullet list (lines 61-66)**: added two bullets — the pickup-photo GCS purge (DS18-01)
  and the four free-text scrubs (DS18-02) — neither had prose coverage even though the table rows
  above now do.

### `docs/routines/documentation-update.md:36`
Text listed the CODE_BUG owning-lane options as "bug-hunt / UX / deep-sweep," omitting the wallet &
data-lifecycle audit (`WD`) lane. `docs/ROUTINES.md`'s "Bug-dedup protocol (the four bug-finders)"
section and its `docs/KNOWN_BUGS.md` ledger both treat `WD` as a full fourth lane (4 of 6 `DOC-16-*`
rows were tagged `WD` historically). This is the item the 2026-07-17 report flagged **AMBIGUOUS**
because it couldn't tell whether the mirror file was stale or the live trigger genuinely omits `WD`.
Re-read `docs/routines/README.md` this run: it states the mirror file **is** "the reviewed source of
truth" and editing it is explicitly step 1 of a two-step sync (step 2 — pasting the updated body into
the live scheduler trigger — is a manual action outside any tool available to this routine, since
`CronList` only surfaces jobs this session created via `CronCreate`, not the `meta_mcp`-created
routine triggers). So this reclassifies as **STALE_DOC** rather than AMBIGUOUS: the file itself is
safe and correct to fix now, matching `docs/ROUTINES.md`. **Human action still needed:** open the
"Documentation update" trigger in the scheduler UI and paste this file's updated body in, per
`docs/routines/README.md`'s step 2 — the file and the live trigger are not in sync until that's done.

---

## CODE_BUG

None found this run.

## ORPHAN

None new. The prior run's `docs/plans/2026-rider-wallet-design-brief.md` / `-design.md` ORPHAN
annotations (the self-serve top-up flow superseded by `DOC-16-02`'s "contact support" card) are
untouched — no commit in this window's range touches that area.

## AMBIGUOUS

None. The one open item from the 2026-07-17 report (routines-mirror WD-lane omission) is resolved
above as STALE_DOC.

---

## Verified correct, no action needed

- **`docs/FRAUD-REVIEW.md`** KYC section: makes no claim about the DS17-03→DS18-04 lock-bypass race
  (its own P1-2 is a different, already-fixed bug); the doc's status banner already defers to
  `docs/KNOWN_BUGS.md`, which is accurate (`KNOWN_BUGS.md:15,720,767`).
- **`docs/SECURITY.md`**: no mention of `ParseUUIDPipe` or wallet-route UUID validation anywhere
  (grepped) — DS18-05's fix doesn't falsify anything written.
- **`docs/DESIGN-SYSTEM.md`, `docs/PRICING.md`, `docs/LOAD-MODEL.md`**: no references to the
  admin fare-adjust/cancel-order copy, the 404-reason banner, `adminFetchResult`, or `OrderActions`
  (grepped, zero hits) — UX18-01..03 doesn't touch anything these docs claim.
- **`docs/JOURNEY-BUGS.md:214-216` (C12)**: still literally true (`verify.tsx` still reads
  `res.needsProfile` and routes to `/profile/setup`); doesn't describe the deeper interrupted-PATCH
  failure mode BH-15 fixed, but nothing in the sentence is false, so no edit — `KNOWN_BUGS.md:1084`
  already carries the full fix detail.
- **`docs/DESIGN.md`, `docs/CONCEPT.md`**: no rows/claims for the boot-route flow, `advanceM`
  409-reconciliation, the pickup-checklist wipe, or notification coverage of the account-status
  pushes UX18-04/05 fixed — grepped for all of the above, zero relevant hits.
- **`apps/mobile/CHANGELOG.md`**: already lists the `c2b9ed7` (BH-15..17) fix via release-please;
  no manual sync needed.
- **`docs/KNOWN_BUGS.md`** and this window's dated reports (`UX-USABILITY-REVIEW-2026-07-18.md`,
  `WALLET-DATA-AUDIT-2026-07-17.md`, `DEEP-SWEEP-2026-07-18.md`, `BUG-HUNT-2026-07-17.md`): all
  already updated in the same PRs per the routine protocol; spot-checked, accurate.

---

_Line references are as of this run against `da0c16d`; re-verify after any refactor of the cited
files._
