# Documentation Sync Report

Generated by the doc-reconciliation routine. Incremental pass against the marker in
`docs/.last-doc-sync` (previously `a731e01`, this run's HEAD `a456040`). That range (88 files)
turned out to already contain a completed prior doc-sync commit (marker `5e9fdd7` → `a731e01`,
reported previously) plus substantial new work landed alongside it: a new deploy-mechanics doc
(`LAUNCH-DEPLOYMENT-STRATEGY.md`), a PostHog section in `OBSERVABILITY.md`, a founder-arming
section (§8) in `LAUNCH-EXECUTION-RUNBOOK.md`, an FCM API-name fix in `PILOT-READINESS.md`, a new
UX review (`UX-USABILITY-REVIEW-2026-07-08.md`) plus the mobile/admin code implementing its
findings, and infra changes (`staging.tf`, `monitoring.tf`, `iam.tf`, `variables.tf`).

Nearly everything in those new/changed docs makes specific, checkable claims about pipelines,
env vars, and code paths that didn't exist at the last sync — so this run verified each one
against the actual `.github/workflows/*`, `infra/terraform/*`, `apps/api/src/**`, and
`apps/mobile/**` files, rather than trusting the "✅" markers at face value.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 2 |
| CODE_BUG | 0 |
| ORPHAN | 0 |
| AMBIGUOUS | 0 |
| Claims checked and confirmed correct | 26 |

---

## STALE_DOC — fixed this run

### `docs/LAUNCH-DEPLOYMENT-STRATEGY.md`
- **§0.5 status table, "OTA hotfix lane" row** — said "updates explicitly disabled until the EAS
  project id exists." That was true when the doc was written, but a later same-day commit
  (`0b95f53`, "feat(mobile): link EAS project…") hardcoded a real EAS project id as the fallback
  value of `easProjectId` in `apps/mobile/app.config.ts:34`. Since the `updates` config
  (`app.config.ts:48-50`) resolves truthy off that fallback, OTA updates are now live by default —
  only the separate `EAS_RELEASE_ENABLED` gate on the CI *publish* workflow is still dormant.
  Reworded the row to state the project is linked and updates are live, and to name the CI gate as
  the actual remaining dormant mechanism, so a reader doesn't believe OTA is inert.

### `docs/plans/UX-FIX-DESIGN-GAP-PLAN.md`
- **§Cluster A, "A2 · Cooldown" screen spec** — the illustrative copy for this not-yet-built
  empty-state screen said "wait a few minutes." That's the exact dishonest-duration copy that
  `docs/UX-USABILITY-REVIEW-2026-07-08.md` finding #3 flagged as a trust bug (real cooldown is 2
  hours) and that was since fixed in `apps/mobile/src/logic/gates.ts:90-94` (now states "about 2
  hours") and `apps/mobile/app/rider/job.tsx:191-200` (now surfaces the real `cooldownUntil`).
  Left as-is, building this planned screen from the doc would reintroduce the bug that was just
  fixed elsewhere in the same codebase. Updated the illustrative copy to match the shipped,
  honest-duration behavior and pointed at the fix so the two don't drift again.

---

## Confirmed correct / no action (26 claims checked)

**`docs/LAUNCH-DEPLOYMENT-STRATEGY.md` §0.5 status table** — all other 10 rows confirmed against
code: Cloud Run canary (`release.yml` `--no-traffic`/`--tag canary`, `CANARY_STEPS`, 5xx gate via
`request_count`, `iam.tf:74` `monitoring.viewer`), manual rollback (`rollback.yml`), GitHub
Environments (`release.yml`/`mobile-release.yml`/`mobile-ota.yml`/`deploy-staging.yml` all
reference the right environment names), Play staged submit (`mobile-release.yml` gated on
`EAS_RELEASE_ENABLED`, `eas.json` `releaseStatus: inProgress` + `rollout: 0.1`), `versionCode`
discipline (`eas.json` `autoIncrement` + `appVersionSource: remote`), CODEOWNERS + PR template
(both exist with the claimed sections), the §2e LB-`/healthz` smoke correction (confirmed —
`health.controller.ts:14` is `@Get("healthz")`, no `/health` route exists), staging stack
(`staging.tf` gated on `staging_enabled` default-false, `deploy-staging.yml` exists), min-version
gate (`health.controller.ts:31-34` `GET app/version-gate` returns `minSupportedVersion` reading
`MIN_SUPPORTED_APP_VERSION`; mobile fetch is fail-open per `use-server-version-gate.ts:29-30`),
release train (`release-please.yml` + `release-please-config.json`, `app.config.ts:40`
`x-release-please-version` annotation).

**`docs/LAUNCH-EXECUTION-RUNBOOK.md` §8** — all 7 claims confirmed: `AdminOrSchedulerGuard`
(`apps/api/src/auth/admin-or-scheduler.guard.ts:29-83`) gates the Google-OIDC scheduler path on
`SCHEDULER_SERVICE_ACCOUNT` being set; `slo_alerts_enabled` (`variables.tf:223-227`, default
`false`) gates the SLO alert-policy resources in `monitoring.tf:29,61`; the canary tunable names
and defaults (`CANARY_STEPS`/`CANARY_OBSERVE_SECONDS`/`CANARY_MIN_SAMPLE`/`CANARY_MAX_5XX_PCT`/
`PUBLIC_HEALTH_URL`/`CANARY_DISABLED`) match `release.yml` lines 345-355 exactly; `easProjectId`
fallback in `app.config.ts:34` reads `EAS_PROJECT_ID`; `APP_ENV` (`env.ts:27`, default
`"production"`) relaxes the OTP/KYC launch-hygiene guards only `if (env.APP_ENV !== "staging")`
(`env.ts:186-209`) while `release.yml:255,261` hardcodes `APP_ENV=production` directly (not from a
variable); the version-gate route/field/env-var names all match.

**`docs/OBSERVABILITY.md` PostHog section** — confirmed against
`apps/mobile/src/telemetry/analytics.tsx`: no key configured → early return renders children with
no SDK init (line 41-42); `autocapture={false}` explicit (line 44); screen names built from
`useSegments()` route patterns, not resolved paths with real ids (lines 14-22).

**`docs/PILOT-READINESS.md`** — the `fcm.googleapis.com` API-name claim (not
`firebasecloudmessaging.googleapis.com`) matches `infra/terraform/project.tf:36-37` exactly,
including the comment there explaining the old name 404s at enable time; `iam.tf:37-41` grants
the matching `roles/firebasecloudmessaging.admin`. Only the one occurrence exists in the file —
no stale duplicate elsewhere.

**`docs/UX-USABILITY-REVIEW-2026-07-08.md` execution-status note** — spot-checked the 5
highest-severity claims, all confirmed shipped as described: #1 `activeForCustomer`
(`orders.service.ts:446-453`, `orders.controller.ts:90-92`) + `home.tsx` banner (lines 89-98,
505-530); #2 `uuidV4FromSeed` (`util.ts:37-52`) + persisted `idempotencyNonce` in
`order-draft.ts:26-30`, restored before the `useMemo` key derivation in `home.tsx:135,192,343-349`;
#3 `gates.ts:90-94` honest "about 2 hours" copy + `job.tsx:191-200` surfacing `cooldownUntil`; #4
`kyc/page.tsx:184-196` relabeled to "Rider photo" with the fake-liveness tile now annotated
"Honest placeholder"; #5 `push.ts:83-92` `pushDestination` now branches on `kind` for
`broadcast`/`riders_available`.

**Grep sweep for orphaned old copy** — searched all docs/design files for the specific strings the
UX review fixed ("wait a few minutes" beyond the one caught above, "Rider's submitted ID document",
"National ID · front", "en route pickup/dropoff", "Broadcast request" as a literal button label,
`firebasecloudmessaging.googleapis.com`). No further stale hits in living-spec docs. The remaining
matches are in `packages/design/` component-library/exploration files (`Button.prompt.md`,
`ui_kits/`, `_ds_bundle.js`, `Stepper.jsx`, `StatusPill.jsx`) and mockup HTML
(`ui_kits/admin/kyc.html`) — these are design-system source/mockup artifacts, not living specs
describing current production copy, so out of scope per the authority hierarchy (design intent
sources aren't judged against shipped code).

---

## Worth knowing, not a discrepancy

- `packages/design/ui_kits/admin/kyc.html:57` still mocks up a real "Didit liveness capture" tile.
  This isn't stale — it's the *target* design once the vendor-integration gap logged in
  `UX-USABILITY-REVIEW-2026-07-08.md` §6 ("Real Didit ID-document + liveness image fetch… vendor
  integration") is closed. The current code's honest placeholder annotation is a documented
  interim state, not the intended end state, so mockup and code are correctly not in sync yet.

---

## Method note

Re-running this routine with no further code/design changes should produce zero additional doc
edits (determinism requirement) until the next real drift.
