# Documentation Sync Report

Generated by the doc-reconciliation routine. Marker-scoped pass: previous marker `da0c16d`
(2026-07-18 05:50, set by the 2026-07-18 sync run), this run's HEAD `ef3d030` (2026-07-19 04:19).
That range is unusually wide — it covers not just last night's three fix routines
(bug-hunt `BH-18..20`, UX `UX19-01..04`) but a full day of feature work merged since the last
sync: the Bird + local-A2P SMS OTP channels, the admin console going from scaffolded/dormant to
fully armed behind cryptographic IAP-JWT verification, and the admin console engineering/design
review's fixes landing (`d0acd6e`, `7e809a8`, `aa0a107`) — 75 files under `apps/` +
`packages/shared/` + `infra/`.

Fanned this out across two parallel research passes (backend/API/infra/security surface vs
`SECURITY.md` / `SECURITY-OPS.md` / `SECRET-ROTATION.md` / `FRAUD-REVIEW.md` / `WHATSAPP-SETUP.md`
/ `ARCHITECTURE.md` / infra docs; mobile/admin UI surface vs `CONCEPT.md` / `MOCKUP-ALIGNMENT-REVIEW.md`
/ `docs/plans/*` / `KNOWN_BUGS.md` / the dated review docs), then independently re-verified every
proposed finding against the cited diff and current doc/code text myself before editing anything —
this caught one false positive (see "Checked and found NOT a discrepancy" below) and narrowed two
of the research passes' findings (`ADM-07`, `DOC-16-03`) from full-fix to partial-fix once I
confirmed exactly what shipped vs. what's still missing.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 10 files, ~20 individual claims/rows/bullets |
| CODE_BUG (logged, not doc-edited) | 0 |
| ORPHAN | 1 (missing SMS-OTP setup runbook — flagged, not auto-authored) |
| AMBIGUOUS | 0 (the one candidate — ARCHITECTURE.md's OTP-channel prose — reads as a defensible simplification, not a false claim; left as-is) |
| Ledger rows corrected (`KNOWN_BUGS.md`, not new findings — status/scope corrections against current code) | 8 (`ADM-02` through `ADM-10`, `DOC-16-03`) |

---

## STALE_DOC — fixed this run

### `docs/SECURITY.md` — IAP status stale across 5 locations (P0-2 "founder step" → armed)
Since the last sync, `infra/terraform/admin.tf`, `infra/scripts/arm-admin.sh`, and
`apps/admin/app/lib/iap-jwt.ts` (new — verifies the signed `x-goog-iap-jwt-assertion` via IAP's
JWKS, pinning `algorithms:["ES256"]` + `clockTolerance:"30s"`) all shipped, and
`docs/ADMIN-CONSOLE-LAUNCH-SMOKE-TEST.md`'s "Known environment (from prod, 2026-07-18)" table
already documents IAP as enabled in prod. `SECURITY.md` still described this as a pending founder
step in five places:
- **:83-84** (STRIDE trust-boundary diagram) — "◀── currently no operator auth" / "shared admin
  token" edge label → now "◀── IAP (verified JWT, §5 P0-2)" / "IAP + shared admin token".
- **:92-93** (vendor box) and **:397-398** (§7 third-party data flows) — listed only
  "WhatsApp · FCM · Didit"; missing Bird + the local A2P SMS gateway (see the OTP-channel item
  below) → both updated.
- **:193** (status table) — `🟨 ... IAP/SSO+MFA is the founder step` → `✅`, citing `iap-jwt.ts` +
  `admin.tf` + the smoke-test doc.
- **:208** (roadmap) — "P0-2 Admin SSO+MFA (IAP)" was lumped into the still-pending `📋` row with
  Maps-key restriction / WAF tuning / pentest → split out (those three items are still genuinely
  pending; P0-2 is not).
- **:327-328** (P2-4 detail) — "reject boot if `OTP_CHANNEL !== "whatsapp"`" no longer matches
  `env.ts:280-288`'s actual guard (rejects only `console` and the unimplemented `sms` stub; `bird`
  and `local-sms` boot fine in prod, by design — see below) → reworded to match the real guard.

### `docs/SECURITY-OPS.md:10-25,126` (§A admin SSO+MFA runbook)
Described only the plaintext-header trust mode ("IAP sets `X-Goog-Authenticated-User-Email`... no
app change needed"). The code now has a stronger, preferred mode: `apps/admin/middleware.ts`
verifies the signed JWT via `iap-jwt.ts` whenever `ADMIN_CONSOLE_IAP_AUDIENCE` is set (and now
warns in prod if it's unset — `warnedProxyFallback`). Added step 5a recommending the audience var,
and added `iap-jwt.ts` to the quick-reference table (previously listed only `middleware.ts` /
`console-auth.ts`).

### `docs/SECRET-ROTATION.md:22,89-90` (vendor-key rotation)
The rotation schedule table and §5 heading/steps named "WhatsApp, Didit, FCM" exhaustively but
`apps/api/src/config/env.ts` shipped two new bearer-token vendor secrets this window
(`BIRD_ACCESS_KEY`, `LOCAL_SMS_API_KEY`, for the new SMS OTP channels below) with no rotation
entry. Added both to the schedule table and the §5 list.

### OTP channel docs — three sites describing `OTP_CHANNEL=whatsapp` as a hard launch requirement
`env.ts:84` added `bird` (Bird SMS) and `local-sms` (a Zimbabwe A2P gateway fallback) as legitimate
production channels alongside `whatsapp`/`console`; the boot-guard (`env.ts:280-288`) only rejects
`console` and the stub `sms` value, never `bird`/`local-sms`. Three places asserted the older,
narrower rule:
- **`apps/api/src/config/env.ts:89`** (comment on `OTP_TEST_PHONES`) — "MUST be empty (and
  `OTP_CHANNEL=whatsapp`) before real launch" → corrected to "OTP_CHANNEL not console or the
  unimplemented sms stub" (doc comment, in scope per the routine's "doc comments treated as spec").
- **`docs/WHATSAPP-SETUP.md:82-85`** — same claim, same fix, plus a note that `bird`/`local-sms`
  are valid production fallbacks if WhatsApp BSP onboarding is still pending at launch.
- (`docs/PILOT-READINESS.md`'s references to `OTP_CHANNEL=whatsapp` describe the **default**
  value and the founder's chosen primary channel, both still true — not edited.)

### `docs/CONCEPT.md:120` (risk-register item 11 — "WhatsApp-only OTP, no SMS fallback")
This item's own text anticipated exactly this: *"revisit an SMS fallback for the OTP if reach
proves a problem in the corridor."* The Bird + local-A2P channels landing this window are that
anticipated mitigation being built (still opt-in; `whatsapp` remains the default channel, so the
underlying design decision is intact). Added a build-status annotation in the same style the doc
already uses elsewhere (see its existing "Update (2026-07-13/16)" note on the wallet item) — did
**not** rewrite the "WhatsApp-only, no SMS fallback" framing that appears in ~6 other locations in
this doc, since that still accurately describes the *default*, live behavior; only item 11's own
"revisit if needed" clause is what's now stale.

### `docs/MOCKUP-ALIGNMENT-REVIEW.md` — 5 lines quoting/labeling now-changed OTP copy
This is a literal code-tracing audit ("what does the screen currently show"), not an intent doc,
so its quoted copy is a pure code-behavior claim. `apps/mobile/app/phone.tsx:34` changed
`"We'll WhatsApp a one-time code..."` → `"We'll text you a one-time code..."`; `verify.tsx:159`'s
own comment says this is "now that the OTP is delivered over SMS (Bird) rather than WhatsApp" for
autocomplete purposes. Updated the two evidence rows (:72-73, :119-120, including the literal
quote) and the shell-built summary bullet (:199) to say "SMS" instead of "WhatsApp" for the OTP
node specifically (the separate WhatsApp **help-hub** rows, :103/:157/:186, are untouched — that
surface still genuinely uses WhatsApp).

### `docs/FRAUD-REVIEW.md:310` and `docs/plans/2026-identity-and-pod-hardening.md:73,83` — fabricated "48-hour contest window"
`UX19-02` (merged in this window, `fcdca21`) removed a fabricated "48-hour contest window" claim
from 4 code sites because `IssuesService.raise()` has no elapsed-time gating at all — confirmed
fixed in `docs/KNOWN_BUGS.md:1006` and the code (`apps/admin/app/orders/[id]/OrderActions.tsx:86`
now reads "...can report a problem via the app at any time."). Both docs repeated the same
now-false "48 hours" claim describing the same feature (`KB-POD-DISPUTE` Phase B / IR16-12); fixed
both to describe the corrected, no-deadline behavior. (Other "48h"/"48-hour" hits across the repo
— checked — are either unrelated PR-staleness thresholds in the PR-health reports, or accurate
historical bug-report entries describing what the bug *was* before UX19-02 fixed it; left as-is.)

### `docs/ANDROID-LAUNCH-REVIEW-2026-07-18.md:140` (D4 deferred-backlog bullet)
"Notifications feed routes an orderId-less row to `/rider` regardless of role" is exactly `BH-18`,
fixed the same day in a later commit (`03ff2e9`) — confirmed against `push.ts`'s
`notificationRowDestination`. Struck the line with a "Fixed by BH-18" note rather than deleting it
(this is a dated point-in-time review; the rest of its B1-B6/D-list is unaffected and left as-is).

### `docs/ADMIN-ENG-DESIGN-REVIEW-2026-07-18.md` — superseded recommendations
This dated review (code reviewed @ `599f0af`) has a §8 "Prioritized recommendations" list (D1-D5,
J1, J2, E2, E3) that reads as an open TODO. Verified against current code that D1 (Overview
rebuild), D2/D3 (shared shell + KPI grid), D4/D5 (directory/KYC split + standing column), J1
(sign-out), the single-rider half of J2 (wallet UI), E2 (fail-closed auth parse), and E3 (JWT alg
pinning) are all shipped (`d0acd6e`, `7e809a8`, `aa0a107`) — only J2's **bulk seed-credit** action
is still open. Added a superseded-note pointer at the top rather than rewriting the historical
review itself.

### `docs/KNOWN_BUGS.md` — 8 ledger rows corrected against current code
The ledger is the routine's own "is this bug still open" doc, so a row saying **OPEN** when the
code has since shipped the fix is exactly the STALE_DOC case, just in ledger form rather than
prose. Verified each individually against the actual diff/current code (not just trusting the
`ADMIN-ENG-DESIGN-REVIEW` doc's claims) before flipping status:
- **`ADM-02`** → FIXED — `middleware.ts:43` now fails closed on any value other than
  `false`/`0`/`no`/`off`.
- **`ADM-03`** → FIXED — Overview rebuilt (`<main className="content">`, Completed/Fares-today
  KPIs, "Needs attention" queue with count badge, funnel substrip).
- **`ADM-04`** → FIXED — `globals.css:146` gives `main.content` `flex:1; width:100%`; all three
  pages use the shared shell class.
- **`ADM-05`** → FIXED — `orders/page.tsx`/`riders/page.tsx` use `DataTable`/`StatusPill`/`Pill`;
  no "← Dashboard" leftover; riders directory split from the KYC queue with `accountStatus`-based
  standing pills.
- **`ADM-06`** → FIXED — `Sidebar.tsx` takes `operator`, shows it, and renders an IAP sign-out
  link.
- **`ADM-07`** → **narrowed to PARTIALLY FIXED** — single-rider manual credit shipped
  (`WalletActions.tsx` + `creditRiderWallet`, form-open idempotency, transactional ledger+audit).
  Still open: the admin-console **bulk seed-credit** campaign action (grepped `apps/api/src
  apps/admin/app` for `seed-credit`/`campaignId`/`bulkCredit` — zero hits).
- **`ADM-08`** → FIXED — `ConfirmModal` now traps/restores focus, uses `<fieldset>/<legend>` and
  real `<label htmlFor>`, and has an `<h3>` title.
- **`ADM-10`** → FIXED — `iap-jwt.ts` pins `algorithms:["ES256"]` + `clockTolerance`; unset-audience
  fallback logs a one-time prod warning; `StatusPill` has `requested`; `mutateRider` dropped the
  dead `target` param; `console-auth.ts` matches `"/icon."` (with the dot).
- **`DOC-16-03`** → **narrowed** — same evidence as `ADM-07`: the rider-detail wallet UI is now
  built (and its manual-credit cap **matches the design brief exactly** — see "Checked and found
  NOT a discrepancy" below); re-scoped the row to just the still-missing bulk seed-credit action,
  marked as a duplicate of `ADM-07`.

Historical narrative blocks elsewhere in `KNOWN_BUGS.md` that describe past run outcomes (e.g. the
2026-07-16 WD-audit prose confirming `DOC-16-03` was open *at that time*, or `IR16-12`/`UX17-03`'s
descriptions of the pre-UX19-02 "48-hour" copy) were **not** touched — they're accurate as
point-in-time records; only the live ledger rows describing current status were corrected.

---

## Checked and found NOT a discrepancy (caught by independent re-verification)

The mobile/admin research pass flagged `docs/plans/2026-rider-wallet-design-brief.md:107`'s "manual
credit form (amount capped at $50...)" as **AMBIGUOUS**, since the shipped `WalletActions.tsx`
form has no visible `max` attribute and the reviewer couldn't find a cap in
`wallet.service.ts` on a first pass. Re-checked directly: the cap **is** enforced server-side —
`apps/api/src/wallet/wallet.service.ts:478-481`'s `creditManual` throws
`ForbiddenException` above `WALLET_MANUAL_CREDIT_CAP_USD` (`env.ts:230`, `.default(50)`) — an
env-tunable $50 default, exactly matching the design brief. Not a stale doc; no edit made. (This
is why the routine's process re-verifies research-pass findings against code directly rather than
trusting a research summary — a UI-only grep would have missed the backend enforcement.)

## ORPHAN

**Missing setup runbook for the new Bird / local-A2P SMS OTP channels.** `docs/WHATSAPP-SETUP.md`
is a full Meta-onboarding runbook for `OTP_CHANNEL=whatsapp`; there is no equivalent for
`bird`/`local-sms` despite ~150 new lines of sender code and 8 new env vars
(`BIRD_ACCESS_KEY`, `BIRD_WORKSPACE_ID`, `BIRD_SMS_CHANNEL_ID`, `BIRD_BASE_URL`,
`BIRD_BRAND_NAME`, `BIRD_ANDROID_SMS_HASH`, `LOCAL_SMS_API_URL`, `LOCAL_SMS_API_KEY`,
`LOCAL_SMS_SENDER_ID`, `LOCAL_SMS_ANDROID_HASH` — `apps/api/src/config/env.ts:111-142`). This
isn't a case of a doc describing something that no longer exists (the usual ORPHAN shape); it's
new capability with no runbook yet. Not auto-authored — writing a full vendor-onboarding runbook is
new content, not a reconciliation edit, and per the routine's scope only `STALE_DOC` gets
auto-fixed. **Flagging for a human decision:** either a new `docs/SMS-OTP-SETUP.md` or a section
appended to `WHATSAPP-SETUP.md` (retitled to cover all SMS channels).

## AMBIGUOUS

None this run. The one borderline candidate — `docs/ARCHITECTURE.md:694`'s "A one-time code
(WhatsApp / SMS / console) proves phone ownership" — reads as a defensible umbrella simplification
(Bird and local-sms are both SMS-delivery mechanisms) rather than a false claim; left as-is.

## CODE_BUG

None found. No discrepancy this run looked like a doc encoding a genuine design decision that the
code has since regressed from — every gap traced to a doc lagging a shipped, intentional change.

---

## Verified correct, no action needed

- **`docs/CLOUDFLARE.md`**, **`docs/MOBILE-CERT-PINNING.md`**, **`docs/INFRA-HARDENING-ROLLOUT.md`**,
  **`docs/OBSERVABILITY.md`**, **`docs/LOAD-MODEL.md`**, **`docs/PRICING.md`**, **`docs/DATA-RETENTION.md`**,
  **`docs/IR-RUNBOOK.md`**, **`docs/DEPLOYMENT-AUTOHEAL.md`** — untouched by this diff range or
  spot-checked against current code/infra; no drift found.
- **`docs/plans/2026-admin-console-arming.md`**, **`docs/plans/2026-admin-console-deployment.md`** —
  cross-checked against `admin.tf`, `deploy-admin.yml`, `arm-admin.sh`, `iap-jwt.ts`; all
  phases/verify-steps match shipped code.
- **`docs/ADMIN-CONSOLE-LAUNCH-SMOKE-TEST.md`** — every checked claim (sign-out, Overview KPIs,
  directory/KYC nav, banned-rider banner, wallet-credit idempotency) matches current code.
- **`docs/GCP-PROVISIONING-REVIEW.md`** — self-marked superseded; out of scope, no new
  contradiction introduced.
- **`docs/KNOWN_BUGS.md` `ALR-01..09`**, **`docs/UX-USABILITY-REVIEW-2026-07-19.md`**,
  **`docs/BUG-HUNT-2026-07-18.md`** — all re-checked against the diff; fully accurate.
- **`docs/JOURNEY-BUGS.md`**, **`docs/INDRIVE-UX-REVIEW.md`**, **`docs/DESIGN.md`**,
  **`docs/DESIGN-SYSTEM.md`**, **`docs/PILOT-READINESS.md`**, **`docs/LAUNCH-READINESS.md`**,
  **`docs/LAUNCH-EXECUTION-RUNBOOK.md`**, **`docs/LAUNCH-DEPLOYMENT-STRATEGY.md`**,
  **`docs/QA-DEVICE-CHECKLIST.md`** — no claims contradicted by this diff.
- **`docs/SECURITY.md` §1, §3 (STRIDE table body), §4, §6, §9, §10** — unaffected, still accurate.

---

_Line references are as of this run against `ef3d030`; re-verify after any refactor of the cited
files. Marker updated to `ef3d030` in `docs/.last-doc-sync`._
