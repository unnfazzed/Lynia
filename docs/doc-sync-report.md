# Documentation Sync Report

Generated by the doc-reconciliation routine. Marker-scoped pass: previous marker `fc11122`
(2026-07-16 05:45, set by the 2026-07-16 sync run `fb3a6a4`), this run's HEAD `210eb6b`
(2026-07-17 05:47). That range covers yesterday's own doc-sync PR plus the night's three fix
routines (bug-hunt `BH-*`/`IR16-*`, UX `UX17-01..07`, deep-sweep `DS17-01..03`) and a release
chore — 71 code/doc files changed under `apps/` + `packages/shared/`. Dominant threads: `WD-012`
(commission-basis fare floor), the proof-of-delivery dispute feature going end-to-end
(`KB-POD-DISPUTE` Phase A `IR16-11` + Phase B `IR16-12`, now CLOSED), `DOC-16-05` (KYC
duplicate-identity auto-verify gap), `IR16-08` (throttle guard subject-keying), `IR16-01`
(refresh hard-rejects a suspended/banned rider), `IR16-09`-era customer-trust rating-weight
gating, `IR16-10` (per-device signup throttle), and the DOC-16-02 top-up-screen rewrite (self-serve
form ripped out in favor of a "contact support" card).

Fanned this out across three parallel research passes (backend/API surface vs `ARCHITECTURE.md`/
`SECURITY.md`/`DATA-RETENTION.md`/`FRAUD-REVIEW.md`; mobile surface vs the wallet plan docs/
`DESIGN.md`/`CONCEPT.md`/`JOURNEY-BUGS.md`; `KNOWN_BUGS.md` ledger + recent dated reports +
routines-mirror consistency), then independently verified every proposed finding against the cited
code myself before editing anything.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 7 files, 17 individual claims/citations/sections |
| CODE_BUG (logged, not doc-edited) | 0 |
| ORPHAN | 1 (annotated in place, not deleted — see below) |
| AMBIGUOUS | 1 (flagged, not edited) |
| Claims checked and confirmed correct | ~20 (5 sampled ledger-fix rows, 2 re-verified cluster headers, 6 routines-mirror files, the wallet balance/ledger screen, the determinism marker) |

---

## STALE_DOC — fixed this run

### `docs/FRAUD-REVIEW.md`
This doc's own status banner already documents itself as stale-by-design ("trust the ledger, not
the per-finding notes"), and follows a convention of prepending a verified status note ahead of the
original finding text. Five spots had fallen behind code that shipped in this window; all five now
carry that same prepended-note treatment, original narrative kept for the exploit history:
- **Status banner (lines 3-9)**: was still listing `KB-POD-DISPUTE` among the "genuinely-still-live"
  set; it closed this window (`IR16-11` + `IR16-12`, verified in `KNOWN_BUGS.md:815`). Also added
  the `WD-012`/`DOC-16-05` fixes to the banner's fixed-list.
- **P0-2 (commission fare floor, ~line 78)**: claimed `chargeCommission` bills on the raw unfloored
  `agreedFare` "same as before." Verified `WalletService.chargeCommission`
  (`wallet.service.ts:221-224`) now calls `perRideCommission(commissionBasis(fare, suggestedFare), rate)`,
  and `commissionBasis` (`packages/shared/src/policy.ts:204-208`) bills on
  `max(agreedFare, basisFloorPct × suggestedFare)`, `basisFloorPct = 0.5`
  (`policy.ts:157`) — WD-012, closing DOC-16-04.
- **P1-3 (KYC duplicate-identity auto-verify, ~line 182)**: claimed `applyKycResult`'s vendor
  auto-mode path "never reads `duplicateIdFlag` at all." Verified `rider.service.ts:419-420` now
  computes `holdForReview = status === "verified" && current?.duplicateIdFlag === true` and routes a
  flagged rider to human review instead of auto-committing — `DOC-16-05`.
- **P2-6 (proof-of-delivery, ~line 297)**: still described as open with only a proposed fix.
  Replaced with the shipped Phase A/B summary (proof-of-drop capture + the CAS-guarded
  `adjudicate-delivered` admin action), citing `KNOWN_BUGS.md`'s `KB-POD-DISPUTE` (CLOSED) row.
- **P3 throttle bullet (~line 325)**: claimed the guard is "keyed by IP, not subject" for
  authenticated routes — but the doc's OWN status banner already says P3-5/IR16-08 was fixed on
  2026-07-16, an internal contradiction. Verified `throttle.guard.ts:34-42` self-decodes the bearer
  token to key by subject, falling back to IP only absent a valid token. Updated to match.

### `docs/DATA-RETENTION.md`
- **Retention schedule, `top_ups.phone` row (line 34)**: claimed the field "is not covered by the
  profile scrub." Verified `privacy.service.ts:235-241` (`DOC-16-01`) now nulls `TopUp.phone` for
  every top-up on the erasing rider. Updated the row and added two rows the schedule was missing
  entirely for fields now scrubbed: `riders.geog`/`position_updated_at` (raw-SQL null,
  `privacy.service.ts:232`) and `orders.item_photo_url`/`delivery_proof_*` (GCS delete + column
  null, `privacy.service.ts:246-258,301-316`).
- **Principles (added #4)**: the schedule is now enforced by a declarative manifest
  (`apps/api/src/privacy/pii-manifest.ts`, new file — a schema-column ⇄ erasure-handling
  cross-check with its own spec test) that the doc didn't mention at all; added one line describing
  the mechanism and the bug class it closes (DS-01, DS15-03, DS15-07, DOC-16-01).

### `docs/ARCHITECTURE.md`
- **§8 refresh sequence diagram (~line 716)**: the `POST /auth/refresh` step didn't mention the
  suspended/banned hard-reject. Verified `auth.service.ts:372-378` (`IR16-01`) rejects refresh for
  `RiderAccountStatus.SUSPENDED`/`BANNED` before the rotation/grace logic runs; added one line to
  the sequence diagram.
- **§7 lifecycle rules, rating bullet (~line 679)**: claimed rating "updates the rider's running
  `ratingAvg`/`ratingCount`/`tripsCount`... in the same transaction" unconditionally. Verified
  `order-lifecycle.service.ts:561-593`: `tripsCount` always increments, but `ratingAvg`/
  `ratingCount` and reliability recovery only move when `customerTrusted` (established customer,
  `customerRatingCarriesWeight` in `policy.ts:255-256`) **and** the pair is distinct
  (`priorPairRatings === 0`) — a penalty from a trusted customer still applies even on a repeat
  pair. Rewrote the bullet to state the actual gating.
- **§16 REST surface table**: three routes shipped in this window with no table row —
  `POST /orders/:orderId/delivery-proof` (`lifecycle.controller.ts:63`),
  `POST /uploads/delivery-proof` (`uploads.controller.ts:70`), and
  `POST /admin/orders/:id/adjudicate-delivered` (`admin.controller.ts:258`). Added all three; the
  table's own header claims it's "the full API surface, by module," so an omission here is the same
  kind of staleness as a wrong row, not a narrative gap.

### `docs/CONCEPT.md`
- **Out-of-scope list, in-app payments bullet (line 144)**: claimed "self-serve top-up UI exists but
  has no working payment backend." Verified `apps/mobile/app/wallet/top-up.tsx` has no self-serve
  form at all anymore — it's a single "contact support" instruction card (`DOC-16-02`, the old form
  was removed because its only confirm path had zero callers, not merely non-functional). Reworded.
- **Risk register #5 (Trust & safety, line 114)**: no mention of the per-device signup throttle that
  shipped this window (`x-device-id`, `RL.deviceSignup`, `IR16-10`) as an identity-abuse control.
  Added one clause; full detail already lives in `docs/plans/2026-identity-and-pod-hardening.md`.

### `docs/plans/2026-rider-wallet-design-brief.md` and `docs/plans/2026-rider-wallet-design.md`
See **ORPHAN** below — handled as an in-place AMENDED annotation (this doc's own established
pattern, see Premise 6) rather than a plain STALE_DOC rewrite, since the underlying design intent
wasn't wrong, just not (yet) buildable.

### `docs/DESIGN.md`
- **Rider screens table, "Active job" row**: no mention of the undelivered flow's proof-of-drop
  capture (`UndeliveredSheet`, `KB-POD-DISPUTE` Phase A). Added one clause to the existing row —
  this is an as-built calibration table, so a shipped screen behavior missing from it is the same
  class of gap as a wrong cell.

---

## ORPHAN

**`docs/plans/2026-rider-wallet-design-brief.md` "NEW SCREEN 2 — Top-up flow"** and
**`docs/plans/2026-rider-wallet-design.md` Flow 3 ("EcoCash top-up (automated)") + its interaction-state
table rows** describe a self-serve amount-entry / rail-selector / USSD-countdown top-up experience
that was **never shipped** as a client flow and, per `DOC-16-02`
(`apps/mobile/app/wallet/top-up.tsx`), has now been explicitly replaced with a single "contact
support" instruction card — the self-serve form's only possible confirm path
(`WalletService.creditFromTopup`) has zero callers anywhere in the codebase.

This wasn't drift from a bug; it's a deliberate, ledgered product/eng decision (`DOC-16-01`'s sibling
`DOC-16-02` — see `KNOWN_BUGS.md:546`) to stop shipping a self-serve UI that could only ever time
out, in favor of the working admin manual-credit path. Per the routine's own instruction ("it may be
aspirational/planned — don't silently delete"), I did **not** delete this spec. Instead I added
**AMENDED** notes at each of the three locations (mirroring the doc's existing Premise-6 pattern),
explaining what shipped instead and marking the original spec as "kept for reference" for whenever a
real rail integration lands. No content was removed.

---

## AMBIGUOUS

**`docs/routines/documentation-update.md:36`** instructs this routine to file a `DOC-` ledger row
"with the owning lane (bug-hunt / UX / deep-sweep per `docs/ROUTINES.md`)" — omitting the **wallet &
data-lifecycle audit (`WD`)** lane. `docs/ROUTINES.md`'s bug-dedup protocol names four bug-finder
lanes (`BH-`/`UX-`/`DS-`/`WD-`), and in practice `WD` is the dominant owning lane for `DOC-` rows in
the ledger today (4 of 6 `DOC-16-*` rows are tagged `WD`). Per `docs/ROUTINES.md`'s own authority
rule, the **live cron trigger** — not this mirror file, not the spec — is what actually runs, and I
have no tool access to read it. I can't tell whether the live prompt already includes `WD` (making
this mirror file merely stale) or genuinely omits it (making it a live bug in what this routine is
told to do each night). Flagging for a human to reconcile the live trigger against
`docs/routines/documentation-update.md` directly; not edited.

---

## Noted but not edited (no existing doc anchor to attach a minimal fix to)

Two more pieces of new mobile behavior surfaced with zero footprint in the doc set, but neither had
an existing section/table row to extend without writing new prose, which is out of scope for this
routine (STALE_DOC fixes are minimal edits, not new sections):
- Rider-board reconnect/foreground `activeJob` reconciliation (`use-rider-board.ts:58-70`,
  `rider/index.tsx:394-410`) and bid-draft expiry on restore (`rider-bid-draft.ts:62-77`) — both
  client robustness fixes for missed realtime events, not new user-facing spec. Already documented
  in `apps/mobile/CHANGELOG.md` #285.
- If either recurs as a citation gap in a future pass, it's a candidate for a small addition to
  `docs/DESIGN.md`'s rider-home row, same treatment as this run's "Active job" edit.

---

## Verified correct, no action needed

Sampled 5 `KNOWN_BUGS.md` fix rows spread across `DS17-*`/`UX17-*`/`BH-*`, prioritizing sensitive
areas (KYC lock, supply-plane eviction, notification-feed synthesis) — all 5 matched the code
exactly as claimed. Re-verified 2 "→ FIXED/MOOT" cluster headers not covered by a recent routine
("Ship/infra correctness", "Money-fraud cluster") against ≥2 named members each — both intact. Diffed
all 6 `docs/routines/*.md` mirror files against their `docs/ROUTINES.md` sections (lane definitions,
Phase-0/0.5 wording, sibling-sweep rules, merge policy) — all matched except the one AMBIGUOUS item
above. Spot-checked the wallet balance/ledger screen (`apps/mobile/app/wallet/index.tsx`) against
the design doc's hero/receipt/empty-state spec — unchanged in this window, still matches. Confirmed
the determinism marker (`docs/.last-doc-sync`) was internally consistent with the prior run's
recorded HEAD before this run overwrote it.

---

_Line references are as of this run against `210eb6b`; re-verify after any refactor of the cited
files._
