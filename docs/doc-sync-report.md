# Documentation Sync Report

Generated by the doc-reconciliation routine. Marker-scoped pass: previous marker `ef3d030`
(2026-07-19 04:19, set by the 2026-07-19 sync run), this run's base HEAD `d829ef8`
(2026-07-20, post the night's three fix routines — bug hunt, UX, deep sweep — plus a full day
of shipped feature work). That range covers: the wave-2 performance program (`GET
/app/bootstrap` BFF aggregate, standardized `MicroCache` layer, `POST /riders/heartbeat`,
notifications-feed parallelization, the new weekly Performance-watch routine), `BH-21`/`BH-22`
(rider sent-offer persistence, issue-raise dedup), `UX20-01..04` (active-order error visibility,
commission-rate copy honesty, fare-adjust feed fallback), the `BIRD_ACCESS_KEY` vendor-secret
pre-provisioning, `RF-06` (KYC-review reads extracted out of `AdminRidersService`), `WD-024..026`
(admin KPI status-gating, wallet-cache invalidation on completion), and `DS19-01`/`DS19-02`
(reliability-hold demotion parity, erasure board/geo eviction parity) — 147 files under `apps/`,
`docs/`, `infra/`, `packages/shared/`, and `scripts/`.

Fanned this out across four parallel research passes (perf/infra wave-2 vs `ARCHITECTURE.md` /
`OBSERVABILITY.md` / `PERFORMANCE.md` / `ROUTINES.md`; mobile bug/UX fixes vs `KNOWN_BUGS.md` /
`PRICING.md` / `CONCEPT.md` / `FRAUD-REVIEW.md`; infra/secrets vs `SECRET-ROTATION.md` /
`LAUNCH-EXECUTION-RUNBOOK.md` / `GCP-PROVISIONING-REVIEW.md`; refactor/deep-sweep vs
`REFACTOR-LEDGER.md` / `DATA-RETENTION.md` / `KNOWN_BUGS.md` / `FRAUD-REVIEW.md`), then
independently re-verified every proposed finding against the cited diff/current code myself
before editing anything — this caught one non-discrepancy (`FRAUD-REVIEW.md` P2-3, see below)
and confirmed the rest against fresh `grep`/`Read` output rather than trusting the research
passes' line numbers outright.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 11 files, ~20 individual claims/rows/citations |
| CODE_BUG (logged, not doc-edited) | 0 |
| ORPHAN | 0 |
| Flagged gap (not a false claim, not auto-authored) | 1 (`docs/routines/performance-watch.md` mirror file doesn't exist) |
| AMBIGUOUS | 0 (one candidate — `FRAUD-REVIEW.md` P2-3 — checked and found NOT a discrepancy, see below) |

---

## STALE_DOC — fixed this run

### `docs/ARCHITECTURE.md` — wave-2 BFF/heartbeat endpoints missing from the REST surface + module map
§16 (line 1087: *"The full API surface, by module"*) had no row for the two routes wave-2 shipped
(`d52eb97`):
- **`GET /app/bootstrap`** (`apps/api/src/app-bootstrap/app-bootstrap.controller.ts:36`) — added a
  row after `PATCH /auth/me`.
- **`POST /riders/heartbeat`** (`apps/api/src/riders/riders.controller.ts:71-75`) — added a row
  after `PATCH /riders/online`.

§4's module map (the `features` mermaid subgraph, lines 369-387) omitted `AppBootstrapModule`,
registered in `apps/api/src/app.module.ts:19,54`. Added a node + edges to `auth`/`orders` (its
actual deps per `app-bootstrap.module.ts:2-3,10`), plus one sentence in "Notable cross-module
wiring" noting it composes `AuthService.getProfile` + `OrdersService.activeForRider`/
`activeForCustomer` via `Promise.all` — no new query logic.

### `docs/OBSERVABILITY.md:28-43,69` — missing `micro_cache_requests_total` counter
The Metric → SLO table listed every other wave-2-adjacent counter but not this one, even though
it's declared in `metrics.service.ts:41` (`CounterName`), recorded via `recordMicroCache` at
`:196-198`, and already referenced as load-bearing by `docs/PERFORMANCE.md:80` and
`docs/ROUTINES.md:386` (the new Performance-watch routine's Phase-1 scorecard signal) — its
absence from the metrics doc itself was a real gap. Added the row plus its closed label
vocabularies (`cache` ∈ `nearby_count | pickup_photo_url`; `outcome` ∈
`hit | l2_hit | miss | coalesced | error`, both verified against `metrics.service.ts:54-58` and
`micro-cache.ts:34`).

### `docs/routines/README.md:3,9-17` — "seven routines" stale against `ROUTINES.md`'s eight
`docs/ROUTINES.md:3,28-39` added the "Performance watch" routine 2026-07-19 (8th row in its
routines table); `docs/routines/README.md` still said "The seven scheduled Claude routines" and
its mirror-file table had no `performance-watch.md` entry. `docs/ROUTINES.md` is the canonical
spec (its own line 6: authoritative when it and a mirror disagree), so the README was the stale
side. Fixed the count to "eight" and added a table row — but the row points at "*(none yet — see
note)*" rather than a fabricated filename: `Glob docs/routines/*.md` confirms no
`performance-watch.md` exists on disk, so the mirror-file gap is real, not just a README
oversight. Added a note explaining why the routine did **not** author that file itself (writing a
trigger-prompt mirror is new operational content — a guessed prompt body would itself become a
stale doc the moment it diverged from the real live trigger) and flagging it for a human or a
future prompt-audit pass.

### `docs/KNOWN_BUGS.md:1115` (IR16-12 / `KB-POD-DISPUTE` Phase B row) — fabricated "48-hour contest window" surviving in the row's own description
`UX19-02` (2026-07-19) removed a fabricated "48-hour contest window" claim from 4 code sites and 3
other doc locations (per the 2026-07-19 doc-sync); this ledger row's own "SHIPPED" narrative —
*"post-commit notifies the customer (with a **48-hour contest window** via the existing
issue-raise flow)..."* and *"customer-48h + rider notifications"* in its test list — was the one
remaining site still describing the shipped feature with the fabricated copy. Verified against
current code (`apps/api/src/admin/admin-orders.service.ts:275-279`'s actual push body has no
deadline; `admin-orders.service.spec.ts:638` and `notifications-feed.service.spec.ts:485` assert
the "48 hours" phrase is *absent*) and against the doc's own correct description of the same fact
elsewhere (`docs/KNOWN_BUGS.md:79`, `docs/FRAUD-REVIEW.md:310`,
`docs/plans/2026-identity-and-pod-hardening.md:84-86`). Reworded both spots to
"via the existing issue-raise flow, no fixed deadline — see UX19-02" / "customer + rider
notifications".

### `docs/KNOWN_BUGS.md` — five ledger rows citing pre-fix line numbers from their own fixing commit
`e8c090c` (WD-024..026) and `a03c155` (DS19-01/02) each wrote their `docs/KNOWN_BUGS.md` rows and
sibling-sweep evidence with file:line citations captured from the pre-fix grep, then never
re-checked them against the post-fix file — so several were already wrong the moment the commit
landed (the fix's own inserted comment blocks shifted the lines below them). Verified every
citation against current `Read` output before correcting:
- **WD-024 (:1449) / WD-026 (:1451)** — `apps/api/src/admin/admin.service.ts:89-91` →
  `:101,105,106-109` (the status-gated completed-count, undelivered-count, and fares-aggregate
  lines).
- **WD-025 (:1450)** — `apps/mobile/src/query/use-history-feed.ts:46` → `:52`
  (`invalidateRiderJobQueries`'s definition).
- **DS19-01/DS19-02 sibling-sweep evidence (:817-826)** — `order-lifecycle.service.ts:622` (rate)
  → `:628`; `:762` (cancel below-limit) → `:786`; `:950` (completeOrder recovery, not a sibling) →
  `:993`; `privacy.service.ts:386` (the fixed erasure call site) → `:391`.
- **`docs/DEEP-SWEEP-2026-07-19.md`** (the dated report; lines 106-107, 112, 118-120, 128) carried
  the identical stale numbers for the same reason — corrected alongside the ledger rows since
  they're the evidence a reader would use to actually verify sibling-sweep coverage.
- **KB-HOLD-SESSION-SCOPE (:294)**, caused by `RF-06` (`6b25edc`, landed after `a03c155`): the row
  cited `admin-riders.service.ts` `suspendRider`/`banRider` session-revoke sites at "~line
  330/438", correct when `a03c155` wrote it, but RF-06's ~94-line extraction of `getKycReview`
  shifted both sites upward — current code has them at `:227`/`:335`. Corrected.

### `docs/FRAUD-REVIEW.md:332-333` — stale `clear-hold` route citation
Cited `admin.controller.ts:212` for the `POST /admin/riders/:id/clear-hold` route; current code
has it at `:239` (unrelated file growth shifted it, predates this run's diff window but was
trivial to verify and fix while checking the same file for the P2-3 item below).

### `docs/DATA-RETENTION.md:44-69` — erasure bullet list missing the DS19-02 eviction step
The erasure section (lines 44-69) enumerates everything `PrivacyService.eraseAccount` does,
including other post-commit steps (e.g. the pickup-photo GCS purge at line 64) — but never
mentioned that erasure also evicts a rider from both live-supply planes
(`evictRiderFromSupply(profileId)`, `privacy.service.ts:391`), the exact behavior `DS19-02` added
this window (previously erasure only called the geo-half, `evictRiderFromGeo`). Since the doc
purports to be a complete enumeration and every other post-commit step is listed, the omission
read as incomplete rather than by-design. Added one bullet describing it.

### `scripts/adopt-vendor-secrets.sh:2-4` (script header comment)
The header's parenthetical list of adopted vendor secrets (`DIDIT_API_KEY`,
`DIDIT_WEBHOOK_SECRET`, `WHATSAPP_ACCESS_TOKEN`) omitted `BIRD_ACCESS_KEY`, which the `SECRETS`
array two lines below (and `secrets.tf`) already includes (`0cfd3b5`). In scope per the routine's
"doc comments treated as spec" — added it to the header list.

---

## Checked and found NOT a discrepancy

`docs/FRAUD-REVIEW.md:291` (P2-3, "ban revokes no sessions") reads as an open finding with no
inline FIXED marker, even though `KNOWN_BUGS.md:1003`'s `IR16-01` row explicitly closed exactly
this ("FRAUD P2-3, marked/implied fixed but never was" → **FIXED**, session revoke now in-tx on
suspend/ban). Initially flagged by the refactor/deep-sweep research pass as possibly stale. But
`FRAUD-REVIEW.md`'s own top banner (lines 3-11) explicitly disclaims per-finding status as
unmaintained and directs readers to trust `KNOWN_BUGS.md` instead — the same convention already
covers P2-2 two lines above it ("See `docs/KNOWN_BUGS.md` — 'Money-fraud cluster → MOOT'"). Not
a discrepancy; the doc is deliberately not re-marking each of ~15 individual findings inline. No
edit made.

## Flagged gap (not auto-fixed)

**`docs/routines/performance-watch.md` doesn't exist.** `docs/ROUTINES.md` documents the
Performance-watch routine's live trigger (cron `0 11 * * 0`) as already added 2026-07-19, but per
`docs/routines/README.md`'s own "Edit the file here and land it" convention, no version-controlled
mirror of that trigger's actual prompt text was ever committed. This is new operational content
(a full trigger-prompt body), not a reconciliation edit — the routine can correct the README's
"seven" → "eight" count honestly, but authoring a guessed prompt body risks creating a *new* stale
doc the moment it diverges from the real live trigger. Needs a human (or the routine's next
prompt-audit pass) to paste the actual trigger text into a new `performance-watch.md`.

## AMBIGUOUS

None this run.

## CODE_BUG

None found. Every discrepancy this run traced to a doc (or a doc's own citation) lagging a
shipped, intentional change — no doc encoded a genuine design/PRD decision that code has since
regressed from.

---

## Verified correct, no action needed

- **`docs/PERFORMANCE.md`** — spot-checked 6 specific claims against current code
  (`MICRO_CACHE_*` env vars in `env.ts:53-62`; `GET /app/bootstrap` seeded at
  `apps/mobile/app/_layout.tsx:17,39`; `/riders/heartbeat`'s standing predicate in
  `rider.service.ts:376-417` and its ZCARD-gated waitlist drain in
  `tracking.service.ts:589-595`; the mobile `sendHeartbeat` 404/405 legacy fallback in
  `apps/mobile/src/api/riders.ts:36-43`; the KYC-poll `auto`/`manual` cadence split in
  `apps/mobile/app/rider/index.tsx:290`; the snapshot `events[]` lat/lng trim in
  `orders.service.ts:742-749`) — all accurate, no edits needed.
- **`docs/LOAD-MODEL.md`** — no claims about heartbeat/bootstrap cadence to begin with (its GPS-fix
  row is the position stream, unrelated to the presence beat); not stale, just pre-existing silence.
- **`docs/ROUTINES.md`**'s own Performance-watch section — internally consistent (Phase 0-4 present,
  cron/lane-prefix used consistently throughout, "eight routines" heading matches its own 8-row
  table).
- **`docs/PRICING.md`** — scoped entirely to fare/offer-ranking vocabulary; contains zero
  commission-rate claims, so `UX20-02/03` (commission-rate copy honesty) has nothing there to make
  stale.
- **`docs/CONCEPT.md:111,330-337`** — the "0% commission for first ~6-8 months" revenue-model
  narrative is unaffected by `UX20-02/03` (which fixed 4 UI sites' *reaction* to the live rate, not
  the rate/model itself); verified `COMMISSION_RATE_PCT` (`env.ts:225-228`) is still
  optional/undefined by default.
- **`docs/FRAUD-REVIEW.md:157`** ("Issue/Refund have no per-order uniqueness") — still true after
  `BH-22`; the new dedup only matches `(openedByProfileId, idempotencyKey)` (a client-retry guard),
  not distinct issues on the same order (`issues.service.ts:72-105`).
- **`docs/plans/2026-identity-and-pod-hardening.md:85`** — "no elapsed-time gating" on
  `IssuesService.raise` still accurate; `BH-22` added an idempotency key, not time-based gating.
- **`docs/KNOWN_BUGS.md` `BH-21`, `BH-22`, `UX20-01..04` rows** — all spot-checked against current
  code, file:line citations accurate (UX20-01's citations are the pre-fix "at time of audit"
  convention already used throughout the ledger, not new staleness).
- **`docs/LAUNCH-EXECUTION-RUNBOOK.md` §9, `docs/SECRET-ROTATION.md` §5** — both already updated
  atomically in `0cfd3b5` (the same commit that shipped `secrets.tf`/`adopt-vendor-secrets.sh`);
  verified line-by-line, no drift.
- **`docs/GCP-PROVISIONING-REVIEW.md`, `docs/INFRA-HARDENING-ROLLOUT.md`** — unaffected by this
  window's changes; the former remains self-marked superseded.
- **`docs/REFACTOR-LEDGER.md`, `docs/REFACTOR-2026-07-19.md`** (RF-06) — internally consistent with
  each other and with current code (`AdminKycReviewService` is 113 lines, matching the doc's
  citation exactly); neither cites specific file:line locations, so no line-drift risk there.
- **`docs/ARCHITECTURE.md`**'s admin/tracking sections — never named `AdminRidersService`/
  `AdminKycReviewService` by class, so RF-06's split doesn't touch anything asserted there.

---

_Line references are as of this run against `d829ef8` (plus this run's own doc-fix commits on
top); re-verify after any further refactor of the cited files. Marker updated to `d829ef8` in
`docs/.last-doc-sync`._
