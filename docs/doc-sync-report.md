# Documentation Sync Report

Generated by the doc-reconciliation routine. Incremental pass against the marker in
`docs/.last-doc-sync` (previous marker `e996911`, this run's HEAD `808df80`, 2026-07-13). That range
(270 commits, spanning several PR merges) is large: it includes the RH-01 fraud-hold fix (#221), the
SOS acknowledgement workflow (#224), the mobile TLS cert-pinning rollout (#224), the Cloud Scheduler
and OTel-collector terraform (`scheduler.tf`, `otel.tf`), broadcast-radius policy work, and the wallet
design docs. Fanned this out across four parallel research passes (policy/matching/broadcast/SOS,
admin/orders/notifications/tracking, mobile, infra/terraform) that traced every falsifiable claim in
the touched docs to its current `file:line`, then applied the STALE_DOC fixes below by hand.

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 8 files (14 individual claims) |
| CODE_BUG | 0 |
| ORPHAN | 0 (one near-orphan claim resolved as STALE_DOC — see `FRAUD-REVIEW.md` P0-2/P0-3 below) |
| AMBIGUOUS | 1 (flagged, not edited) |
| Claims checked and confirmed correct | 40+ |

---

## STALE_DOC — fixed this run

### `docs/FRAUD-REVIEW.md`
The commission model was rewritten between when this audit was written and now: the old weekly
post-paid cash-settlement mechanics (`recordPayment`, `adjustFare`-driven settlement, `grossFares`)
were replaced by a read-only prepaid-per-ride console at a 0% launch rate
(`apps/api/src/settlements/settlements.service.ts`). `docs/KNOWN_BUGS.md` had already recorded this
("Money-fraud cluster → MOOT", line 112) but `FRAUD-REVIEW.md` itself — the original point-in-time
audit — carried no such annotation, so a reader landing on P0-1/P0-2/P0-3 would think three open P0s
await a fix:
- **P0-1** (self-dealing wash-trade): added a "Fixed (verified)" note — `makeOffer` rejects
  `order.customerId === riderId` (`offers.service.ts:38`), `selectOffer` re-asserts it
  (`matching.service.ts:79`), matching `KNOWN_BUGS.md`'s "Object-authz / IDOR cluster → FIXED."
- **P0-2** (no fare floor / commission-basis): added a "MOOT" note — the mechanics it critiques no
  longer exist; the fare-manipulation risk may resurface once the prepaid wallet
  (`docs/plans/2026-biker-prepaid-commission.md`) starts debiting per ride, which is noted as worth
  re-checking against that build rather than fixing here.
- **P0-3** (`markUndelivered` penalty-free escape hatch): split into two halves — the commission-
  exclusion half is MOOT (same reason as P0-2); the self-attested-abandonment half is separately
  MITIGATED by the #198 velocity guard (`KNOWN_BUGS.md`'s FRAUD P0-3 row).
- **P3-1** citation: `admin.controller.ts:188` → `:212` (the `clear-hold` route shifted after the SOS
  routes were inserted earlier in the controller).

### `docs/DEEP-SWEEP-2026-07-13.md`
Self-contradicting within the same document — RH-01's narrative section (written when the finding was
first reported) said "Status — REPORTED, not auto-fixed (deliberate)" and "RH-01 remains OPEN", while
the doc's own later "Post-merge status" table (line 311, added after the fix landed) already said
"FIXED — PR #221." Updated both the narrative paragraph and the summary line to say FIXED, citing the
same PR #221 / migration 0024 / persisted `heldReason` details the status table already carried.
Also: the DS13-05 finding text and its own status-table row both said "an acknowledgement workflow
(mark-seen) is still open" — that workflow (`POST /admin/sos/:id/ack`, `sos.service.ts:148`,
`AcknowledgeButton.tsx`) shipped in #224; updated both to reflect it.

### `docs/KNOWN_BUGS.md`
Line 259's summary paragraph said "RH-01 reported-only and still OPEN (see OPEN table)" — contradicted
by the same document's own lines 11-12 and 272, which already say "FIXED #221." Fixed the stale
sentence to match the rest of the ledger.

### `docs/JOURNEY-BUGS.md`
Two stale citations, both caused by later code insertions shifting line numbers:
- `admin.controller.ts:188` → `:212` for the `clear-hold` route (same drift as `FRAUD-REVIEW.md` above).
- `src/push/use-push-registration.ts:49-57` → `:52-76` for the cold-start deep-link block, after the
  `usePathname`/`pathnameRef` addition pushed it down.

### `docs/UX-USABILITY-REVIEW-2026-07-12.md`
Two `file:line` citations drifted after intervening commits, despite one of them (#1) already having
been corrected once in a prior sync pass (`700f573`):
- Finding #1: `orders.service.ts:658-701` → `:641-648` (the `publicWaypoint`/`pickupPoint` helpers were
  extracted to the new `apps/api/src/orders/waypoints.ts`, moving `getSnapshot`/`viewerRole` up ~17
  lines).
- Finding #12: `tracking.service.ts:453-463` → `:516` (`addNotifyRequest` shifted down 63 lines after
  the heartbeat-cutoff filtering and `claimBroadcastRecipients` were added earlier in the file).

### `docs/ARCHITECTURE.md`
The admin REST endpoint table was missing the two SOS routes added for the DS13-05 fix and its #224
acknowledgement follow-up: added `GET /admin/sos` and `POST /admin/sos/:id/ack` rows.

### `docs/GCP-PENDING-REVIEW-2026-07-13.md`
Two §5 table rows described infra as un-terraformed that landed in the same window this doc itself
covers (commit `0ee2a33`, which post-dates the doc's last edit to that section):
- **Cloud Scheduler**: was "Still no `google_cloud_scheduler_job` in terraform" — `infra/terraform/scheduler.tf`
  now defines it (only a `terraform apply` remains). Updated to point at §8a, which already documents
  this as fixed — the table row just hadn't been updated to match.
- **OTel collector**: was "Not deployed... not terraformed, not folded into release.yml" —
  `infra/terraform/otel.tf` now owns the `otel-collector-config` secret + IAM grant, and `release.yml`
  deploys the sidecar behind `OTEL_SIDECAR_ENABLED` (`.github/workflows/release.yml:469-509`). Updated
  to describe what actually remains (flag flip + apply + dashboard import), and updated the §8
  "Recommended order" list (items 4-5) to match.
- **`roles/logging.viewer`**: same pattern as Scheduler — already fixed per §8a, table row updated to
  point at it instead of re-describing it as missing.

### `docs/GCP-PROVISIONING-REVIEW.md`
This 07-10 doc's top banner already marks it superseded by `GCP-PENDING-REVIEW-2026-07-13.md` for its
§3/§4 deploy-breakage sections, but §5's Scheduler and OTel rows carried no such disclaimer and were
now flatly wrong (both describe manual runbook steps that are now terraformed). Updated both rows to
say "Done as of 2026-07-13" with a pointer to the newer doc's §5/§8a for the remaining founder steps.

---

## AMBIGUOUS — flagged, not edited

- **`docs/GCP-PENDING-REVIEW-2026-07-13.md`** overall structure: §5 originally described Scheduler/
  logging.viewer/OTel as gaps, and §8a (same file) separately documents Scheduler and logging.viewer as
  fixed "alongside this review." This run added inline "(fixed alongside this review, see §8a)"
  pointers to those two §5 rows rather than restructuring the doc, since the two-part
  as-found-then-fixed narrative looks like a deliberate structure, not an oversight — a human should
  decide if that structure should change. (OTel wasn't in §8a's "fixed alongside" list, so that row was
  a genuine correction, not just a missing cross-reference — see STALE_DOC above.)

---

## Areas checked with no discrepancies found

- **Broadcast radius / policy** (`packages/shared/src/policy.ts` `BROADCAST`, `broadcast-policy.ts`,
  `ARCHITECTURE.md`'s broadcast section): every number (5km base, 8km@30s, 12km@60s, 25km corridor cap,
  120s ghost cutoff, env var names) matches code exactly.
- **Matching / offer-expiry / SOS core behavior** in `ARCHITECTURE.md`, `KNOWN_BUGS.md` (DS13-01…07,
  BR-01…03 rows).
- **Mobile**: `MOBILE-CERT-PINNING.md`, `SECURITY.md`/`SECURITY-OPS.md` P3-1 sections,
  `apps/mobile/CHANGELOG.md`, `KNOWN_BUGS.md` BH-01/BH-02/KB-MOBILE-PIN entries — all verified against
  `compute-tls-pins.sh`, `push.ts`, `order-tracking.ts`, `orders.ts`, the cert-pinning config plugin,
  and the actual commit history.
- **`docs/OBSERVABILITY.md`**, **`infra/terraform/README.md`** — already accurate against
  `otel.tf`/`iam.tf`/`scheduler.tf`/`variables.tf` (rewritten in the same window as the terraform
  landed). README.md is missing the new `otel-collector-config` secret from its "What it creates"
  table, but that's an omission, not a false claim, so not reported as a discrepancy.
- **`docs/LAUNCH-EXECUTION-RUNBOOK.md`** §2/§3 — both already carry accurate "UPDATE (2026-07-13)"
  banners matching the scheduler/OTel terraform.
- **`docs/PILOT-READINESS.md`** — already carries a top-of-file staleness banner pointing at
  `GCP-PENDING-REVIEW-2026-07-13.md`; no new finding attributable to this diff.
- **Admin/orders/notifications/tracking behavior** in `KNOWN_BUGS.md` DS13-01…07 rows — file:line
  citations there deliberately point at pre-fix code locations (a consistent ledger convention for
  FIXED entries), not current-state citations, so not flagged as stale.
- **`docs/plans/2026-rider-wallet-*.md`** — pure forward-looking design/plan docs for unbuilt work; out
  of scope for this pass (nothing to reconcile against yet).

## Determinism note

Re-running this pass against unchanged code/docs should produce zero further edits — every fix above
is tied to a specific `file:line` read during this run and cited in the sections above.
