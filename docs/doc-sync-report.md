# Documentation Sync Report

Generated by the doc-reconciliation routine. Incremental pass against the marker in
`docs/.last-doc-sync` (previous marker `2ffe0d3`, 2026-07-15; this run's HEAD `fc11122`,
2026-07-16). That range (33 commits, 114 files) is dominated by one thing: the **rider prepaid
commission wallet** shipped end to end — `feat(wallet)` PR1 core, the `WD-001..011` reconciliation
fixes, and `BH-07..12` mobile/contract-seam follow-ups — plus the 2026-07-15 night's UX (UX16-01..08)
and deep-sweep (DS16-01..02) fix routines. Fanned this out across three parallel research passes
(data-retention/observability, wallet plan-docs-vs-implementation, fraud/journey/security citation
drift), then verified every finding myself against the cited code before editing — several of the
research agents' proposed classifications were revised after independent verification (see
"Corrections to research findings" below).

## Summary

| Class | Count |
|---|---|
| STALE_DOC (fixed) | 9 files (≈35 individual claims/citations/sections) |
| CODE_BUG (logged, not doc-edited) | 6 (`docs/KNOWN_BUGS.md` DOC-16-01..06) |
| ORPHAN | 0 |
| AMBIGUOUS | 0 |
| Claims checked and confirmed correct | 40+ |

---

## STALE_DOC — fixed this run

### `docs/ARCHITECTURE.md`
The wallet feature (`apps/api/src/wallet/*`, new Prisma models `CommissionAccount`/
`CommissionLedger`/`TopUp`) shipped entirely inside this window and was completely absent from the
architecture doc, which explicitly claims to enumerate "the full API surface, by module" (§16) and
the complete module map (§4):
- **§4 module map**: added `WalletModule` to the feature-lanes diagram + dependency edges
  (`orders --> wallet`, `admin --> wallet`, `wallet --> prisma`), and extended the
  `OrderLifecycleService` cross-module-wiring bullet to note `chargeCommission` runs **inside** the
  completion transaction (not best-effort, unlike the tracking/notification calls in the same
  bullet) — confirmed via `order-lifecycle.service.ts:520,822`.
- **§5 ERD "grown beyond" list**: added `CommissionAccount`, `CommissionLedger`, `TopUp` to the named
  list of tables not pictured in the scoped ERD (`schema.prisma:274-346`), matching the doc's
  existing convention for `Issue`/`Report`/`SosEvent`/etc.
- **§9 gating bullet**: was already corrected last run for standing/`onHold`, but didn't cover the
  new commission floor. Added: `PATCH /riders/online` additionally enforces `commission_low_balance`
  once the rate is live (`online-gate.ts`'s `onlineRefusalReason`, loaded in `rider.service.ts`'s
  `setOnline`), and clarified `POST /orders/:id/offers` does **not** re-check the balance (verified
  `offers.service.ts:65` calls `onlineRefusalReason(rider)` with no commission fields — the branch
  can never fire there).
- **§16 REST table**: added `GET /orders/earnings/summary` (`orders.controller.ts:102`, new this
  window), the five `/wallet/*` routes (`wallet.controller.ts`), and
  `POST /admin/riders/:id/wallet-credit` (`admin.controller.ts:275`). Also corrected the
  `PATCH /riders/online` table-cell description, which still said "gated on KYC + cooldown only" —
  inconsistent with the already-corrected §9 prose.

### `docs/DATA-RETENTION.md`
Extended the "retained financial record" row to include the wallet's `commission_accounts`/
`commission_ledger`/`top_ups` tables (same retention class as `orders`/`ratings`/`audit_logs`,
confirmed PII-free apart from a `riderId` FK — read the full current `privacy.service.ts`, zero
references to any wallet table). Did **not** add prose describing the `top_ups.phone` gap as a doc
note — that's a real divergence from the doc's own Principle 2 ("erase means anonymise... phone"),
so it's logged as CODE_BUG `DOC-16-01` instead (see below), not edited into the doc.

### `docs/FRAUD-REVIEW.md`
Six citation-only drifts (line numbers shifted by this window's growth in
`matching.service.ts`/`contracts.ts`/`admin-orders.service.ts`/`order-lifecycle.service.ts`/
`admin.service.ts`-split), each re-verified against current code before editing:
- P0-1 header: `matching.service.ts:111` → `:129`; `contracts.ts:80` → `:91`.
- P1-5 header: `admin.service.ts:771-786` (the file is now only 56 lines — stale) →
  `admin.shared.ts:134` (`reportsFor`) / `issues.service.ts:79` (`listForAdmin`).
- P1-6 header: `order-lifecycle.service.ts:356-368`/`:382-406` → `:468` (`rate`) / `:533`
  (`rateSender`).
- P2-1: `admin-orders.service.ts:156-184` → `:179-225`; `:163-164` → `:190`.

Two findings whose "may resurface once the wallet ships" framing is now stale because the wallet
**has** shipped — rewrote to state the confirmed-live status plainly, citing the exact code, and
logged the still-open gap as `DOC-16-04`:
- **P0-2** (commission basis has no fare floor): confirmed `WalletService.chargeCommission`
  (`wallet.service.ts:186-245`) computes commission straight off the same unfloored `agreedFare`
  the original finding describes — dormant only because `COMMISSION_RATE_PCT` defaults to 0%.
- **P2-1** (admin `adjustFare` no downward floor): confirmed `WalletService.adjustCommissionInTx`
  (`:257-280`) has the same no-floor property.

One finding re-verified as partially mitigated rather than fully open or fully fixed — rewrote to
describe the actual current state (a real dedup signal exists but is advisory-only) and logged the
residual auto-mode gap as `DOC-16-05`:
- **P1-3** (banned rider re-registers, no dedup): confirmed `duplicateIdAccountCount`
  (`rider.service.ts:51-56`, A-04) exists and flags duplicates, surfaced to a human reviewer
  (`admin-riders.service.ts:101`), but `applyKycResult` (the auto-mode webhook path,
  `rider.service.ts:386-`) never reads the flag — grepped the function body to confirm.

### `docs/SECURITY.md`
Two citation-only drifts: P0-1's prod guard `env.ts:181-196` → `:208-224`; P2-4's launch-hygiene
guard `env.ts:159-176` → `:249-261` (both shifted by the +28-line commission-env-var block inserted
above them this window).

### `docs/JOURNEY-BUGS.md`
One citation drift: R1's `undeliverM` mutation citation `job.tsx:345` → `:423` (`job.tsx` grew +94
lines this window).

### `docs/CONCEPT.md`
This is the PRD/decision doc, and its §6/§4/§3/§5b language explicitly stated the wallet
infrastructure was **not built yet** ("deferred ~6–8 months", "❌ ... rider balance / top-ups...
deferred"). That's now false — confirmed the wallet was formally designed
(`docs/plans/2026-rider-wallet-design.md`, "Status: APPROVED", dated decision process with a
recorded cross-model review and founder calls) and built, well ahead of the original estimate.
Updated the status/temporal claims in six places (build-status note, risk register item 2, MVP-scope
rider bullet, the ❌ out-of-scope bullet, the §6 "no revenue infrastructure" paragraph, and the
payments-not-a-seam callout) to state plainly that the infrastructure shipped early while keeping
the historical rationale and the still-true fact that the **rate** stays 0% and no real money moves
(self-serve top-up rails are non-functional — see `DOC-16-02`). Did not touch the dated "Resolved
(2026-06-27)" decision-log callout itself (§6) — that's the ADR-style record of *why*, left alone.

### `docs/plans/2026-rider-wallet-design.md` + `2026-rider-wallet-design-brief.md`
Both specified the wallet ships **hidden** behind `WALLET_REVEAL` until the flip-comms window. Code
now has an explicit, dated override: `env.ts:183-189`'s comment reads "**Default on** (product
decision 2026-07-15)". Added a superseding note to both docs rather than deleting the original spec
(the flag/both-states design is still correct as the kill-switch path, just not the default anymore).

### `docs/plans/2026-biker-prepaid-commission.md`
Status line said "wallet is NOT built in this change" and framed itself as the current plan. The
successor doc (`2026-rider-wallet-design.md:455`) explicitly states it supersedes this doc's
phasing while keeping its data-model sketch. Rewrote the status line to point at the successor,
kept the body as historical reasoning.

### `docs/DESIGN.md`
The "Earnings / wallet — payment-agnostic" section (mockup-era) said the wallet is designed
cash-first/mechanism-free and "Do not add payout/commission UI until §6 is decided" — §6 was
decided and a dedicated wallet UI (calibrated against this very section, per
`2026-rider-wallet-design.md`'s own header) was built. Added a superseded note above the original
stance paragraph (preserved as the pre-wallet baseline) and updated the DT11 tracker row.

---

## CODE_BUG — logged in `docs/KNOWN_BUGS.md`, doc NOT edited

Per policy, these describe real design/PRD decisions the shipped code diverges from — the docs
stay as the correct intent statement; only the ledger changes so a fix routine (owning lane noted)
picks each one up:

| ID | One-line summary | Owning lane | Severity |
|---|---|---|---|
| DOC-16-01 | `top_ups.phone` never scrubbed on erasure/retention, unlike every other phone field | WD | LOW-MED |
| DOC-16-02 | Self-serve top-up is non-functional on all 3 rails (`creditFromTopup` has zero callers) — every attempt times out; EcoCash never got the "direct integration in this build" Premise 4 mandated | WD | **HIGH** |
| DOC-16-03 | Admin console has no wallet UI at all (no balance/ledger/credit-form, no bulk seed-credit), despite the backend route existing and the design brief specifying it as in-scope | WD | MEDIUM |
| DOC-16-04 | Commission basis has no fare floor — confirmed live in `chargeCommission`/`adjustCommissionInTx`, dormant only pre-flip | WD | HIGH (dormant) |
| DOC-16-05 | KYC duplicate-ID dedup is advisory-only; the auto-mode webhook path never consults the flag | DS | MEDIUM |
| DOC-16-06 | Design Premise 6 ("drop `Settlement` in this build's migration") wasn't executed — deliberately deferred per the migration's own comment, zero functional impact | WD | LOW |

`DOC-16-02` and `DOC-16-04` are the ones that matter before any real money moves: the top-up flow
is dead code from a user's perspective today (reachable now — `WALLET_REVEAL` defaults visible),
and the fare-lowballing exploit will be live the moment `COMMISSION_RATE_PCT` is ever set above 0
anywhere it reaches production, including a flip-day rehearsal.

## Corrections to research findings (self-check)

The parallel research agents proposed a few classifications I revised after independently
re-reading the code:
- The `top_ups.phone` retention gap was initially drafted as a DATA-RETENTION.md prose edit
  ("known gap" note) — reclassified as CODE_BUG per policy (the doc's own Principle 2 is a real
  design commitment the code violates) and moved to the ledger instead.
- The Settlement-table non-drop was initially considered for a design.md wording edit — reclassified
  as CODE_BUG (an un-executed *agreed premise*, not a superseded one — unlike `WALLET_REVEAL`, there
  is no dated "product decision" comment authorizing the deferral, only an implementer's migration
  note) and moved to the ledger instead of touching Premise 6's text.

## Areas checked with no discrepancies found

- **`docs/OBSERVABILITY.md`** — grepped the whole `apps/api/src` for metric-instrument creation
  outside `metrics.service.ts`; confirmed zero. `wallet.service.ts`/`wallet.controller.ts` never
  reference `MetricsService`. No new metric exists to document (itself arguably a product gap —
  top-ups/commission-floor blocks are unobserved — but that's a code state, not a doc falsehood, so
  out of scope here).
- **`docs/SECRET-ROTATION.md`** — the new `WALLET_REVEAL`/`WALLET_MANUAL_CREDIT_CAP_USD`/
  `COMMISSION_RATE_PCT`/`COMMISSION_SHADOW_RATE_PCT` env vars are plain config/feature-flags, not
  Secret-Manager-injected credentials — outside this doc's own stated scope.
- **`docs/JOURNEY-BUGS.md`** — per its own header, an intentionally static historical ledger with
  inline status annotations, not a growing feature log; the new mobile wallet screens don't belong
  here. Sampled R2/R4/R5/R9/R10/C1-C12 citations, no further drift.
- **`docs/REFACTOR-LEDGER.md`** — grepped for any citation into the files that changed this window
  (`order-lifecycle.service.ts`, `privacy.service.ts`, `rider.service.ts`, `tracking.gateway.ts`);
  zero references, so no drift risk.
- **`docs/FRAUD-REVIEW.md`** P0-1's fix location, P2-3, P1-4, P3 section — spot-checked, no drift
  beyond the citations already listed above.
- **`docs/plans/2026-rider-wallet-design.md`** numeric claims — `COMMISSION.lowBalanceBlockBelow=2`,
  `.minTopUp=5`, `.maxTopUp=50`, `.graceCredit=5` (`policy.ts:130-146`);
  `WALLET_MANUAL_CREDIT_CAP_USD` default `50`; `TOPUP_WINDOW_MS=90_000` — all match code exactly.

## Determinism note

Re-running this pass against unchanged code/docs should produce zero further edits — every fix
above is tied to a specific `file:line` I read and re-verified during this run (grep + direct file
reads, independent of the research agents' drafts) and cited in the sections above. The six
CODE_BUG items are tracked in `docs/KNOWN_BUGS.md`, not this doc, so their eventual fixes won't
show up here as "drift" — they'll show up as `KNOWN_BUGS.md` status changes for the owning lane to
make.
