# Refactoring routine — 2026-08-09 (LC loop R, twenty-third run)

Twenty-third run of the recurring refactoring routine (`docs/ROUTINES.md` → "Refactoring
routine"), run by the temporary LC loop R sprint. Branch `claude/bold-ramanujan-hlmdp5`, based on
`origin/main` @ `05406e4`.

## Phase 0 — orient

- `docs/REFACTOR-LEDGER.md`: the twenty-second run's design pass (`docs/RF-22-RIDER-BOARD-SCREEN.md`)
  left RF-22 as "OPEN — design pass done, next increment is the `activeJobBanner` extraction" — the
  first item in the design note's recommended five-step sequence.
  Read `docs/ROUTINES.md` § "Refactoring routine" and `docs/KNOWN_BUGS.md` for doctrine/dedup; no
  overlap found (no `KNOWN_BUGS.md` row references the rider board screen).
- `list_pull_requests` (open, `unnfazzed/Lynia`) returned two open PRs (#625 `claude/lc-c-20260809`
  — LC loop C, #624 `claude/lucid-rubin-4ldw0u` — a steering PR) — neither on a `claude/lc-r*`/
  `claude/refactor-*` branch, so no in-flight PR from this lane to babysit.
- Gate: fresh checkout needed `pnpm install` + `apps/api`'s `prisma generate` (generated client isn't
  checked in, the same one-time gap every prior run has noted) before
  `pnpm typecheck && pnpm build && pnpm test`:
  - `pnpm typecheck` — 6/6 packages green.
  - `pnpm build` — 5/5 tasks green.
  - `pnpm test` — 98 API test files / 1563 API tests, 11 admin / 57 admin tests, 26 merchant / 183
    merchant tests, 9 shared / 157 shared tests, 127 mobile suites / 902 mobile tests — all green,
    no flakes this run.

## Phase 1 — priority order (a): first actionable OPEN row

RF-22's design note (`docs/RF-22-RIDER-BOARD-SCREEN.md`) named the `activeJobBanner` extraction as
its recommended first increment: the smallest, safest of the five queued extractions — a full
alternate view with five bounded props (`activeJob`, `activeQ.isError`, `activeQ.isFetching`,
`activeQ.refetch`, plus the `pushOnce(router, pathname, "/rider/job")` navigation callback), no
local state, purely a display of query results. Not sensitive: it renders the active-job state, it
doesn't submit a bid or mutate order assignment.

Executed exactly that: moved the hoisted `activeJobBanner` const and its `ActiveJobCheckFailedBanner`
helper (both already isolated in `rider/(tabs)/index.tsx`, lines 62-80 and 677-698) verbatim to a new
`apps/mobile/src/ui/rider/RiderActiveJobBanner.tsx`, following the `src/ui/rider/*` (`JobCard.tsx`,
`SentOfferCard.tsx`) extraction convention already established in this directory. The component takes
`activeJob`, `isError`, `isFetching`, `onRetry`, `onOpenJob` — every prop already available at the
call site with no new derivation. The screen's own `activeJobBanner` const now just calls the new
component instead of inlining the JSX; both the `FlatList` and `ScrollView` return branches still
reference the same single `activeJobBanner` const, so they stay byte-identical to each other as
before. `statusPillLabel` and `formatMoney` imports dropped from the screen (only referenced inside
this block); both re-imported fresh in the new file. No characterization tests needed — the screen's
existing 242-line `app/rider/(tabs)/__tests__/index.test.tsx` already exercises the active-job banner
(assigned-job win state, generic active-job state, and the check-failed retry banner), and the move
is a verbatim relocation with unchanged props/behavior.

## What was deliberately skipped

- The remaining four queued extractions (`sentOffersSection`, `trailingFooterContent`,
  `onlineToggleCard`, `selectedCard`) — one extraction per PR per the routine's "one refactoring
  concern per PR" rule; `sentOffersSection` is next per the design note's sequence.
- A fresh hotspot recompute — priority order (a) had an actionable OPEN row, so step (b) wasn't
  reached; the prior recompute (2026-08-02, merchant/food-scoped) is still current.

## Self-disable check

Not exhausted: RF-22 has four remaining queued extraction increments (three non-sensitive, one
sensitive) per the design note's sequence — the next firing's actionable row is the
`sentOffersSection` extraction. No trigger-disable action taken this run.

## Verification

`pnpm typecheck && pnpm build && pnpm test` green pre+post: 98 API test files / 1563 API tests, 11
admin / 57 admin tests, 26 merchant / 183 merchant tests, 9 shared / 157 shared tests, 127 mobile
suites / 902 mobile tests — unchanged counts everywhere, pure relocation, no test added/removed/
weakened (the screen's own `index.test.tsx` passes unmodified). `pnpm lint` 0 new warnings (1
pre-existing unrelated `admin-orders.service.spec.ts` shadow warning; mobile itself 0/0 on 347
files). `pnpm run depcruise` 0 new violations (6 pre-existing info-level orphan notices, none for
the new file — it's imported by the screen, not orphaned). Public surface untouched: no controller/
route/DTO/socket-event touched, `packages/shared` untouched, screen's default export/route path
unchanged, mobile stays JS/TSX-only and OTA-able. Diff: 1 file changed in the screen (55 raw changed
lines: 11 insertions/44 deletions) + 1 new 68-line file — well under the ~400-line guideline.

## Ledger

`docs/REFACTOR-LEDGER.md` updated in this PR: RF-22's debt-register row updated to reflect the
`activeJobBanner` extraction done, next increment `sentOffersSection`; new completed-log entry at
the top; "Last updated" note revised. This report replaces the twenty-second run's report
(`REFACTOR-2026-08-04f.md`, deleted in this PR) per the docs retention policy — that run's findings
(the RF-22 design pass) are preserved in the ledger's debt register and completed log, the durable
record.
