# LC steer — 2026-08-04b (Harare low-connectivity replan)

Second steer of the day (the sprint schedule runs the steer 2×/day, `30 5,17 * * *`). Since
`docs/LC-STEER-2026-08-04.md` (this morning's run, deleted in this PR per the retention rule),
**three lane PRs landed and were merged by this steer itself** (all green, zero review comments,
found sitting open at orient time) — A-O4, B-O10, C-O2 — and **Lane C is now down to its last two
optimization items**, the closest any non-complete lane has been to closing out.

## 1. Orient

Merged since `docs/LC-STEER-2026-08-04.md`:

| PR | Lane | What |
|---|---|---|
| #571 | LC-A | A-O15 — skip redundant active-order refetch when cache is fresh |
| #576 | LC-C | C-O1 — honest "queued" state for offline-paused mutations (ALR-09) |
| #582 | LC-C | **merged by this steer** — C-O2 — central mobile network-reliability policy (timeouts/jittered backoff), `LC-C15` |
| #583 | LC-B | **merged by this steer** — B-O10 — cursor-paginate `GET /restaurants`; investigated but did NOT land B-O6 (real correctness trap found) |
| #584 | LC-A | **merged by this steer** — A-O4 — gate the rider `activeJob` self-heal poll on online/active-job |
| #585 | refactoring lane | RF-21 send.tsx extraction (not LC-branded, noted for completeness) |

**Orient found three open, green, unreviewed `claude/lc-*`-family PRs** (#582 Lane C, #583 Lane B
branch `claude/ecstatic-pascal-0m07in`, #584 Lane A) sitting unmerged — each lane's most recent
firing had shipped its increment but nothing had merged it yet (all three were created within an
8-minute window shortly before this steer ran, CI had already gone green on all three by orient
time). Per the universal merge-on-green policy (`docs/ROUTINES.md`, applies to all Claude-authored
PRs, not just scheduled routines) this steer merged all three itself rather than leaving them for
the next lane firing to babysit. No stalled-red PRs, no unresolved review comments anywhere.

**Also found and corrected: `docs/APP-SIZE.md` shows `EAS_RELEASE_ENABLED` is now armed and
shipping** — a release `.aab` (build `c248fbf5`, v0.17.9) is live on the internal track, first
measured at **32,546,001 B (31.04 MiB) raw, pre-split**. This directly clears the founder-gated
blocker §9 and the prior steers' "Next steer" watch-list have been carrying since Day-0. §2's
native-per-device-download budget row and §9's open-questions list were both stale (still read
"unmeasured... blocked on EAS_RELEASE_ENABLED") — updated in this PR with the real number and a
corrected account of what A-O8/B-O6 are actually still blocked on (see §3/§4 below — arming EAS is
not the same as this environment having on-device/emulator access, and B-O6's own 2026-08-04e
investigation is direct evidence of that distinction, not just theory).

## 2. Loop health

**Three of four lanes are healthy and landing real work every firing; the fourth (Lane D) remains
in the same tooling-gap holding pattern flagged by every steer since 08-02b.**

- **Lane A** — A-O4 landed this window (merged by this steer). 8 optimization items remain
  (`A-O5`, `A-O16`, `A-O10`, `A-O7`, `A-O2`, `A-O3`, `A-O8`, `A-O18`), no reorder needed (see §4).
- **Lane B** — B-O10 landed this window (merged by this steer); B-O6 was investigated and correctly
  NOT force-landed (see §1). 5 optimization items remain, re-ranked this steer (see §4) —
  `B-O13`/`B-O14`/`B-O15` (all real, S-effort, fully landable here) now lead `B-O3`/`B-O6` (both
  confirmed blocked on resources this environment doesn't have).
- **Lane C** — C-O2 landed this window (merged by this steer). **Only 2 optimization items remain**
  (`C-O4`, `C-O10`) — the shortest checklist of any non-complete lane, one or two firings from a
  completion call.
- **Lane D** — **still COMPLETE, trigger disable still blocked** (see below).

**Trigger-disable tooling gap — reconfirmed a sixth time.** `ToolSearch` for
`list_triggers`/`update_trigger`/`create_trigger`/`trigger` again returned no matching tool in this
session's toolset. Lane D's checklist has been 100% checked since D-O3 (2026-08-04, PR #552); its
trigger ("LC loop D — journey & soundness sweep") is still enabled and cannot be disabled from
inside any LC steer or lane session run so far (08-02, 08-02b, 08-03, 08-03b, 08-04, now 08-04b).
Per the program doc, any further Lane D firing is a safe no-op (nothing left to audit or optimize)
— wasted tokens, not a correctness risk. The scheduled sprint-cadence revert (2026-08-04 23:00 UTC,
which would drop Lane D from 8×/day back to a daily 07:00 UTC no-op) had not yet fired as of this
steer (17:30 UTC) — reconciled the note in `docs/routines/harare-loops.md`. **Escalating again, now
a sixth consecutive occurrence**: this needs either a session with real trigger-management tools or
the founder disabling the Routine directly in the claude.ai UI — no LC session can close this loop.

**`LC-B-SIB-1`/`LC-B-SIB-3`/`LC-B-SIB-4`/`LC-D-SIB-1`/`LC-D-SIB-3`/`LC-D-SIB-4` are still
unclaimed** (`LC-B-SIB-2` was claimed and fixed by Lane D as `D-O4`, 2026-08-04d — the one sibling
item that DID get picked up). A repeat of the search from the last two steers (merged PRs matching
`WD-`/`BH-`/`UX-`/`DS-` prefixes or `claude/wallet*`/`claude/bug-hunt*`/`claude/ux*`/
`claude/deep-sweep*` branches) still found nothing past PR #377 (2026-07-21) — now **over two
weeks stale**, a fourth consecutive steer flagging the same standing-routine stall with no visible
response. Not moving these onto an LC lane by fiat (same reasoning as the last three steers — it's
scope creep outside this program's low-connectivity/low-end-device mandate), but this is now past
the "worth a more direct channel than another report paragraph" threshold the last steer named.
**Flagging directly for the founder once more, more bluntly: something is wrong with the
wallet-audit/bug-hunt/UX/deep-sweep routine rotation, not just unlucky scheduling — five OPEN
money-adjacent findings have sat unclaimed for two-plus weeks across four separate steer checks.**

## 3. Budget trend

`apps/mobile/size-budget.json`: `androidExportTotalBytes: 7,850,000`, `androidHermesBundleBytes:
6,455,000` — **unchanged this run**. Last real `expo export` re-measurement was A-O13
(2026-08-03e): export total 6,546,466 B (≈16.6% headroom), Hermes ≈6,189,316 B (≈4.1% headroom).
This window's three merges (A-O4, B-O10, C-O2) all touched mobile JS (B-O10 added a new
`useInfiniteQuery` hook + screen wiring; C-O2 added a new `network-policy.ts` module) but none
re-ran `expo export` to report a fresh number — the required `mobile-bundle-size` CI gate passed on
all three against the unchanged budget, so no regression slipped through, but the real headroom
number is now stale-in-the-pessimistic-direction (some JS was added, actual headroom is likely
somewhat below 4.1%/16.6%, not above). Flagging for whichever Lane A firing next touches bundle
size to re-measure and correct the record, same as the session-data row below.

**No ratchet this run** — same call as every steer so far. The §2 bar is time-elapsed ("2+ weeks
of headroom"), not magnitude, and the program is 4 days old (2026-08-01 → 2026-08-04).

**Native per-device download — NEW real number this run** (see §1): 32,546,001 B (31.04 MiB) raw
AAB, pre-split. Recorded in §2 and `docs/APP-SIZE.md`; no ratchet action here since it's a first
baseline, not a regression to react to. Worth a "watch" note for the next steer: once Play Console
reports the actual per-device split download (smaller than the raw AAB), that's the number to track
against native-size regressions going forward.

**Session-data budgets — still the same stale-optimistic flag the last two steers carried, still
not re-baselined** (A-O14/A-O17 landed since the last A-T4 trace, both further improving on the
already-beaten provisional targets). Not re-baselining in a steer session per the same reasoning as
before — needs A-T4's field-by-field trace rigor, not a steer-session estimate. Still the natural
next A-T4-style audit whenever Lane A's checklist next has room.

## 4. Re-rank

**Lane A — no reorder**, same call as 08-04's steer: nothing new this window argues to move
`A-O5`/`A-O16`/`A-O10`/`A-O7`/`A-O2`/`A-O3`/`A-O8`/`A-O18` relative to each other. A-O4 (which just
landed) was already correctly positioned ahead of all of these.

**Lane B — B-O13/B-O14/B-O15 promoted ahead of B-O3/B-O6** (full rationale in the program doc's
checklist header, summarized in §2 above): both remaining items ahead of them in the old order are
confirmed blocked on resources this environment doesn't have — B-O3 needs on-device
systrace/logcat timestamps (flagged blocked since 2026-08-02 steer #2, no new evidence changes
that), and B-O6's 2026-08-04e investigation (this window) found a REAL correctness trap (Inter's
subset TTFs' internal name tables don't match this app's `fontFamilies` keys — landing the naive
fix would have silently broken iOS typography app-wide) that needs an actual on-device/emulator
check to verify a fix against, which this environment cannot provide. Two firings in a row landing
on a doomed-to-defer item wastes real cycles; `B-O13`/`B-O14`/`B-O15` are all real, S-effort, and
fully landable here today. `B-O3`/`B-O6` stay on the checklist (never silently dropped) — just
behind the work that can actually close this run.

**Lane C — no reorder of the 2 remaining items** (`C-O4`, `C-O10`): C-O10's own text is explicit it
needs a >15-minute outage to matter, a materially rarer trigger than C-O4's MicroCache serve-stale
scope, so effort-per-impact still correctly ranks it last — same call as 08-04's steer, just with
everything ahead of them now landed.

**Lane D — no re-rank; checklist complete** (see §5).

**Not moved onto any lane's checklist:** `LC-B-SIB-1/3/4`/`LC-D-SIB-1/3/4` — see §2's escalation.
**No newly discovered work to append** — none of this window's three merged PRs (A-O4, B-O10, C-O2)
left a fresh follow-on finding; B-O10's investigation of B-O6 refined that existing item in place
(added the concrete fix recipe, see the program doc) rather than spawning a new one.

## 5. Completion calls

- **Lane A** — audit phase complete; 8 optimization items remain.
- **Lane B** — audit phase complete; 5 optimization items remain (2 confirmed blocked on
  resources this environment lacks, 3 real and landable).
- **Lane C** — audit phase complete; **2 optimization items remain — the shortest active
  checklist yet**, both real and landable, no blockers. Watch for the next steer to be a
  completion call if both land before then.
- **Lane D** — **COMPLETE since 2026-08-04** (D-O3). Trigger disable still blocked on the same
  tooling gap flagged in §2 — sixth consecutive steer unable to execute it.

The §8 four-lanes-disabled program-close condition is not met (1 of 4 complete, 0 of 4 disabled,
same as the last steer) — closer than last time (Lane C down to 2 items instead of 6) but not at
the next real inflection point yet.

## 6. Ledger / doc hygiene

- `docs/KNOWN_BUGS.md`: `LC-C15` (new, from C-O2) already added by that PR; checked against the
  program doc's current checkbox state, no drift found beyond that.
- **Report retention cleanup** (in this PR): deleted the now-superseded
  `docs/LC-A-REPORT-2026-08-04.md`/`-04b.md`/`-04c.md` (kept `-04d.md`, the A-O4 report);
  `docs/LC-B-REPORT-2026-08-04.md`/`-04b.md`/`-04c.md`/`-04d.md` (kept `-04e.md`, the B-O10
  report); `docs/LC-C-REPORT-2026-08-04.md`/`-04b.md`/`-04c.md`/`-04d.md` (kept `-04e.md`, the C-O2
  report); `docs/LC-D-REPORT-2026-08-04.md` (kept `-04d.md`, the D-O4 report). One most-recent
  report per lane now on disk, matching the retention rule — this cleanup had fallen behind over
  today's high firing rate (5 reports had accumulated for both Lane B and Lane C). This report
  replaces `docs/LC-STEER-2026-08-04.md` (deleted in this PR).
- `docs/routines/harare-loops.md`: reconciled the Lane D status note (sixth-occurrence count,
  confirmed the sprint-cadence revert hadn't fired yet as of this steer).
- Program doc (`docs/plans/2026-08-01-low-connectivity-program.md`): §2 native-download budget row
  and §9 EAS-arming open question both updated with the real 2026-08-04 measurement; Lane B
  optimization checklist physically reordered (B-O13/14/15 ahead of B-O3/B-O6, not just annotated,
  since a lane firing takes the first unchecked item by document position); Lane B and Lane C
  checklist header rationale notes updated to point at this report instead of the deleted one and
  to stop citing now-landed items as "just promoted."

## Next steer

Watch for: whether Lane C's last 2 items (`C-O4`, `C-O10`) land, making it the program's second
completion call; whether Lane B's re-rank actually produces landed `B-O13`/`14`/`15` work instead
of another blocked-item cycle; whether the sprint-cadence revert (2026-08-04 23:00 UTC) has fired
and dropped Lane D to a daily no-op; whether the trigger-disable tooling gap has been resolved
(sixth ask); and whether the wallet-audit/bug-hunt/UX/deep-sweep routine stall (now 2+ weeks, four
steers flagging it) has gotten a founder look — this steer escalated it more directly than the last
three did.
