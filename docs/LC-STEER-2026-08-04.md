# LC steer — 2026-08-04 (Harare low-connectivity replan)

Since `docs/LC-STEER-2026-08-03b.md`, all four lanes fired and landed real work — most notably,
**Lane D's checklist is now fully complete** (every Day-0 defect, audit territory, and optimization
item checked). No open/stalled `claude/lc-*` PRs were found this run — the four lanes are clean.

## 1. Orient

Merged since the last steer (`docs/LC-STEER-2026-08-03b.md`), LC-branded only:

| PR | Lane | What |
|---|---|---|
| #531 | LC-B | B-O8 — gate the unconditional 1s countdown tickers in the food-order screens |
| #532 | LC-C | C-O5/LC-C07 — durable rider delivery-confirm terminal marker survives an app kill |
| #533 | LC-D | D-O1 — extract shared low-connectivity error/retry/stale components |
| #534 | LC-A | A-O9 — socket-gate the food-order/rider-job tracking polls |
| #540 | LC-B | B-O11 — KYC/pickup-photo previews use the downscaled upload asset |
| #541 | LC-A | A-O6 — sample RUM apifetch 1-in-4, widen flush cadence |
| #542 | LC-C | C-O6/LC-C08 — reconcile `selectOffer` 409s before showing "rider just taken" |
| #543 | LC-D | D-O2 — OTP delivery + verify success telemetry by carrier |
| #549 | LC-B | B-O7 — defer push-register/version-gate/feature-flags behind bootstrap |
| #551 | LC-C | C-O7/LC-C09 — persist pickup-proof photo capture before upload fires |
| #552 | LC-D | D-O3 — collapse-key on order-status pushes — **Lane D closes out** |
| #553 | LC-A | A-O14/LC-A06 — omit null cash-handshake/debt/refund fields from food order responses |

(Non-LC merges in the same window: refactoring-lane RF-19/RF-20/RF-21, four `chore(main): release`
PRs, three dependency bumps, and a run of Play/EAS release-pipeline PRs (#536/#537/#545/#546/#547/
#550) fixing fingerprint-parity and CI-arming issues on the path to arming `EAS_RELEASE_ENABLED` —
noted for completeness, not LC-branded, and not this steer's lane to act on. That work is directly
relevant background for A-T5/A-O8/B-O6/the native-binary budget row: EAS is closer to armed but
still blocked on the founder re-creating `GOOGLE_MAPS_API_KEY` as a Sensitive env var per #550's own
stop-rule — no LC item unblocks yet.)

Twelve LC-branded PRs landed across four lanes since the last steer, in roughly even distribution
(3 per lane) — no lane forked a second in-flight branch while one was open, no lane re-did another
lane's or a sibling's work. `list_pull_requests(state: open)` found exactly one open PR in the repo
right now (`#548`, a release-please branch) — zero open `claude/lc-*` PRs, so there was nothing to
babysit or merge this run (unlike the last steer, `docs/LC-STEER-2026-08-03b.md`, which found four
stalled-green PRs sitting unmerged and merged them itself).

## 2. Loop health

**All four lanes are healthy.** Every lane landed 2-3 merged PRs since the last steer, none stalled,
none left a green PR unmerged. **Lane D's checklist reached 100% completion this window** (D-O3,
the last item, shipped 2026-08-04) — every Day-0 defect (D-D0a-f), every audit territory (D-T1-T5),
and every optimization item (D-O1-O3) in the program doc §5 Lane D section is checked.

**Trigger-disable tooling gap — now confirmed absent in every LC steer session run so far.** Per
SELF-DISABLE, a lane whose checklist is fully checked should have its trigger disabled
(`list_triggers` + `update_trigger enabled:false`). This session's `ToolSearch` for
`list_triggers`/`update_trigger`/`create_trigger`/`trigger` returned **no matching tool** — same
gap the D-O3 firing hit when it tried to self-disable, and the same gap steers 08-02, 08-02b, 08-03,
and 08-03b each recorded (none of them needed to act on it before now, since no lane was complete).
This is the first time the gap has actually blocked a real action. **Escalating per the program's
own "if a third [occurrence] passes... worth escalating harder" standard** (previously applied to
the SIB items below, now clearly also true of this tooling gap): **the "LC loop D — journey &
soundness sweep" trigger needs to be disabled by a session with `update_trigger` available, or by
the founder directly in the claude.ai Routines UI.** Until then it is a safe no-op (nothing left to
audit or optimize) — wasted tokens, not a correctness risk — and the 2026-08-04 23:00 UTC
sprint-cadence revert (already scheduled, unrelated to this gap) at least drops its firing rate from
8×/day back to a 1×/day no-op tonight.

**No lane's prompt needed repair** — no lane's trigger errored on model validation this window
(the model-pinning caveat in `docs/routines/harare-loops.md` is unchanged: programmatic pinning is
still `model_update_disabled`, so all five LC triggers still fire on the account/environment
default until the founder applies the intended per-Routine model in the UI).

**Escalating harder, as flagged last steer: `LC-B-SIB-1..4`/`LC-D-SIB-1..4` are now unclaimed for a
third consecutive steer**, and this run found concrete evidence of *why* — a search for merged PRs
matching the standing bug-finding routines' branch/title conventions (`WD-`, `BH-`, `UX-`, `DS-`
prefixes; `claude/wallet*`, `claude/bug-hunt*`, `claude/ux*`, `claude/deep-sweep*` branches) found
**zero merges since 2026-07-28** — the wallet & data-lifecycle audit routine's last shipped PR is
`#377` (2026-07-21), two weeks stale. This is outside LC steer's mandate to diagnose or fix (the
eight standing routines' health isn't an LC lane), but it directly explains the stall: the routines
`LC-B-SIB`/`LC-D-SIB` are dual-routed to aren't landing anything right now, LC-branded or otherwise.
**Flagging directly for the founder**: worth checking whether the wallet-audit/bug-hunt/UX/deep-sweep
routines' triggers are still enabled and firing — `docs/routines/routine-chain.md`'s maintenance
rotation (5 slots/day across 8 standing lanes, `0 2,5,8,14,20 * * *`) may simply not have reached
them during the LC-sprint + Restaurants/Send-build-loop grid crunch, or something else may be
blocking them; either way, 8 real (if low-severity/dormant) money-path findings sitting unclaimed
for 2+ weeks is worth someone's direct look, not a fourth steer just re-flagging it. Not moved onto
any LC lane's checklist this run either, for the same reason as before: forcing them onto an LC lane
by fiat would be scope creep (§1 — this program is low-connectivity/low-end-device, not general
money-correctness) rather than letting the actual dedup-ledger owner claim them.

## 3. Budget trend

`apps/mobile/size-budget.json`: `androidExportTotalBytes: 7,850,000`, `androidHermesBundleBytes:
6,455,000` — unchanged this run. No PR since the last steer touched the JS bundle or export assets
(A-O9/A-O6/B-O7/B-O8/B-O11/C-O5/C-O6/C-O7/D-O1/D-O2/D-O3/A-O14 are all logic-only, server-response-
shape-only, or already-reflected changes — none re-measured `expo export`), and `mobile-bundle-size`
is a required CI gate on every one of those PRs, so no regression could have slipped through
unnoticed. Headroom stands where the last steer left it: Hermes bundle ≈4.1% (post A-O11/A-O12),
export total ≈16.6% (post A-O13, the stronger of the two).

**No ratchet this run** — same call as the last two steers. The §2 bar is "2+ weeks of headroom
exists" (a time-elapsed bar, not a magnitude one); the program is 3 days old (2026-08-01 → 2026-08-04)
regardless of how comfortable either number looks. Recording the trend again so the eventual ratchet
has a paper trail: both numbers have now held flat with zero regression across five steers running.

**Session-data budgets (§2's other row) — worth flagging for the next audit-quality re-baseline, not
re-baselining here.** A-T4's original trace (2026-08-03) explicitly attributed ≈140 KB of the 172 KB
parcel-journey response to RUM telemetry, and both of the two biggest levers it identified have
since landed: A-O9 (food dual-poll, −90%/−92% on the two traced windows) and A-O6 (RUM sampling,
−72%/−73% on the two traced hourly rates). The provisional targets (parcel ≤120 KB, food ≤150 KB,
rider ≤200 KB/h) are very likely already beaten in practice, but computing the *actual* new numbers
needs the same field-by-field trace rigor A-T4 used (not a back-of-envelope steer-session estimate —
this doc's numbers get treated as ground truth by every subsequent lane firing, and an imprecise
guess here would be worse than leaving it stale with an honest caveat). Not adding a new checklist
item for this — it's report-only bookkeeping, not a code change — but flagging it as the natural
next A-T4-style audit whenever Lane A's checklist next has room, since the current row is now
demonstrably stale in the *optimistic* direction (real usage is better than what's written, not
worse — no urgency, but worth correcting for accuracy).

## 4. Re-rank

**Lane B — B-O16/B-O17/B-O18 promoted to #1-3, ahead of B-O5.** These three are B-T4's findings
(audited 2026-08-03b, the same "this week" horizon this steer's evidence bar applies to): concrete
file:line locations, S/M/S effort, and — critically — they hit the **merchant kitchen tablet**, an
always-mounted Go-class device sitting on a counter for an entire shift, which is exactly the
hardware this lane's mandate targets. B-O16 (unconditional 1Hz ticker on `payment`/`ready`-bucket
cards) and B-O17 (no memo boundary on `QueueBoard`/`OrderCard`) are natural to bundle in one pass per
B-O17's own note; B-O18 (a single `useNativeDriver: false` flip) is a one-line trailer. By contrast
`B-O5` ("socket self-heal refetch cadence — KNOWN backlog") has zero evidence behind it — no file:line,
no measured cost, seeded early and never revisited since sitting behind a queue of concrete work.
Re-ranking by (impact/effort) using this week's evidence clearly favors the evidenced trio. `B-O3`
(blocked on on-device hardware access this environment lacks) and `B-O6` (blocked on the native build
train, itself blocked on the same EAS-arming gap noted in §1) stay where they were — genuinely
un-actionable today, not just deprioritized by preference. `B-O10`/`B-O13`/`B-O14`/`B-O15` are
unchanged (already correctly ordered by severity: one real M-effort scaling item, then three
S-effort completeness-only Set-growth notes).

**Lane C — C-O9 promoted to #2, ahead of C-O1/C-O2/C-O4, for the identical reason the 2026-08-03
steer promoted C-O5/C-O6/C-O7:** it's a concrete, S-effort, evidenced-this-week (C-T4, 2026-08-03c)
fix — a one-line `refetch()` call in an existing catch block, mirroring an already-proven pattern
elsewhere in the same lane — versus C-O1/C-O2/C-O4's broader M-effort backlog scope with no fresh
measurement behind any of them. `C-O8` stays first: it's the direct continuation of the C-O7 pattern
that just shipped this window (same durable-draft-before-request shape, different screen), so it's
both cheap and low-risk to implement immediately while that code is fresh. `C-O10` (the socket
auth-callback hardening item) stays last — its own text is explicit that it needs a >15-minute
outage to matter, a materially rarer trigger than anything else on the list, so effort-per-impact
still ranks it below the M-effort items it sits behind.

**Lane A — no reorder.** The remaining checklist (`A-O17` → `A-O15` → `A-O4` → `A-O5` → `A-O16` →
`A-O10` → `A-O7` → `A-O2` → `A-O3` → `A-O8` → `A-O18`) was set by the A-T4 evidence trace and the
2026-08-03 steers; nothing new this window argues to move anything. `A-O17` (multiplex the three
independent Socket.IO connections during an active rider job) correctly leads now that A-O9/A-O6/
A-O14 — everything that outranked it — is done; it also happens to be the fix Lane C's now-struck
`C-O3` deferred to, so landing it closes a second lane's item for free.

**Lane D — no re-rank; checklist complete (see §5).**

**Not moved onto any lane's checklist:** `LC-B-SIB-1..4`/`LC-D-SIB-1..4` — see §2's escalation.
**No newly discovered work to append** — none of this window's four PRs (A-O14, B-O7, C-O7, D-O3)
left a fresh follow-on finding beyond what's already tracked (A-O14 explicitly compounds with the
already-tracked A-O18; B-O7/C-O7/D-O3 close out clean).

## 5. Completion calls

**Lane D is COMPLETE — every box checked, trigger still enabled, cannot be disabled from this
session** (see §2). Recording the call here per the program doc's own instruction even though the
mechanical disable couldn't execute: **Lane D's mission is done.** Status of the other three:

- **Lane A** — audit phase complete; 11 optimization items remain (`A-O17`, `A-O15`, `A-O4`, `A-O5`,
  `A-O16`, `A-O10`, `A-O7`, `A-O2`, `A-O3`, `A-O8`, `A-O18`).
- **Lane B** — audit phase complete; 10 optimization items remain (`B-O16`, `B-O17`, `B-O18`, `B-O5`,
  `B-O3`, `B-O6`, `B-O10`, `B-O13`, `B-O14`, `B-O15`).
- **Lane C** — audit phase complete; 6 optimization items remain (`C-O8`, `C-O9`, `C-O1`, `C-O2`,
  `C-O4`, `C-O10`) — the shortest remaining checklist of the three still-active lanes, and the
  closest to the next completion call.
- **Lane D** — **COMPLETE.** Trigger disable pending (see §2).

The §8 four-lanes-disabled program-close condition is not met (1 of 4 complete, 0 of 4 disabled) —
the program is not close to done, but it has crossed its second real inflection point: one full lane
finished, not just audit-complete.

## 6. Ledger / doc hygiene

- `docs/KNOWN_BUGS.md` is current: `LC-C09` correctly shows **FIXED** (C-O7, this window); `LC-A06`
  correctly shows **FIXED** (A-O14, this window); all other LC-* rows checked against the program
  doc's current checkbox state, no drift found, nothing to reconcile.
- **Report retention cleanup** (in this PR): deleted `docs/LC-A-REPORT-2026-08-03f.md`/`-03g.md`
  (superseded by `-04.md`, the A-O14 report); `docs/LC-B-REPORT-2026-08-03b.md`/`-03e.md` (superseded
  by `-04.md`, the B-O7 report); `docs/LC-C-REPORT-2026-08-03d.md`/`-03f.md`/`-03g.md` (superseded by
  `-04.md`, the C-O7 report); `docs/LC-D-REPORT-2026-08-03f.md`/`-03h.md` (superseded by `-04.md`,
  the D-O3/Lane-D-closes-out report). One most-recent report per lane now on disk, matching the
  retention rule. This report replaces `docs/LC-STEER-2026-08-03b.md` (deleted in this PR).
- `docs/routines/harare-loops.md`: updated the Lane D status note to record this steer's confirmation
  of the same trigger-disable tooling gap the D-O3 firing hit (see §2), and to note the sprint-cadence
  revert tonight shrinks (but doesn't eliminate) the wasted-firing cost until someone can disable it.
- Program doc (`docs/plans/2026-08-01-low-connectivity-program.md`) §5: Lane B and Lane C
  optimization-checklist re-ranks from §4, both physically reordered (not just annotated) since a
  lane firing takes the first unchecked item by document position.

## Next steer

Watch for: whether Lane C's 6-item checklist has shrunk enough to be the next completion call (it's
now the shortest of the three still-active lanes); whether the `EAS_RELEASE_ENABLED`/
`GOOGLE_MAPS_API_KEY` blocker noted in §1 clears, which would unblock A-T5's native-binary
measurement and eventually `A-O8`/`B-O6`; whether the wallet-audit/bug-hunt/UX/deep-sweep routine
stall flagged in §2 has been looked at (this is the third steer to flag `LC-B-SIB`/`LC-D-SIB` as
unclaimed, now with evidence attached — if a fourth steer finds them still sitting there with no
sign anyone looked, that's worth a more direct channel than another report paragraph); and whether
the `list_triggers`/`update_trigger` tooling gap has been resolved so Lane D can actually be
disabled. The sprint-cadence revert (2026-08-04 23:00 UTC, already scheduled) should land before the
next steer fires — worth a rendered double-check that no lane got stranded mid-item by the cadence
drop from 8×/day back to daily.
