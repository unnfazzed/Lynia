# LC steer — 2026-08-03b (Harare low-connectivity replan)

Second sprint-cadence steer today (2×/day, 05:30/17:30 UTC through the Tue 2026-08-04 23:00 UTC
revert). Since `docs/LC-STEER-2026-08-03.md`, all four LC lanes fired multiple times and landed
real work — most notably, **every lane's audit-territory phase is now complete.** All four LC
loops are, for the first time, simultaneously in pure OPTIMIZE MODE. This run found three
in-flight PRs (one per lane: A, B, D) plus one refactoring-lane PR sitting green with nobody
watching them and merged all four, then swept a stale duplicate checklist item and did report
retention cleanup that had fallen behind again.

## 1. Orient

| PR | Lane | What | Status |
|---|---|---|---|
| #503 | LC-B | B-T4 audited (animation/JS-thread) — zero defects, 3 new optimizations (B-O16..B-O18) | merged |
| #504 | LC-A | A-T5 audited (native binary levers) — closes Lane A's audit phase | merged |
| #505 | LC-C | C-T3 audited (onboarding/OTP/KYC) — 1 defect fixed (LC-C10), 1 optimization ledgered | merged |
| #508 | LC-D | D-T2 swept (merchant journey) — 7 low-connectivity defects fixed | merged |
| #509 | LC-B | B-O1 — history/notifications lists → FlatList | merged |
| #510 | LC-A | A-O12 — zod v4 locales barrel redirected out of the Hermes bundle | merged |
| #511 | LC-C | LC-C12 fix — merchant N-03 auto-cancel safety net restored via queue-presence socket | merged |
| #512 | LC-D | D-T3 swept (notification/deep-link coherence) — LC-D18 cold-start race fixed | merged |
| #518 | LC-B | B-O1b/B-O12 — rider board open-orders list virtualized, board cache capped | merged |
| #519 | LC-A | A-O11 — dropped shared fixtures from the `@lynia/shared` barrel | merged |
| #520 | LC-C | C-T5 audited (realtime reconnect semantics) — zero defects, 1 optimization (C-O10); **closes Lane C's audit phase** | merged |
| #521 | LC-D | D-T4 swept (infra soundness I, read-only) — 9 findings ledgered, founder-owned | merged |
| #525 | LC-B | B-O2/B-O9 — memo boundaries for JobCard/JobDetailsCard/ComposeMap, `ranked`/`chooseOrder` memoized | **found open, green, unreviewed — merged this run** |
| #526 | LC-A | A-O13 — subset self-hosted Inter fonts to actually-used glyph ranges | **found open, green, unreviewed — merged this run** |
| #527 | LC-D | D-T5 swept (infra soundness II, read-only) — 11 findings ledgered, founder-owned; **closes Lane D's audit phase** | **found open, green, unreviewed — merged this run** |
| #528 | (RF) | Refactoring-lane RF-19, unrelated to LC | **found open, green, unreviewed — merged this run** |

(#513/#522/#507 refactoring-lane RF-18 work and #514/#517/#516/#515/#524/#523 releases/dependabot/
docs also merged in this window — noted for completeness, not LC-branded.)

**The headline: with #520 (Lane C, C-T5) and #527 (Lane D, D-T5) both merged, every one of the
four lanes' audit-territory checklists (A-T1..5, B-T1..4, C-T1..5, D-T1..5) is now fully ticked.**
No lane has an unswept audit territory left. All four move (or, for A/B, continue) in pure
OPTIMIZE MODE against their seeded/evidenced checklists.

## 2. Loop health

**All four lanes are healthy — no diagnosis, no prompt repair needed.** Between this steer and the
prior one (~12 hours), the four lanes together landed 11 LC-branded merged PRs, none stalled, none
forked a second in-flight branch while one was open, none re-did another's or a sibling's work.

**One action taken, same shape as last steer:** three lane PRs (#525 LC-B, #526 LC-A, #527 LC-D)
plus one refactoring-lane PR (#528) were sitting `open`, `mergeable_state: clean`, all CI checks
`success` (CodeQL, typecheck·build·test, mobile bundle-size, terraform validate, dependency
audit·secret scan, actionlint, prisma migrate), zero unresolved/pending review threads on any of
them, for roughly 1–1.5 hours with nobody watching. Squash-merged all four
(`0c018969`/`94fbd2e1`/`0fee70b6`/`5c5191ab`) rather than leave them idle — the universal
merge-on-green policy applies the moment a PR is green, not only to the lane/routine that opened
it, and each lane's own next firing would otherwise have babysat a PR that was already done.

**Tooling note (unchanged, third steer running):** `list_triggers`/`update_trigger` are still not
available in this session (re-confirmed via `ToolSearch` — only `CronCreate`/`CronList`/
`CronDelete` session-local reminders exist here, which are NOT the account's Routines triggers).
Not needed this run (no lane stalled or complete), but recording it again for whichever future
steer session actually needs to flip a trigger — it'll need to either run with those tools present
or hand the edit to the founder via the claude.ai Routines UI.

**Doc-hygiene catch (fixed this run, not a lane-health issue):** Lane B's checklist carried two
separate entries both numbered `B-O9` — the original `B-T2` audit finding (unchecked) and PR #525's
later fix-summary (checked), both using the same id. PR #525 ticked the fix-summary entry but never
resolved the older duplicate, which would have looked like an unimplemented open item to the next
Lane B firing. Ticked it done with a pointer to the real entry — see §4.

## 3. Budget trend

`apps/mobile/size-budget.json`: `androidExportTotalBytes: 7,850,000`, `androidHermesBundleBytes:
6,455,000` — unchanged this run (still no ratchet; see below).

Real measurements landed twice since the last steer:
- **A-O11+A-O12** (already reflected in the last steer, unchanged since): Hermes bundle measured
  6,189,316 B → **4.1% headroom**.
- **A-O13** (this window, font subsetting): Android export total measured **7,240,457 → 6,546,466
  bytes (−693,991 B / −9.6%)** — the three self-hosted Inter TTFs cut 65-67% each. Hermes bundle
  itself unchanged (fonts are export assets, not JS) — still ~6.19 MB. **Export-total headroom is
  now ≈16.6%** (was ≈7.8% before A-O13, back-computed from the pre-fix 7,240,457 B measurement),
  up from single digits a few days ago.

**No ratchet this run.** The bar in program-doc §2/the mirror is "2+ weeks of headroom exists" —
this is a time-elapsed bar, not a magnitude one, and the program is 3 days old regardless of how
comfortable either number now looks. Recording the trend so the ratchet has a paper trail once the
bar is actually met: Hermes headroom moved 0.4% → 4.1% (A-O11/A-O12) and has held there since (no
regression, no further Hermes-shrinking optimization has landed); export-total headroom moved
≈4.8% → ≈16.6% (Day-0 baseline → A-O13) and is the more comfortable of the two numbers by a wide
margin — worth flagging as the stronger ratchet-down candidate once 2 weeks actually elapse, since
it has both the bigger cushion and the more consistent one-directional trend. No regression found
on any PR this window — `mobile-bundle-size` is a required CI gate and merge-on-green means a
violation would have blocked the merge, not slipped through.

Session-data budgets (§2's other row): unchanged since A-T4 baselined them (parcel ≤120 KB, food
≤150 KB, rider hour ≤200 KB/h) — still gated on A-O9/A-O6 landing, both still open on Lane A's
checklist (see §4).

## 4. Re-rank

**Lane B: fixed the `B-O9` id collision (see §2), no other reorder.** With that resolved, Lane B's
first unchecked optimization item is `B-O8` (the food-order/rider-food-job unconditional 1Hz ticker,
`B-T2` finding, S-effort, same `AuctionClock`-extraction shape as the already-shipped `PERF20-02`)
— it already sat ahead of `B-O11` positionally and nothing found this week argues to swap them:
both are S-effort with confirmed root causes, `B-O8` is continuous JS-thread churn for most of an
order's real duration on the always-mounted screen, `B-O11`'s OOM-preview fix is real but narrower
(bounded to the KYC/pickup-photo flow). Leaving the existing order stands.

**Lane A, Lane C, Lane D: no reorder.** Lane A's remaining checklist (`A-O9`, `A-O6`, `A-O14`,
`A-O17`, `A-O15`, `A-O4`, `A-O5`, `A-O16`, `A-O10`, `A-O7`, `A-O2`, `A-O3`, `A-O8`) was already
ranked by the A-T4 evidence and nothing new this window argues to move anything — `A-O9`/`A-O6`
remain the two biggest `[data]` levers by a wide margin per that trace. Lane C's checklist
(`C-O5`/`C-O6`/`C-O7` → `C-O1`/`C-O2` → `C-O4` → `C-O8`/`C-O9`/`C-O10`) was re-ranked last steer
right after `C-T1`/`C-T2` evidence landed and `C-T5` only appended `C-O10` at the bottom (its own
correctly-ranked position, being the newest and lowest-severity finding) — no change. Lane D's
3-item checklist (`D-O1` → `D-O2` → `D-O3`) is unchanged; `D-T2`'s own report explicitly named
`D-O1` (the reusable low-connectivity state-pattern components) as "the standardization follow-up"
for several of that sweep's now-fixed ad hoc patches, which if anything reinforces `D-O1` staying
first rather than arguing to demote it for the cheaper `D-O3`.

**Not moved onto any lane's checklist: `LC-B-SIB-1..4` and `LC-D-SIB-1..4`.** Both quads of
money/wallet-path findings (commission-guard gaps, ledger-truncation/pagination, admin-action
CAS/idempotency gaps, isError-blind screens) remain correctly OPEN in `docs/KNOWN_BUGS.md`,
dual-routed to "Lane D or the wallet & data-lifecycle audit routine, whichever fires next" per the
standard dedup protocol — still un-picked-up this window. Same call as last steer: these are real
bugs but outside all four LC lanes' actual mandate (§1 — low-connectivity/low-end-device, not
general money-correctness), so forcing them onto an LC checklist now would be scope creep by fiat
rather than letting the next routine that does a Phase-0 ledger read claim them as designed.
Flagging again here so two consecutive steers not acting on them isn't mistaken for silently
dropping them — if a third steer passes with these still unclaimed, that's worth escalating harder.

**No newly discovered work to append beyond what each lane's own audit/optimize PR already
appended this window** (`A-O13` shipped with no further findings; `B-O2`/`B-O9` shipped clean;
`C-T5` appended `C-O10`, already reflected above; `D-T4`/`D-T5` appended `LC-D19`–`LC-D38`, all
correctly founder-owned infra rows, not lane checklist items per the read-only doctrine).

## 5. Completion calls

No lane qualifies for self-disable — every lane still has unchecked optimization items, and this
session still can't flip a trigger even if one did (see §2's tooling note). Status:

- **Lane A** — audit phase complete; 13 optimization items remain (`A-O9`, `A-O6`, `A-O14`,
  `A-O17`, `A-O15`, `A-O4`, `A-O5`, `A-O16`, `A-O10`, `A-O7`, `A-O2`, `A-O3`, `A-O8`).
- **Lane B** — audit phase complete; 12 optimization items remain (`B-O8`, `B-O11`, `B-O7`, `B-O5`,
  `B-O3`, `B-O6`, `B-O10`, `B-O13`, `B-O14`, `B-O15`, `B-O16`, `B-O17`, `B-O18`), post the `B-O9`
  dedup this run.
- **Lane C** — **audit phase now fully complete** (`C-T1`..`C-T5` all swept); 7 optimization items
  remain (`C-O5`, `C-O6`, `C-O7`, `C-O1`, `C-O2`, `C-O4`, `C-O8`, `C-O9`, `C-O10` — post the `C-O3`
  strike).
- **Lane D** — **audit phase now fully complete** (`D-T1`..`D-T5` all swept); only 3 optimization
  items remain (`D-O1`, `D-O2`, `D-O3`) — the shortest remaining checklist of any lane and the
  closest to a completion call, though still not there.

The §8 four-lanes-disabled program-close condition is not close, but the program has crossed a
real inflection point this run: **zero unswept audit territories remain anywhere** — every future
LC firing from here on is pure optimize-mode work against evidenced, ranked checklists.

## 6. Ledger / doc hygiene

- **`B-O9` id collision** (§2/§4): resolved — the stale duplicate is ticked done with a pointer to
  the entry that actually carries the fix, plus a note for future firings to never reuse a
  checklist id even for a same-item refinement.
- **Report retention cleanup** (fallen behind again, same as last steer): deleted
  `docs/LC-A-REPORT-2026-08-03.md`, `-03b.md`, `-03c.md`, `-03d.md` (superseded by `-03e.md`, the
  A-O13 report); `docs/LC-C-REPORT-2026-08-03.md`, `-03b.md`, `-03c.md` (superseded by `-03d.md`,
  the C-T5 report); `docs/LC-D-REPORT-2026-08-03b.md`, `-03c.md`, `-03d.md` (superseded by
  `-03f.md`, the D-T5 report — `-03e.md` had already been retired by the D-T5 PR itself). Lane B's
  single report (`-03b.md`) was already the sole file, nothing to prune there. This report replaces
  `docs/LC-STEER-2026-08-03.md` (deleted in this PR) per the same rule.
- `docs/KNOWN_BUGS.md` is current: `LC-A05` correctly shows **FIXED** (A-O13, this window);
  `LC-D28`–`LC-D38` correctly ledgered OPEN/founder-owned from `D-T5`; `LC-B-SIB-1..4`/`LC-D-SIB-1..4`
  correctly OPEN and dual-routed (§4). Nothing else to reconcile.
- Program doc (`docs/plans/2026-08-01-low-connectivity-program.md`) §5 edited in this PR: only the
  `B-O9` dedup fix. No re-rank changed any other line this window (§4).
- `docs/routines/harare-loops.md` (the trigger-prompt mirror): no changes needed — no trigger
  prompt was edited, no model fallback triggered, and the sprint-cadence revert note (§ "Sprint
  cadence 2026-08-02 → 2026-08-04") still correctly describes a revert tomorrow (2026-08-04 23:00
  UTC) that hasn't happened yet.

## Next steer

Fires again at ~05:30 UTC tomorrow (or the next 2×/day slot before the Tue 2026-08-04 23:00 UTC
sprint-cadence revert). By then, worth checking: whether Lane D's 3-item checklist has shrunk
further (it's the shortest of the four and the nearest thing to a completion call); whether
`LC-B-SIB`/`LC-D-SIB` have been picked up by a third routine yet (escalate the flag if still
untouched); and whether the export-total headroom (≈16.6%, §3) has held or grown enough that the
2-week ratchet bar is worth revisiting once it's actually been 2 weeks. Also worth a rendered
double-check that the sprint-cadence revert (tomorrow 23:00 UTC) doesn't strand any lane mid-item —
all four checklists are non-empty, so the daily-cadence crons (vs. the current 3-hourly ones) will
just pick up where the sprint cadence left off, one increment per firing as originally designed.
