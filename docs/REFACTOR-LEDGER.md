# REFACTOR-LEDGER.md — refactoring-routine memory

**Purpose.** The recurring refactoring routine (see `docs/ROUTINES.md` → "Refactoring
routine") reads this file FIRST on every run and writes back to it in the same PR as its
changes. It carries four things between runs: the hotspot map, the debt register, in-flight
multi-run migrations, and the completed-refactor log. Without this file the routine
re-analyzes from scratch; with it, each run resumes where the last one stopped.

**Last updated:** 2026-08-03 (tenth run, LC loop R — see `docs/REFACTOR-2026-08-03.md`).

## Hotspot map

Ranked churn × complexity candidates (45-day window). Recomputed 2026-08-02 — this run re-ran the
churn scan and, per the routine's mandate to triage newly-landed hot code, scoped it specifically to
`apps/api/src/merchant/*`, `apps/merchant/*`, and the food screens in `apps/mobile` — the
recently-landed Restaurants/merchant surface that had never had a refactor pass. Rows 1-9 below are
carried over unchanged from the 2026-07-26 general recompute (still stable — no new hit above them
in the general 45-day window); rows 10+ are new from this run's merchant/food-scoped scan.

| Rank | File | Churn (45d commits) | Why it's hot | Status |
|---|---|---|---|---|
| 1 | `apps/api/src/riders/rider.service.ts` | 9 | 559+ lines, KYC + rider-standing logic | SENSITIVE — avoided (KYC gating); needs characterization before any future touch |
| 2 | `apps/api/src/orders/order-lifecycle.service.ts` | 8 | 1058 lines, order assignment/lifecycle | SENSITIVE — avoided (order assignment) |
| 3 | `apps/api/src/admin/admin-orders.service.ts` | 9 (rose from 7) | 584 lines, `adjustFare` mutates agreed-price + `adjudicateDelivered` is KB-POD-DISPUTE adjudication | SENSITIVE-ADJACENT — avoided (agreed-price/dispute adjudication); not yet scoped |
| 4 | `apps/api/src/admin/admin-riders.service.ts` | 8 (spec) | 630→~525 lines pre-this-run, mixed KYC-review reads + standing mutations | **RF-06 DONE this run** (2026-07-19) — see completed log. The read-side (`getKycReview` + duplicate-ID collision + photo-URL signing) is now `AdminKycReviewService`; the sensitive mutation core (`suspendRider`/`liftRider`/`banRider`/`clearHold`) stays in `AdminRidersService`, untouched, still SENSITIVE (KYC/standing gating) |
| 5 | `apps/mobile/src/auth/session.ts` → `device-state.ts` (589→68 lines + new 527) | 4, steady riser pre-this-run | mixed the real auth session with ~10 unrelated per-order/per-flow SecureStore groups | **RF-10 DONE this run** (2026-07-21) — see completed log. Non-auth groups (delivery codes, confirmItemsPending, pendingRating, riderJobTerminal, senderRatingPending, pendingTopup, rolePreference, onboarding/permissions/disclaimer flags, handback acks, `clearDeviceState()`) moved verbatim to `device-state.ts`; `session.ts` now holds only `Session`/`getDeviceId`/`load-/save-/clearSession` and re-exports the rest |
| 6 | `apps/api/src/privacy/privacy.service.ts` | 6 (+6 spec) | 483 lines; `eraseAccount` alone was a 300-line method (preflight → CAS anonymise → rider scrub → PII sweep → GCS purge) | **RF-11 DONE this run** (2026-07-21) — see completed log. Split into `preflight`/`anonymiseProfileTx`/`scrubPiiTx`/`postCommitPurge`, all CAS/TOCTOU comments carried verbatim, no reordering; still sensitive-adjacent, unchanged behavior |
| 7 | `apps/api/src/tracking/tracking.gateway.ts` | 828 → ~750 (RF-05a extraction) → 870 lines (accumulated comments/features since, incl. RF-05c) | mixes socket handlers/heartbeat/emit-coalescing over shared mutable state | **RF-05 DONE** — RF-05a (2026-07-26): position-coalescing (`positionEmit` map + coalesce/flush/prune) extracted to `position-coalescer.ts`. RF-05b (2026-08-02): design re-pass found the remaining four structures (`staleNotified`, `customerStaleNotified`, `customerPresence`, `customerSocketOrders`) cross ~10 boundary calls (2 direct `server.fetchSockets` queries, 2 gateway emit methods, 6 `TrackingService` calls) interleaved inside the scan-loop bodies with the map mutations — not a single well-defined flush boundary like RF-05a had — so a narrow-interface extraction would relocate complexity rather than reduce it on the repo's most bug-history-dense file; **resolved WONT-DO (extraction)**, reclassified as correctly gateway-resident (see `docs/RF-05-WS-GATEWAY-STATE.md`). RF-05c (2026-08-02): added the per-process-guarantee doc-comments on `staleNotified`/`customerStaleNotified` the design doc called for — done, no logic change. The `customerPresence`-authority open question is also resolved this run: traced every read site, confirmed it's a per-process candidate filter (every escalation is gated by a cluster-wide `fetchSockets` check before it fires), not an authority — no code change needed. |
| 8 | `apps/mobile/app/rider/index.tsx` | 955 lines, cooled out of top-40 churn | bid-compose screen | OPEN — bid-adjacent, needs a design pass; not scoped |
| 9 | `apps/admin/app/lib/adminTypes.ts` | 303 lines | duplicates API response shapes vs `packages/shared` | OPEN — not scoped; promoting to `packages/shared` is a public-surface/contract design question, not a mechanical move |
| 10 | `apps/api/src/merchant/food-order.service.ts` | 9 | 861→~845 lines, C2 food-order lifecycle (pre-dispatch): placement/accept/payment-confirm/prep + 4 reconciler sweeps | **RF-12 DONE** (2026-08-02) — its duplicated `ownMerchantId`/`notifyQueue`/`isDishOutOfStock` helpers now delegate to a shared `merchant-lookup.util.ts`. **RF-14 DONE this run** (2026-08-02) — `sweepExpiredAcceptWindows`/`sweepExpiredPhase`/`sweepEndOfDayClose`'s shared "CAS-cancel → orderEvent → notify → notifyQueue" commit now delegates to one private `commitStaleCancellation` helper; `sweepPaymentReminders` untouched (reminder push, not a cancellation — doesn't fit the shape). Section dividers already in the file (Customer/Merchant/Rider/Reconciler sweeps/Shared lookups) are a ready-made future split seam — not attempted this run (would exceed one concern). |
| 11 | `apps/api/src/merchant/merchant.service.ts` | 6 | 543→~535 lines, merchant profile/hours/categories/dishes CRUD + customer read API + statement reporting | **RF-12 DONE this run** (`findOwnMerchantIdOrThrow`/`isOutOfStock` now delegate to the shared util). RF-19 (OPEN) tracks the remaining category/dish CRUD tightening — 8 methods repeat the same ownership-lookup shape. |
| 12 | `apps/api/src/merchant/food-dispatch.service.ts` | 4 | 574 lines, C3 food dispatch — `tick`/`acceptDispatch`/`declineDispatch`/`dropDispatch`/offer-search sweeps | **SENSITIVE (dispatch/order-assignment core) — avoided**; only its non-decision `ownMerchantId`/`notifyQueue` boilerplate was touched, as part of RF-12 (mechanical, zero behavior change to the assignment algorithm). The `tick`/`acceptDispatch`/`declineDispatch`/`dropDispatch` core needs characterization before any future structural touch — not attempted. |
| 13 | `apps/mobile/app/food/order/[orderId].tsx` | 3 (cooled from a launch-week peak) | 888 lines, single component branching over ~9 `merchantPhase`/`status` combinations | RF-18 OPEN — cleanest split candidate found: each phase branch is already a self-contained view. Full split is L effort (bigger than one PR); scope a first PR to 2-3 branches. |
| 14 | `apps/merchant/app/components/queue/OrderCard.tsx` | — | 362→~330 lines, payment-bucket state machine + `canRefund` gating | **RF-17 DONE this run** (2026-08-03) — 23-case `OrderCard.test.tsx` characterization suite added first (payment-bucket flows, refund gating/sheet, mark-ready, pickup-code reveal, cash-rule copy). **RF-16 DONE this run** — the file's 4-5 near-identical busy/error try/catch/finally wrappers collapsed into one local `useAsyncAction` hook once the tests made the collapse safe to verify. |
| 15 | `apps/merchant/app/components/queue/QueueBoard.tsx` | — | 273→255 lines, ~15 near-identical `async function handleX(id) { await api(id); refetch(); }` handlers | **RF-13 DONE this run** (2026-08-02) — collapsed into a generic `withRefetch(action, refetch)` wrapper; every handler's argument list, call order, and error-propagation behavior unchanged. |
| — | `apps/api/src/settlements/settlements.service.ts` (189 lines), `apps/api/src/issues/issues.service.ts` (313 lines), `apps/api/src/admin/admin-audit.service.ts` (84 lines) | cooled / healthy size | — | no concern found 2026-07-19 |
| — | `apps/api/src/merchant/merchant-order.controller.ts` | 5 | 246 lines, pure routing/guards over dispatch actions | checked 2026-08-02 — already clean, zero misplaced logic; no dedicated spec is a minor test-health note but low priority (thin pass-through, covered by service-level + e2e specs) |
| — | `packages/shared/src/policy.ts` | cooled to 3 (was 6) | public surface, no `any` | skip — cooled and clean |
| — | `apps/api/src/tracking/tracking.service.ts` | fell out of top 40 (was 8) | 648 lines, mixes presence-escalation/geo/notify-wait concerns | RF-04 DEPRIORITIZED — churn went cold; re-promote only if it heats up again |
| — | `apps/mobile/app/order/[id].tsx`, `apps/mobile/app/rider/job.tsx` | — | bid-selection/execution screens | SENSITIVE — avoided (bid acceptance UI) |
| — | `apps/api/src/matching/matching.service.ts` | — | order-assignment ranking | SENSITIVE — avoided (order assignment) |
| — | `apps/api/src/orders/orders.service.ts` | — | agreed-price/bid logic | SENSITIVE — avoided (agreed-price/bid acceptance) |
| — | `apps/api/src/admin/admin-riders.service.ts` → `admin-kyc-review.service.ts` (630→~525 + new ~113 lines) | 8 | KYC-review read-side split from rider-standing mutations | DONE — RF-06 |
| — | `apps/api/src/notifications/notifications.service.ts` (612→~370 lines) + new `notifications-feed.service.ts` (~370 lines) | 6 | push write-path split from the feed read-model | DONE — RF-02 |
| — | `apps/mobile/src/logic/saved-recipients.ts` / `apps/mobile/src/net/history-store.ts` | — | orphaned `clear*` wrappers | DONE — RF-01 |
| — | `apps/mobile/src/ui/fonts.ts` | — | `any`-heavy render patch | DONE — RF-03 |

## Debt register

Every distinct refactoring opportunity found gets a row, whether or not it was done in that
run. IDs are `RF-NN`. Kind is one of: `dead-code`, `duplication`, `oversized`, `misplaced`,
`type-safety`, `test-health`, `migration`.

| ID | File(s) | Kind | Description | Effort | Status | PR |
|---|---|---|---|---|---|---|
| RF-01 | `apps/mobile/src/logic/saved-recipients.ts`, `apps/mobile/src/net/history-store.ts` | dead-code | Removed orphaned `clearRecipients()`/`clearHistorySnapshot()` — sign-out already clears the same keys directly in `auth/session.ts` | XS | DONE | this PR (`bd83cae`) |
| RF-02 | `apps/api/src/notifications/notifications.service.ts` → `notifications-feed.service.ts` | oversized/misplaced | Extracted `feedForUser` + `NotificationRow`/`FEED_*`/`ACCOUNT_FEED_*`/`ISSUE_RESOLUTION_FEED_COPY` constants (~386 lines) into a new `NotificationsFeedService`; wired into `notifications.module.ts` providers/exports and `notifications.controller.ts` (now injects both services, `feed()` calls the new one). The 18-case "derived in-app feed" spec block moved verbatim into a new `notifications-feed.service.spec.ts` (its own `makeDeps()` needs only a `PrismaService` mock, not the push adapter). The 2026-07-15 ~470-line estimate was stale — the feed slice had grown 612→763 lines of the source file since (4 features landed on it in the two days between runs: UX17-01/02/03, UX-2026-07-16 issue-resolution rows) — actual diff is ~830 changed lines (extraction + full-fidelity test move), over the routine's ~400 guideline. Went ahead anyway: it's a byte-identical single-concern move (nothing rewritten, only relocated), the ledger had already sanctioned exceeding the cap for this specific extraction, and splitting it further would leave "one concern" half-moved across two PRs, which is a worse trade than a larger single mechanical diff. | M | DONE | this PR (`claude/refactor-2026-07-17`) |
| RF-03 | `apps/mobile/src/ui/fonts.ts` | type-safety | Replaced all `any` usages (`patchRenderable`/`applyInterToTextComponents`/Fast-Refresh guard markers) with a named `PatchedRenderFn`/`Patchable` type pair | S | DONE | this PR (`b44e6db`) |
| RF-04 | `apps/api/src/tracking/tracking.service.ts` | oversized | Bundles presence-escalation (SOS-adjacent) tokens, rider geo/position tracking, and the notify-when-available waiting list over shared state | L | DEPRIORITIZED (2026-07-17: churn fell out of the top 40 in the 45d window — no longer a hotspot; re-promote if it heats up again) | — |
| RF-05 | `apps/api/src/tracking/tracking.gateway.ts` | oversized | Bundles socket-event handlers, presence-heartbeat scanning, and position-emit coalescing over shared mutable maps/sets (`positionEmit`, `customerPresence`, `staleNotified`) | L | **DONE** (2026-08-02) — all three sub-items have a terminal disposition. RF-05a (position-coalescing: `positionEmit` map + `coalescePositionEmit`/`flushPositionEmit`/`prunePositionRooms`) DONE 2026-07-26 — genuinely isolated subsystem, zero shared state with the rest, extracted cleanly to `position-coalescer.ts`. RF-05b (the four presence/customer-tracking structures + `scanPresence`/`scanCustomerPresence`/`handleDisconnect`/`subscribeOrder`/`customerLiveInRoom`/`riderLiveInRoom`) — the follow-up design pass `docs/RF-05-WS-GATEWAY-STATE.md` asked for is done: traced ~10 cross-boundary calls (2 direct `server.fetchSockets` queries, 2 gateway emit methods, 6 `TrackingService` calls) interleaved inside the scan-loop bodies with the map mutations, no single flush-style boundary like RF-05a had; **resolved WONT-DO (extraction)** — a narrow-interface split would relocate complexity, not reduce it, on the repo's most bug-history-dense file. Reclassified as correctly gateway-resident, not misplaced logic. RF-05c (per-process-guarantee doc-comments on `staleNotified`/`customerStaleNotified`) DONE 2026-08-02, comment-only. The `customerPresence`-authority open question is also resolved: traced every read site, confirmed it's a per-process candidate filter gated by a cluster-wide `fetchSockets` check before any escalation fires, not an authority — no code change needed. | PR (`claude/refactor-2026-07-26`) for RF-05a; this PR for RF-05b/c |
| RF-06 | `apps/api/src/admin/admin-riders.service.ts` → new `admin-kyc-review.service.ts` | misplaced | Mixed `getKycReview` (KYC review, read-only) with `suspendRider`/`liftRider`/`banRider`/`clearHold` (rider-standing mutations). **Deliberately extracted the read-side, not the mutations**: `getKycReview` (duplicate-ID collision query + signed photo-URL minting) had zero shared state with the mutation methods (only `prisma`; `pii`/`storage` deps used nowhere else in the file) and touching it can't affect the sensitive suspend/ban/clear-hold core at all — same RF-06 outcome, strictly lower risk than moving the tested-but-sensitive mutations. | M | **DONE** (2026-07-19) | this PR (`claude/quirky-tesla-y06g9q`) |
| RF-07 | `packages/shared/src/design-tokens.ts` (`accent700`) | dead-code | Deprecated alias, zero importers in app code — but it's an exported symbol of `packages/shared`, and this routine's public-surface-diff rule requires that package's exports stay byte-identical across a refactor PR. Needs its own explicitly-labeled "remove deprecated public export" PR, not a bundled refactor. | XS | WONT-DO (this routine) | — |
| RF-08 | `packages/shared/src/offer-ranking.ts` | dead-code (false positive) | `DEFAULT_OFFER_WEIGHTS`/`RankedOffer`/`OfferRankInput`/`OfferRankWeights` looked unused by raw import-count grep but are legitimate (default param value / `rankOffers` signature types) | — | WONT-DO | — |
| RF-09 | `apps/admin/app/components/StatusPill.tsx` vs `apps/mobile/src/ui/index.tsx` | duplication (not mechanical) | Status→category maps share shape but deliberately diverge in copy/category per platform; merging risks an accidental user-visible copy change | — | WONT-DO | — |
| RF-10 | `apps/mobile/src/auth/session.ts` → new `device-state.ts` | oversized | Moved the non-auth groups (delivery-code trio, confirmItemsPending, pendingRating, riderJobTerminal, senderRatingPending, pendingTopup, rolePreference, onboarding/permissions/disclaimer flags, handback acks, `clearDeviceState()`) verbatim into a new `device-state.ts`; `session.ts` re-exports them via `export * from "./device-state"` so none of its 17 importers changed. Added 3 characterization tests first (`session.test.ts` had only 4 cases) pinning `clearDeviceState()`'s exact wiped-key set, including the oddities (device id / onboarding-seen / permissions-primed survive sign-out; session token cleared separately). Also had to regenerate `.dependency-cruiser-known-violations.json`: 3 pre-existing session.ts circular-import entries now route through the new file's `export *` hop (same underlying cycles, confirmed via a clean-main-vs-branch baseline diff in a fully-installed workspace — nothing new). | M | **DONE** (2026-07-21) | PR #375 (`claude/refactor-2026-07-21`) |
| RF-11 | `apps/api/src/privacy/privacy.service.ts` | oversized (function) | `eraseAccount` was a single ~300-line method (preflight standing gate → in-tx CAS anonymise → rider scrub incl. raw-SQL `geog` → PII column sweeps → post-commit GCS purge). Split into named private helpers: `preflight()`, `anonymiseProfileTx(tx,…)`, `scrubPiiTx(tx,…)` (renamed from the ledger's placeholder `scrubRiderTx` — it also scrubs non-rider free text and the standalone PII stores, not just rider fields), `postCommitPurge(…)`, all CAS/TOCTOU comments carried verbatim, tx client threaded explicitly, no reordering. Strong existing coverage (494-line spec, 31 cases) — no characterization needed, per the sensitive-adjacent disposition. | M | **DONE** (2026-07-21) | PR #376 (`claude/refactor-2026-07-21-privacy`) |
| RF-12 | `apps/api/src/merchant/{food-order,food-dispatch,food-debt,merchant}.service.ts` → new `merchant-lookup.util.ts` | duplication | Four byte-identical `ownMerchantId`/`findOwnMerchantIdOrThrow` private methods (same Prisma lookup + `NotFoundException`), two byte-identical `notifyQueue` methods (kitchen-socket push), and two byte-identical `isDishOutOfStock`/`isOutOfStock` module functions — collapsed into three exported functions in a new shared util. Every call site's method name/signature kept identical (each class's private method now just delegates one line) so the diff touches only the duplicated bodies, not any caller. Deliberately left `food-dispatch.service.ts`'s `tick`/`acceptDispatch`/`declineDispatch`/`dropDispatch` (the actual dispatch/order-assignment decision logic) untouched — only its non-decision boilerplate helpers were in scope. | S | **DONE** (2026-08-02) | this PR (`claude/lc-r-20260802`) |
| RF-13 | `apps/merchant/app/components/queue/QueueBoard.tsx` | duplication | ~15 near-identical `async function handleX(id) { await api(id); refetch(); }` handlers collapsed into one generic `withRefetch<Args>(action, refetch)` wrapper — each handler is now `const handleX = withRefetch(apiFn, refetch)`, or a small wrapped lambda for the 3 handlers whose call needed argument-shaping (`handleAccept`'s empty-to-`undefined` mapping, `handleReleaseUnpaid`'s hardcoded reason, `handleRefund`'s body-destructure) or an extra side effect before refetch (`handleHoldResume`/`handleHoldCancel`'s `setOpenHoldId(null)`, preserved via a wrapped async lambda so call order is unchanged). `handleRevealPickupCode`/`handleOpenHold`/`handleHoldDismiss` don't fit the shape (no refetch, or no API call) and were left as-is. | S | **DONE** (2026-08-02) | this PR (`claude/bold-ramanujan-ox0ymx`) |
| RF-14 | `apps/api/src/merchant/food-order.service.ts` | duplication | `sweepExpiredAcceptWindows`, `sweepExpiredPhase` (the already-generic helper `sweepExpiredItemApprovals` calls), and `sweepEndOfDayClose` each inlined the same "CAS `updateMany` on `status:"requested"` → on success write `orderEvent` → `notifyCancelledCustomer` → `notifyQueue`" commit, differing only in the extra `where` guard and the rejection reason. Extracted a private `commitStaleCancellation(orderId, merchantId, whereExtra, reason)` that all three now call; each caller keeps its own query/pause/hours-check logic and its own per-row try/catch (error-message prefixes unchanged). `sweepPaymentReminders` was left alone — it's a reminder push (claims via `paymentReminderSentAt`), not a cancellation, so it doesn't fit the shared shape. | M | **DONE** (2026-08-02) | this PR |
| RF-15 | `apps/mobile/app/food/order/[orderId].tsx` + 5 other screens | duplication (false positive) | Re-checked 2026-08-03: only 2 of the 6 flagged `Row()`s actually share the `{label, value}` shape (`food/cart.tsx`, `food/order/[orderId].tsx`), and even those differ in a deliberate-looking value font size (13.5 non-bold vs a fixed 14.5) — not byte-identical. The other 4 (`history/index.tsx`, `notifications/index.tsx`, `settings/index.tsx`, `rider/documents.tsx`) are differently-shaped components entirely (order-history card, notification row, settings item w/ icon+press, KYC document row w/ verified pill) that happen to share the name `Row`. Extracting a shared component for the 2 genuine matches would need a style-override prop to preserve both exact renders — net line savings not worth the added indirection. | S | WONT-DO (mostly false positive; the 2 genuine matches aren't worth extracting) | — |
| RF-16 | `apps/merchant/app/components/queue/OrderCard.tsx` | duplication | The busy/error try-catch async-wrap pattern repeats 4-5 times in the file; extract a `useAsyncAction`-style hook. | S–M | **DONE** (2026-08-03) — see RF-17's ledger row (hotspot map #14) for the combined writeup; done in the same PR as RF-17's characterization tests, per the routine's "uncovered code gets tests first, same PR" rule. | this PR |
| RF-17 | `apps/merchant/app/components/queue/OrderCard.tsx` | test-health | 362 lines with real branching logic (`canRefund` gating, payment-bucket state machine) and **zero dedicated test file**. Per the routine's rule, this needs characterization tests before any structural change — file it as the prerequisite, not a deferral. | M | **DONE** (2026-08-03) — 23-case `OrderCard.test.tsx` added (all 4 buckets, payment-bucket sub-flows, refund gating/sheet incl. the distinct non-Error fallback message, mark-ready busy/error, pickup-code reveal busy/error, cash-rule copy branches), then RF-16's dedup executed against that harness. | this PR |
| RF-18 | `apps/mobile/app/food/order/[orderId].tsx` | oversized | 888 lines, single component branching over ~9 `merchantPhase`/`status` combinations, each already a self-contained JSX view — a clean split seam. Full split is L effort (bigger than one PR); scope a first extraction PR to 2-3 branches. | L (full) / M (scoped first PR) | OPEN | — |
| RF-19 | `apps/api/src/merchant/merchant.service.ts` | duplication | 8 category/dish CRUD methods repeat `findOwnMerchantIdOrThrow → findFirst ownership check → mutate` almost identically each time; a small ownership-lookup helper would tighten this. Low urgency — mechanical but not yet a hotspot pain point. | S–M | OPEN | — |

Statuses: `OPEN` (found, not started), `BLOCKED-NO-TESTS` (needs characterization tests
first — say which behaviors to pin), `IN-PROGRESS` (multi-run; see migrations below),
`DONE` (link PR), `WONT-DO` (say why — e.g. cold code, style-only churn).

**Note on `KB-SETTLEMENT-DROP` (`docs/KNOWN_BUGS.md`):** that ledger tags this routine as the
owning lane for dropping the dormant `Settlement` table + `Refund.settlementId` FK. Re-checked
2026-07-19: the drop is a Prisma **schema** migration, which this routine's own hard rules
explicitly forbid ("no Prisma schema changes"). It is NOT a valid target for a normal refactor
PR under this routine's constraints — `KNOWN_BUGS.md` should read "blocked on a dedicated
maintenance-window migration (out of the refactor routine's behavior-preserving remit)" rather
than "owned by the refactor lane." No code or doc change made here beyond this note; the
`KNOWN_BUGS.md` row itself is owned by the bug-ledger dedup protocol, not this file.

## In-flight strangler migrations

Multi-run refactors only. Each entry tracks: target, the new path introduced, callers moved
so far / remaining, and the deletion condition for the old path. A migration entry with no
movement across 3 consecutive runs must be either finished next run or downgraded to
`WONT-DO` with a reason — no zombie migrations.

_(none yet — RF-02/RF-04/RF-05 are single-PR-sized extractions once undertaken, not strangler
migrations; promote to a migration entry only if a future run finds it needs to span >1 PR.)_

## Completed-refactor log

One line per merged refactor PR, newest first: date, PR, RF-IDs, one-line summary,
before/after evidence (e.g. "exports diff clean, 14 tests green pre+post").

| Date | PR | RF-IDs | Summary | Evidence |
|---|---|---|---|---|
| 2026-08-03 | (this PR, `claude/bold-ramanujan-1esqm5`) | RF-17, RF-16 | First actionable OPEN debt-register row after RF-14's merge was RF-15, which re-investigation showed to be mostly a false positive (4 of its 6 flagged `Row()` components are differently-shaped, unrelated components; the 2 genuine matches differ in a deliberate value-size choice) — resolved WONT-DO with the corrected finding recorded, no code change. Moved to the next candidate: `OrderCard.tsx` (RF-16/RF-17), which the previous run had already flagged as a two-step job. Added a 23-case `OrderCard.test.tsx` characterization suite first (payment-bucket sub-flows incl. the confirm-sheet mismatch path, refund gating/sheet incl. its distinct non-Error fallback string, mark-ready and pickup-code-reveal busy/error states, cash-rule copy branches) — zero dedicated test file existed before this. With that harness green, collapsed the file's 4-5 near-identical busy+error try/catch/finally blocks (`PaymentBucketActions.run`/`submitConfirm`, `RefundAction`'s promise-chain confirm handler, `OrderCard`'s `handleMarkReadyClick`/`handleRevealClick`) into one local `useAsyncAction(fallbackMessage?)` hook; each call site's exact behavior preserved, including `RefundAction`'s distinct "Something went wrong" (no "— try again." suffix) fallback via the hook's optional parameter, and `submitConfirm`/`handleRevealClick`'s success-only side effects (closing the sheet, storing the returned pickup code) via the hook's `{ ok, value }` result. | `pnpm typecheck && pnpm build && pnpm test` green pre+post (96 API test files / 1511 API tests, 90 mobile suites / 671 mobile tests, 19 merchant test files / 143 merchant tests — 120 pre-existing + 23 new characterization tests, no existing test removed/weakened), `pnpm lint` 0 new warnings (1 pre-existing unrelated `admin-orders.service.spec.ts` shadow warning; merchant lint itself 0 warnings/errors), `pnpm run depcruise` 0 new violations (4 pre-existing unrelated mobile orphan infos, matches prior-run baseline), public surface untouched (`OrderCard`'s exported props/`OrderCardBucket` type byte-identical, `useAsyncAction` is an unexported local hook, no controller/route/DTO/socket-event touched, `packages/shared` untouched), 1 source file / 117 raw changed lines + 1 new 287-line test file (well under the ~400-line guideline for the source diff) |
| 2026-08-02 | (this PR, `claude/bold-ramanujan-tn3li2`) | RF-14 | First actionable OPEN debt-register row after RF-13's merge: collapsed the shared CAS-cancel-and-notify commit duplicated across `sweepExpiredAcceptWindows`, `sweepExpiredPhase` (item-approvals), and `sweepEndOfDayClose` into one private `commitStaleCancellation` helper; each caller keeps its own stale-order query/pause/hours-check logic and error-message prefix. `sweepPaymentReminders` untouched — reminder push, not a cancellation. | `pnpm typecheck && pnpm build && pnpm test` green pre+post (96 API test files / 1511 API tests + 90 mobile suites / 671 mobile tests + merchant/admin/shared suites, unchanged counts — pure mechanical collapse, no test added/removed/weakened), `pnpm lint` 0 new warnings (1 pre-existing unrelated `admin-orders.service.spec.ts` shadow warning), `pnpm run depcruise` 0 new violations (4 pre-existing unrelated mobile orphan infos, confirmed via `git stash -u` baseline diff), public surface untouched (no controller/route/DTO/socket-event touched, `packages/shared` untouched, every sweep's external signature/return-shape byte-identical), 1 file / 32 raw changed lines (well under the ~400 guideline) |
| 2026-08-02 | (prior PR, `claude/bold-ramanujan-ox0ymx`) | RF-13 | First actionable OPEN debt-register row (per the routine's priority order) after the prior run's merchant/food triage: `QueueBoard.tsx`'s ~15 near-identical `handleX` async wrappers collapsed into a generic `withRefetch(action, refetch)` helper; each handler is now a one-line delegation (or a small wrapped lambda for the 3 whose call needed argument-shaping or an extra side effect before refetch). Argument lists, call order, and the `LC-D03` error-propagation contract are unchanged. | `pnpm typecheck && pnpm build && pnpm test` green pre+post (1500 API + 668 mobile + 111 merchant tests, unchanged counts — pure mechanical collapse, no test added/removed/weakened), `pnpm lint` 0 new warnings (1 pre-existing unrelated `admin-orders.service.spec.ts` shadow warning), `pnpm run depcruise` 0 new violations (3 pre-existing unrelated mobile orphan infos, confirmed via `git stash -u` baseline diff), public surface untouched (`QueueBoard`'s props + `OrderActions`/`MoneyActions` shapes byte-identical, no controller/route/DTO/socket-event touched), 1 file / 83 raw changed lines (well under the ~400 guideline) |
| 2026-08-02 | (prior PR, `claude/lc-r-20260802`) | RF-12 | Merchant/food-scoped hotspot recompute (the recently-landed Restaurants code had never had a refactor pass) surfaced `apps/api/src/merchant/*` as the top new churn cluster; triaged 7 new candidates (RF-13 through RF-19) into the debt register and executed the cleanest one same-run: collapsed 4 byte-identical `ownMerchantId` lookups, 2 byte-identical `notifyQueue` pushes, and 2 byte-identical `isDishOutOfStock` checks across `food-order.service.ts`/`food-dispatch.service.ts`/`food-debt.service.ts`/`merchant.service.ts` into a new `merchant-lookup.util.ts`; every call site kept its original method name/signature (one-line delegation), so zero callers changed. `food-dispatch.service.ts`'s actual dispatch/assignment decision logic (`tick`/`acceptDispatch`/`declineDispatch`/`dropDispatch`) was left untouched — confirmed SENSITIVE, only its non-decision boilerplate was in scope. | `pnpm typecheck && pnpm build && pnpm test` green pre+post (95 API test files / 1500 API tests + 88 mobile suites / 668 mobile tests, unchanged counts — no test added/removed/weakened, pure dedup), `pnpm lint` 0 new warnings (1 pre-existing unrelated `admin-orders.service.spec.ts` shadow warning), `pnpm run depcruise` 0 new violations (3 pre-existing unrelated mobile orphan infos), public surface untouched (no controller/route/DTO/socket-event changed, `packages/shared` untouched, every affected method's external name+signature byte-identical), 5 files / 32 raw changed lines (well under the ~400 guideline) |
| 2026-08-02 | (prior PR, `claude/bold-ramanujan-4sii55`) | RF-05b, RF-05c | Follow-up design pass on RF-05b (the entanglement `docs/RF-05-WS-GATEWAY-STATE.md` flagged after RF-05a): traced every cross-boundary call the four remaining presence structures make and concluded a narrow-interface extraction would relocate complexity rather than reduce it — resolved WONT-DO (extraction), not a deferral. RF-05c: added the per-process-guarantee doc-comments on `staleNotified`/`customerStaleNotified` the design doc called for. Also resolved the design doc's open `customerPresence`-authority question by tracing every read site (confirmed cache, not authority — no code change needed). Docs + 2 comment-only lines in `tracking.gateway.ts`; no logic changed. | `pnpm typecheck && pnpm build && pnpm test` green pre+post (1500 API + 668 mobile tests, unchanged counts — no test added/removed, no behavior changed), public surface untouched (comment-only gateway edit; no route/socket-event/DTO change), 4 files (`RF-05-WS-GATEWAY-STATE.md` rewrite, `REFACTOR-LEDGER.md`, `tracking.gateway.ts` 2 comment blocks, new/deleted dated reports) |
| 2026-07-26 | PR (`claude/refactor-2026-07-26`) | RF-05a | Extracted the position-emit coalescing subsystem (`positionEmit` map, `CoalesceState`, `POSITION_COALESCE_MS`/`POSITION_ROOM_TTL_MS`, `coalescePositionEmit`/`flushPositionEmit`-callback split/`prunePositionRooms`) out of `TrackingGateway` into a new `PositionCoalescer` class (`position-coalescer.ts`); gateway keeps thin delegating methods (`coalescePositionEmit`/`prunePositionRooms` unchanged signatures) and the actual socket-emit/metrics callback (`flushPositionEmit`, needs `this.server`/`this.metrics`) | `pnpm typecheck && pnpm build && pnpm test` green pre+post (1215 api + 45 admin + 120 shared tests, same count — one internal-state-peek line in `tracking.gateway.spec.ts` adapted, no tests added/removed), `pnpm lint` 0 new warnings (1 pre-existing unrelated `admin-orders.service.spec.ts` shadow warning), `pnpm run depcruise` 0 new violations (1 pre-existing unrelated `apps/mobile/src/logic/earnings.ts` orphan info, confirmed via `git stash -u` baseline diff), no public surface touched (gateway's own exports — `POSITION_COALESCE_MS`/`POSITION_ROOM_TTL_MS`/`PRESENCE_SCAN_INTERVAL_MS`/`PRESENCE_STALE_DEDUP_TTL_S` — stay byte-identical via a re-export; no route/socket-event/DTO change), 3 files / 175 raw changed lines (88-line new file + 19 insertions/68 deletions across the two edited files) |
| 2026-07-21 | PR #376 (`claude/refactor-2026-07-21-privacy`) | RF-11 | Split `eraseAccount` into `preflight()`, `anonymiseProfileTx(tx,…)`, `scrubPiiTx(tx,…)`, `postCommitPurge(…)` — all CAS/TOCTOU comments carried verbatim, tx client threaded explicitly, no reordering | `pnpm typecheck && pnpm build && pnpm test` green pre+post (1169 api tests unchanged — extraction only), `pnpm lint` 0 new warnings, `pnpm run depcruise` 0 new violations, no public surface touched (`eraseAccount`/`purgeExpiredData` signatures + `DELETE /auth/me` route unchanged), 1 file / 488 raw changed lines (over the ~400 guideline — same "byte-identical single-concern move" exception) |
| 2026-07-21 | PR #375 (`claude/refactor-2026-07-21`) | RF-10 | Extracted the non-auth SecureStore groups (delivery codes, confirmItemsPending, pendingRating, riderJobTerminal, senderRatingPending, pendingTopup, rolePreference, onboarding/permissions/disclaimer flags, handback acks, `clearDeviceState()`) out of `apps/mobile/src/auth/session.ts` into a new `device-state.ts`; `session.ts` keeps only the real auth `Session` + `getDeviceId` and re-exports the rest | `pnpm typecheck && pnpm build && pnpm test` green pre+post (1169 api + 520 mobile tests, +3 net new characterization tests — none removed/weakened), `pnpm lint` 0 new warnings, `pnpm run depcruise` 0 new violations (known-violations baseline regenerated for the 3 pre-existing session.ts cycles that now route through the new file), all 17 importers unchanged (named-import grep confirmed `export *` compatibility), 1126 raw changed lines across 2 commits (over the ~400 guideline — same "byte-identical single-concern move" exception the ledger already granted RF-02/RF-06) |
| 2026-07-19 | (this PR, `claude/quirky-tesla-y06g9q`) | RF-06 | Extracted `AdminKycReviewService.getKycReview` (KYC-review read model: duplicate-ID collision query + signed photo-URL minting) out of `AdminRidersService`, leaving the sensitive standing-mutation core (`suspendRider`/`liftRider`/`banRider`/`clearHold`) untouched; moved its 2 describe blocks (9 tests) to a new `admin-kyc-review.service.spec.ts`; fixed a positional-DI test fixture (`admin-authz.e2e.spec.ts`'s manual `design:paramtypes` array) that the new constructor param shifted | `pnpm typecheck && pnpm build && pnpm test` green pre+post (1070 api + 449 mobile tests, same count — tests moved, not added/removed), `pnpm lint` 0 new warnings (1 pre-existing unrelated warning in `admin-orders.service.spec.ts`), `packages/shared` export surface + API route (`GET /admin/riders/:profileId/kyc`) + response shape byte-identical, 7 files / ~606 raw changed lines (over the ~400 guideline — same "byte-identical single-concern move" exception the ledger already granted RF-02; nothing rewritten, only relocated + one DI-wiring fix forced by the move) |
| 2026-07-17 | (this PR) | RF-02 | Extracted `NotificationsFeedService` (read-only in-app feed model) out of `NotificationsService` (push write-path); moved the 18-case feed spec block into its own spec file | `pnpm typecheck && pnpm build && pnpm test` green pre+post (1010 api + 410 mobile tests, same count — tests moved, not added/removed), `pnpm lint` 0 new warnings, `packages/shared` export surface + API route (`GET /notifications/feed`) + response shape byte-identical, 6 files / 832 insertions / 800 deletions |
| 2026-07-15 | (prior PR) | RF-01, RF-03 | Removed 2 orphaned mobile helper functions; typed the Text/TextInput font-patch (`fonts.ts`) end to end, eliminating all `any` | `pnpm typecheck && pnpm build && pnpm test` green pre+post (872 api + 375 mobile tests unchanged), `pnpm lint` 0 warnings/errors, `packages/shared` export surface untouched, 3 files / 39 insertions / 27 deletions across 2 commits |
