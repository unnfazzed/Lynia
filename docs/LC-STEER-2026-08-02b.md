# LC steer — 2026-08-02b (second Harare low-connectivity replan)

Second steer run today. The sprint cadence (`docs/routines/harare-loops.md`, 8×/day per lane,
steer 2×/day at 05:30/17:30 UTC) turned the first steer's "day 1, nothing to steer yet" into a
full day of real firing history in under 12 hours: 11 LC-branded PRs merged since
`docs/LC-STEER-2026-08-02.md`, every Day-0-confirmed CRITICAL/HIGH defect across all four lanes
now fixed, and two clean audit territories closed. This run re-ranks on that evidence, confirms
loop health, and is explicit about the one thing it still cannot do from this environment.

## Tooling note (unchanged from the first steer)

`list_triggers`/`update_trigger` are still not available in this session (re-confirmed via
`ToolSearch`) — only `CronCreate`/`CronList`/`CronDelete` (session-local reminders, not the
account's Routines triggers). This run could not read live trigger state or flip
`enabled:false`/`true` on anything. It didn't need to (no lane is stalled or complete — see §2/§5
below), but the constraint is recorded again so it isn't mistaken for an oversight. A future steer
that needs an actual trigger edit must run in a session with those tools, or the founder applies it
in the claude.ai Routines UI.

## 1. Orient — what happened since the first steer (PR #472, 03:08 UTC)

| PR | Lane | What | Merged |
|---|---|---|---|
| #473 | LC-B | B-D0/LC-B04 (CRITICAL) fixed — merchant render-loop | 04:22→12:21 UTC |
| #474 | LC-C | C-D0b/c/d / LC-C02-04 (HIGH×2, CRITICAL) fixed — merchant timeout/latch/healthz | 06:26→09:18 UTC |
| #476 | LC-D | D-D0a/LC-D02 (CRITICAL) fixed — second-order-unanswerable | →07:24 UTC |
| #477 | (meta) | Sprint-cadence doc update (8×/day) | →10:26 UTC |
| #478 | LC-D | D-D0b/LC-D03 (HIGH) fixed — bare void-promise mutations | →10:25 UTC |
| #479 | LC-C | C-D0e/LC-C05 (MEDIUM) fixed — coalesced refetch + generation sequencing | →15:37 UTC |
| #480 | LC-A | A-T2 audited clean — dependency/import-graph sweep, seeded A-O11/A-O12 | 12:50→15:41 UTC |
| #481 | LC-D | D-D0c/LC-D06 (HIGH, sensitive: money) fixed — admin ConfirmModal double-apply | →15:46 UTC |
| #483 | LC-B | B-T1 audited clean — boot-path trace, seeded B-O7 | 15:25→15:53 UTC |

All nine LC PRs (plus #482/#484, refactoring-lane work, unrelated to LC) merged clean — no red CI
requiring a steer fix, no stuck PR to babysit (`state:open head:claude/lc-*` returns zero right
now).

## 2. Loop health

**All four lanes are healthy — no diagnosis, no prompt repair needed.**

| Lane | Firings today | Landed | Current position |
|---|---|---|---|
| A | 1 | A-T2 (audit, clean) | Next: A-T3 (asset inventory) |
| B | 2 | B-D0 (fix), B-T1 (audit, clean) | Next: B-T2 (re-render audit) |
| C | 3 (incl. 1 interactive pre-program) | C-D0a..e (all 5 fixed) | Next: **C-T1** (audit mode starts — customer order journey) |
| D | 3 | D-D0a, D-D0b, D-D0c/LC-D06 (all fixed) | Next: D-D0d (`reachability.ts:98` dead offline discipline) |

Every lane is one-PR-per-firing, babysit-don't-fork, exactly as designed — no lane forked a second
branch while a PR was open, no lane re-did another's work. Lane C is the first to clear its entire
Day-0 defect list and cross into audit mode; Lane D still has three MEDIUM Day-0 defects
(D-D0d/e/f) ahead of its own audit territories, which is correct per the checklist's existing
severity order — no reorder needed there (see §4).

## 3. Budget trend

`apps/mobile/size-budget.json` unchanged: `androidExportTotalBytes: 7,850,000`,
`androidHermesBundleBytes: 6,455,000`. A-T2 (PR #480) re-measured against the same, unmodified
bundle and got the same underlying numbers the first steer already knew about — **7.13 MiB export
(4.8% headroom)**, **6.13 MiB Hermes (0.4% / 23.7 KB headroom)**. That's corroboration, not a
second independent data point on a trend: nothing has touched `apps/mobile` or `packages/shared`
bytes since the 2026-08-01 baseline, so the program's own "2+ weeks of headroom" ratchet-down bar
still isn't met. **No budget change this run.** No regression either — none of today's nine merged
PRs touched mobile or shared bundle content (all merchant/admin/docs).

**Flagging the risk plainly, since it's the single biggest threat to this program's own
guardrail:** 23.7 KB of Hermes headroom means almost *any* PR from a non-LC-A lane that touches
`apps/mobile/**`, `packages/shared/**`, or `pnpm-lock.yaml` could tip `ci.yml`'s
`mobile-bundle-size` job red before LC-A's own optimize-mode firings get a turn — LC-A is still
finishing its audit territories (A-T3/A-T4/A-T5) before it reaches its optimization checklist.
That's why §4 promotes the two size-shrinking items (A-O11, A-O12) to the front of that checklist
now, ahead of schedule, rather than waiting for LC-A to reach optimize mode and re-derive the same
urgency itself.

## 4. Re-rank

**Lane A optimization checklist — A-O11 and A-O12 promoted to the front** (was positions 11-12,
now 1-2). Rationale: of the twelve items on that list, A-O11 (drop the unused `fixtures.ts`
barrel export) and A-O12 (stop zod's 872 KB unused locale-tables barrel) are the *only two* that
shrink the Hermes JS bundle itself — A-O1/A-O4/A-O6/A-O7/A-O9/A-O10 are [data]/round-trip diet
(session bytes, not install/OTA bytes), A-O2/A-O3 are CI-guardrail process work, and A-O8 needs a
native build train. Given the 0.4% headroom, these are now the two most urgent items on the whole
Lane A checklist by (impact on the thing actually at risk)/effort, even though the Day-0 sweep's
original ordering (which prioritized [data] findings) put them last. A-O11 is S-effort/zero-risk;
A-O12 is S/M-effort and, per A-T2's own report, "likely the highest-leverage single item" here.

**Lane B optimization checklist — B-O7 promoted (was #7, now #3), B-O3 demoted (was #3, now #5),
B-O4 struck through.** B-O7 (cold-boot request prioritization) is concrete, S-effort, and backed
by this week's B-T1 trace evidence — a real candidate for the next optimize-mode firing. B-O3
(overlap boot keystore reads) is not obsolete but its own report says the only remaining question
is native-side Keystore serialization, which "needs an on-device profile" — something no firing in
this environment can produce, so it's moved behind the items an autonomous firing can actually act
on (it stays above B-O6, which is explicitly `L`/native-build-gated, since B-O3 could still become
actionable without a full native build if on-device data ever arrives). B-O4 (push-registration off
first-paint) is struck through per the "never silently delete, strike with a reason" rule: B-T1's
own evidence showed its render-blocking half was already fixed by ALR-04, and explicitly rescoped
its only remaining scope (bandwidth contention) into B-O7 — leaving both unchecked would double-
count one piece of work across two checklist items.

**Lane C, Lane D: no reorder.** Lane C's checklist (C-D0a..e, all now fixed; C-T1..T5 next in
create→auction→accept→...→reconnect-semantics order) was already correctly sequenced by journey
criticality — no new evidence this week changes that. Lane D's remaining Day-0 defects
(D-D0d structural reachability gap → D-D0e narrower missing-catch taps → D-D0f ledger-pagination
UX) are already ordered broadest-impact-first; nothing in this week's reports suggests moving
D-T1..T5 or D-O1/D-O2 relative to each other or to another lane.

**No newly discovered work to append.** A-T2 and B-T1 already appended their own findings
(A-O11/A-O12, B-O7) in their own PRs, per the audit-mode instructions — this run's job was to
re-rank what's there against the week's evidence, not re-find it. Nothing in this week's reports
surfaced work that belongs on a different lane's list, and nothing was found obsolete enough to
strike beyond B-O4 above.

## 5. Completion calls

No lane qualifies. Lane C is closest (all Day-0 defects clear, 5 audit territories + 4
optimization items remain); Lane D still has 3 Day-0 defects before its own audit territories;
Lanes A and B each have 3 audit territories left plus their full optimization checklists. The
four-lanes-disabled program-close condition is nowhere close, and — per the tooling note above —
this session could not have flipped a trigger even if a lane had qualified.

## 6. Ledger / doc hygiene

- `docs/KNOWN_BUGS.md`'s "Day-0 LC sweep" table is already current — every lane PR today updated
  its own rows in the same PR (LC-B04, LC-C02/03/04/05, LC-D02/03/06 all read **FIXED** with the
  correct PR references; LC-A03/LC-A04/LC-D04/LC-D05/LC-D07/PW-LC1/PW-LC2 correctly still OPEN).
  Nothing to reconcile there this run.
- **Report retention cleanup:** `docs/LC-C-REPORT-2026-08-01.md` and `docs/LC-C-REPORT-2026-08-02.md`
  were still present alongside `docs/LC-C-REPORT-2026-08-02b.md` — the mirror's retention rule
  ("one most-recent dated report per lane... previous one deleted in the same PR") wasn't fully
  applied by the LC-C firings that superseded them. Deleted both in this PR; only
  `docs/LC-C-REPORT-2026-08-02b.md` (the current, complete state — all 5 Day-0 defects) remains.
  Not a defect in the fixes themselves, just doc hygiene this run is the right place to catch.
- This report replaces `docs/LC-STEER-2026-08-02.md` (deleted in this PR) per the same retention
  rule.

## Next steer

Per the sprint schedule this fires again at ~17:30 UTC today or ~05:30 UTC tomorrow (whichever the
grid lands next), still 2×/day through the Tue 2026-08-04 23:00 UTC revert. By then LC-C should
have made progress into C-T1..T5, LC-D should have cleared its remaining Day-0 defects, and — if
any non-LC PR has touched mobile/shared bytes in the meantime — the Hermes-headroom risk flagged in
§3 will either have resolved (LC-A reaches A-O11/A-O12) or gotten sharper. Worth an explicit check
next run either way.
