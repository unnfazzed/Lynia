# LC steer — 2026-08-09 (Harare low-connectivity replan)

First steer of the **weekly** Sunday cadence (`docs/routines/routine-chain.md`, moved 2026-08-04 —
2026-08-01 was a Saturday, so 2026-08-09 is the first Sunday the new `0 0/2/4/6/8/10 * * 0` grid has
actually run). No LC lane fired between `docs/LC-STEER-2026-08-04b.md` (this morning's predecessor,
deleted in this PR per the retention rule) and now — expected, not a stall: the grid simply hadn't
reached its first Sunday yet. This steer runs before the day's LC-A/B/C/D/R slots (00:00 UTC, ahead
of 02/04/06/08/10), so it re-ranks against a five-day-old but still-accurate checklist state.

## 1. Orient

Zero `claude/lc-*` PRs merged or open since PR #591 (C-O4, closed 2026-08-04T23:14 UTC) — confirmed
via `search_pull_requests` for `head:claude/lc-` merges `>=2026-08-04` (12 hits, all already known
to the 08-04b steer) and a scan for `merged:>=2026-08-05` (27 hits, none LC-branded — see §2).

**Found and reconciled: the Lane D trigger-disable escalation (six consecutive steers, 08-02→08-04b)
is already resolved.** `docs/routines/routine-chain.md`'s "Applying / reverting" table (added by PR
#594, 2026-08-04, moving the whole grid to the weekly cadence) records `LC loop D` as `disabled` —
applied live by an *interactive* session that, unlike every scheduled LC session before or since
(including this one), actually carried `list_triggers`/`update_trigger`. `docs/routines/
harare-loops.md`'s long-form Lane D status paragraph had not been updated to match and still read
"blocked, sixth occurrence" — corrected in this PR (§6).

**Found and fixed: a real stale-docs bug in the program doc's own §2 budget table**, unrelated to
this week's gap — the "Android export total" row had read the **pre-ratchet** figure (`12,690,000
B`) since the 2026-08-01 Day-0 ratchet to `7,850,000` (A-T1). Stale for 8 days across all 7 prior
steers without being caught, because no steer had reason to cross-check that row against
`apps/mobile/size-budget.json` directly. Corrected in this PR (§3).

Read: program doc, `docs/routines/harare-loops.md`, `docs/routines/routine-chain.md`,
`docs/KNOWN_BUGS.md` (LC- rows, spot-checked — no drift beyond the two items above), `docs/APP-SIZE.md`,
the four lanes' latest reports (`-A-2026-08-04e`, `-B-2026-08-04f`, `-C-2026-08-04f`, `-D-2026-08-04d`).

## 2. Loop health

**All four lanes: nothing to diagnose — no firings occurred in the window, and that's correct
behavior under the new weekly grid, not a stall.** Lane-by-lane:

- **Lane A** — 7 optimization items remain (`A-O16`, `A-O10`, `A-O7`, `A-O2`, `A-O3`, `A-O8`,
  `A-O18`; `A-O5` landed 08-04e, the last item before the gap). Re-ranked this steer (§4).
- **Lane B** — 4 optimization items remain (`B-O14`, `B-O15`, `B-O3`, `B-O6`; `B-O13` landed 08-04f).
  No reorder, evidence re-checked (§4).
- **Lane C** — **1 optimization item remains** (`C-O10`; `C-O4` landed 08-04f) — the shortest
  checklist of any non-complete lane, one firing from a completion call.
- **Lane D** — **COMPLETE, trigger DISABLED** — resolved (see §1, §6).

**Non-LC activity this window was substantial and worth flagging even though it's outside this
program's mandate to act on**: 27 non-LC PRs merged 2026-08-05 alone (`chore(main): release`
0.18.0→0.21.0, a "design-kit audit" program building out previously-unbuilt screens, EAS build
fixes/visibility). This is the founder's own active development, not a competing routine — noted
here only because it's the entire explanation for this week's budget-headroom erosion (§3) and
because a program this size landing in one day is exactly the kind of blind spot A-O2/A-O3 (now
promoted, §4) exist to close going forward.

**Standing routines (bug-hunt/UX/deep-sweep/wallet-audit) — still stale, now worse.** Newest
reports on `main` remain `BUG-HUNT-2026-07-25`, `UX-USABILITY-REVIEW-2026-07-26`,
`DEEP-SWEEP-2026-07-26`, `WALLET-DATA-AUDIT-2026-07-21` — unchanged since the 08-04b steer's count,
now **15+ days stale** for the oldest. Today is also their first Sunday under the retimed weekly
grid (`0 1/3/5/7/9 * * 0` per `routine-chain.md`), so there's a real chance they simply haven't
fired yet either (same as the LC lanes) rather than being broken — this steer cannot tell the
difference without Routines-UI visibility neither this nor any prior LC session has had. Not
escalating further this round beyond recording the count: the last three steers already made the
case as bluntly as a report can, and the actionable next check is "did they fire today," which only
next week's steer (or the founder) can observe. `LC-B-SIB-1/3/4`/`LC-D-SIB-1/3/4` remain unclaimed
for the same reason, still correctly out of LC's own mandate to move by fiat.

## 3. Budget trend

**Fresh measurement this steer** (`pnpm install && pnpm --filter @lynia/shared build && expo export
--platform android` + `scripts/check-bundle-size.mjs`, since no lane had re-measured since 08-03e
and five days of unrelated feature work had landed):

| | Budget | Last measured (08-03e) | **Now (08-09 steer)** | Headroom |
|---|---:|---:|---:|---:|
| Android export total | 7,850,000 B | 6,546,466 B | **6,677,779 B** | 14.9% (was 16.6%) |
| Hermes JS bundle | 6,455,000 B | 6,189,316 B | **6,326,143 B** | **2.0%** (was 4.1%) |

**Within budget, but Hermes headroom has nearly halved** — +136,827 B consumed between 08-05 and
08-09, entirely by the non-LC feature work in §2 (no LC lane touched the bundle in that window).
2.0% headroom is now tight enough that the next legitimately-sized feature PR risks tripping the
`mobile-bundle-size` CI gate for real, not hypothetically. **No ratchet this run** — the §2 bar is
excess headroom held for 2+ weeks, and this data point is the opposite (erosion, not stability); a
DOWN ratchet on 2.0% headroom would be actively wrong. Instead: promoted A-O2/A-O3 (§4) — the
missing per-PR visibility is the direct, evidenced cause of an unmonitored budget squeeze, which is
exactly what those two items were seeded to prevent.

**Corrected the stale `12,690,000` figure in the program doc's own §2 table** (see §1) — both rows
now read live-measured numbers with today's date, closing an 8-day, 7-steer documentation gap.

**Native per-device download** — unchanged since 08-04b (`docs/APP-SIZE.md` "First shipped
artifact", 32,546,001 B raw AAB, pre-split, still no Play Console per-device figure recorded).
Despite six native releases shipping this week (0.18.0→0.21.0 per the merged-PR list), no new AAB
size measurement was recorded in `docs/APP-SIZE.md` — flagging for whichever session next has a
release build in hand to capture it, same "watch" framing as the last steer.

**Session-data budgets** — still the same stale-optimistic flag every steer since 08-04b has
carried, still not re-baselined (needs A-T4-trace rigor, not a steer-session estimate; no Lane A
audit-territory work happened this window to trigger a natural re-baseline).

## 4. Re-rank

**Lane A — A-O2/A-O3 promoted from #6/#7 to #1/#2**, ahead of `A-O16`/`A-O10`/`A-O7` (new order:
`A-O2`, `A-O3`, `A-O16`, `A-O10`, `A-O7`, `A-O8`, `A-O18`; physically reordered in the program doc,
not just annotated, per the 08-04b steer's own precedent that a firing takes the first unchecked
item by document position). Rationale is §3's measurement: the exact failure mode A-O2 (merge-time
size-diff PR comment) and A-O3 (asset-PR guardrail) exist to prevent — budget erosion invisible on
a per-PR basis — just happened for real, for the first time since these items were seeded
2026-08-02. Both are S-effort and fully landable in one firing each. `A-O8`/`A-O18` stay last
(native-build-blocked / needs new sensitive-path server emits, unchanged rationale).

**Lane B — no reorder**, but re-verified the premise behind the existing `B-O14`/`B-O15` >
`B-O3`/`B-O6` order (set 08-04b): the new EAS build-status bridge (PRs #600/#601, 2026-08-05) was
new evidence worth checking against B-O6's specific blocker (on-device/emulator verification of a
native font-table patch) — read its workflow source directly and confirmed it's `eas build:list`
status/error-log output only, no device or emulator access. Doesn't change anything; noted in the
program doc so a future steer doesn't have to re-derive this from scratch.

**Lane C — no reorder** (one item, `C-O10`, nothing to reorder against).

**Lane D — no re-rank; checklist complete, trigger disabled** (§1, §6).

**Not moved onto any lane's checklist:** `LC-B-SIB-1/3/4`/`LC-D-SIB-1/3/4` — unchanged reasoning
from the last three steers (§2). **No newly discovered work to append** — no LC lane produced a
report this window to surface follow-on findings from.

## 5. Completion calls

- **Lane A** — audit phase complete; 7 optimization items remain (re-ranked, §4).
- **Lane B** — audit phase complete; 4 optimization items remain (2 confirmed blocked on resources
  this environment lacks, re-confirmed this steer; 2 real and landable).
- **Lane C** — audit phase complete; **1 optimization item remains** (`C-O10`) — closest to a
  completion call of any active lane. If it lands before the next steer, Lane C becomes the
  program's second completed lane.
- **Lane D** — **COMPLETE since 2026-08-04, trigger DISABLED** (resolved this steer — see §1, §6).
  No action needed.

The §8 four-lanes-disabled program-close condition is not met (1 of 4 disabled) — closer than
08-04b only in the trivial sense that Lane D's disable is now *confirmed* rather than *pending*; no
lane crossed a real completion threshold this window since none fired.

## 6. Ledger / doc hygiene

- **`docs/routines/harare-loops.md`**: replaced the stale "Lane D disable still blocked, sixth
  occurrence" paragraph with a resolution note (PR #594 disabled it 2026-08-04 via an interactive
  session) — see §1.
- **Program doc §2**: corrected the `12,690,000` stale pre-ratchet figure to the live
  `7,850,000`-budget / `6,677,779`-measured pair, and refreshed the Hermes row with today's
  measurement — see §1, §3.
- **Program doc §5 Lane A**: physically reordered `A-O2`/`A-O3` ahead of `A-O16`, with rank-history
  annotations on all three items; extended the checklist header rationale comment. Lane B checklist
  gained a confirmation note under `B-O6` (no reorder, no annotation drift risk).
- **`docs/KNOWN_BUGS.md`**: spot-checked the `LC-` rows (D19–D38, A09) against current program-doc
  checkbox state — no drift found, nothing new to ledger this steer (no lane produced findings this
  window).
- **Report retention**: this report replaces `docs/LC-STEER-2026-08-04b.md` (deleted in this PR).

## Next steer

Watch for: whether Lane C's last item (`C-O10`) lands, making it the program's second completion
call; whether Lane A's `A-O2`/`A-O3` promotion produces landed CI-visibility tooling (and whether it
catches the next round of non-LC bundle growth in the act); whether the standing routines' first
weekly Sunday firing (today, per `routine-chain.md`) actually landed anything — if `BUG-HUNT-*`/
`UX-USABILITY-REVIEW-*`/`DEEP-SWEEP-*`/`WALLET-DATA-AUDIT-*` are still dated 07-2x next week, that's
a stronger signal than this steer can currently produce that something is actually broken rather
than merely not-yet-retimed; and whether Hermes headroom (2.0%, tight) holds or a legitimate PR
trips the CI gate before A-O2/A-O3 land.
