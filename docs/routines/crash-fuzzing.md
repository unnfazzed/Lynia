You are running the weekly **crash-fuzz routine** for the Lynia codebase (a pnpm/Turbo monorepo delivery platform: `apps/api` NestJS + Prisma, `apps/mobile` React Native + Expo, `apps/admin` + `apps/merchant` Next.js, `packages/shared`). This is a fresh session with no repo context — if the repo is not already checked out, attach `unnfazzed/lynia` with the `add_repo` tool (push access) and clone it; then start from the latest `main` and read `CLAUDE.md` in full (the pixel-parity rules, merge-on-green policy and sensitive-lane doctrine all bind this run). Work on branch `claude/crash-fuzz-<YYYY-MM-DD>`. Model policy (owner instruction 2026-08-16): this lane runs on **Sonnet 5** (`claude-sonnet-5`) — do not escalate the model.

**Mission: find and fix RUNTIME failures by running the real apps — no mocks of the app under test.** Your evidence source is the running app, not the source tree. You are hunting the MOB-BOOT class of bug: crashes, unhandled promise rejections, hangs (>10 s stuck UI), infinite spinners, and flash-of-wrong-state frames (a screen rendering a decision it has not yet made) — the failures that the source-reading bug lanes (bug hunt 01:00, deep sweep 05:00) cannot see.

## Phase 0 — Dedup + baseline
1. Read `docs/KNOWN_BUGS.md` in full — it is the dedup ledger; your prefix is **CF-**. Re-verify any cluster "FIXED" claim that overlaps your lane against the code before trusting it.
2. List open `claude/*` PRs and read any not-yet-merged sibling PRs so you don't duplicate an in-flight fix.
3. `pnpm install`, then `pnpm typecheck && pnpm test` on clean `main`. If red: STOP — the PR-health watchdog owns red main. Record the breakage, ship a docs-only note, end the run.

## Phase 1 — Fuzz the real faces
Boot each face and abuse it at the canonical viewports (CLAUDE.md): mobile via the react-native-web harness in `tools/parity/` at 360×720 plus the mandatory 320×640 entry-phone pass; merchant at 1024×680 and admin at 1440×900 via Playwright against their real Next servers; seed the backend where a journey needs it (the screen-inventory allowlist marks `BACKEND_GATED` targets). Fuzz moves per journey: rapid double-taps on primary actions, back-button spam, mid-flow offline/online toggles, expired/stale sessions, empty states and maximal data, malformed deep links, interrupted OTP and stream flows, viewport resize mid-interaction. Capture every failure with a minimal deterministic repro (exact steps, viewport, seed state) and a screenshot.

## Phase 2 — Root-cause and fix, same run
Fix every confirmed finding in this run — no deferrals (universal routine policy). Each fix gets a regression test where the failure is testable. Sensitive lanes (bid acceptance / order assignment / agreed-price / KYC gating / wallet) get conservative fixes plus a regression test each, mandatory. Never fix a runtime bug by adding UI the mocks don't draw — pixel parity binds; if the correct fix collides with a mock, that is a `docs/DESIGN-DEVIATIONS.md` conversation, not an improvised screen. Sibling-sweep: for each root cause, sweep the codebase for the same bug class and fix or record every instance (deliberate non-fixes get a `CF-…-SIB` row with the reason).

## Phase 3 — Ledger, report, ship
Update `docs/KNOWN_BUGS.md` (new CF- rows + write-backs) and write `docs/CRASH-FUZZ-REPORT-<date>.md` (repros, root causes, fixes, non-fixes with reasons, journeys covered vs. skipped — no silent coverage gaps) in the SAME PR as the fixes. `pnpm typecheck && pnpm test` green locally, push, open the PR ready for review with the GitHub MCP tools (`gh` is not available), subscribe to it, and enable auto-merge (squash) per the repo merge-on-green policy — never merge red; fix forward until green. A clean run (zero findings) still ships the dated report + ledger note.
