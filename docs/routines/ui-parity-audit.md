You are running the weekly **UI parity audit** for the Lynia codebase (a pnpm/Turbo monorepo delivery platform: `apps/api` NestJS + Prisma, `apps/mobile` React Native + Expo, `apps/admin` + `apps/merchant` Next.js, `packages/shared`). This is a fresh session with no repo context — if the repo is not already checked out, attach `unnfazzed/lynia` with the `add_repo` tool (push access) and clone it; then start from the latest `main` and read `CLAUDE.md` in full, especially "Pixel parity" — **the mocks in `packages/design/` are the source of truth for structure, card design and every UI element.** Work on branch `claude/ui-parity-audit-<YYYY-MM-DD>`. Model policy (owner instruction 2026-08-23): this lane runs on **Sonnet 5** (`claude-sonnet-5`) — do not escalate the model.

**Mission: report, never implement.** Per explicit user instruction, this routine's sole output is a dated comparison of the shipped app against its design mock, for every screen it can render, structure/card-design/UI-elements first — where they diverge, explain *why* in the code and recommend a fix, but make **zero** app or design code changes. This is a standing, permanent exception to the repo's usual "fix every finding in the same run" universal policy — modeled on the LC-loop-D infra lane (`docs/ROUTINES.md`): report-only by design, not a shortcut available to any other routine.

## Phase 0 — Context (read before touching product code)
1. Read `docs/KNOWN_BUGS.md` in full — your prefix is **UIP-**. A finding already logged (or a drifted repeat of one) gets a one-line "reconfirmed `<date>`" note on the existing row, never a duplicate.
2. Read `docs/PIXEL-PARITY-TRACKER.md`, `docs/DESIGN-DEVIATIONS.md` (an **APPROVED** entry is a sanctioned difference, not a finding — never re-flag it) and `tools/parity/rendered-conformance.pending.json` (the existing rendered-drift inventory, each key already carrying its own named blocker) so this run extends that knowledge instead of re-deriving it.
3. `pnpm install`. No baseline test gate is required — this run changes no product code — but if `main` is red, say so in the report and continue; fixing or blocking on it is not this lane's job.

## Phase 1 — Machine-checkable sweep (every wired screen, every run)
Run the mobile structural checks across the full wired set — cheap, exact, no eyeballing required:
```
node tools/parity/codegen/cli.mjs check
node tools/parity/extract-expected.mjs --check
pnpm --filter ./apps/mobile exec jest rendered-conformance
```
Diff this run's pass/fail set against the prior `docs/UI-PARITY-AUDIT-<date>.md` (git history — the previous report is deleted from `main` each run per the report-retention convention) to flag **regressions**: a screen that conformed last time and does not now. A regression is the single highest-value finding this routine can produce — it means an unrelated change silently broke an already-aligned screen.

## Phase 2 — Visual sweep (rotating deep-dive, `tools/parity/pair.mjs`)
Background the web dev servers (`node serve-web.mjs admin`, `node serve-web.mjs merchant`) and render the full side-by-side sheet: `node pair.mjs --wired --out out/sheet-wired`. Pick **one** surface category least-recently deep-reviewed — check the prior report's "Deep-dive" heading and rotate: customer food (RC) → customer send/auth/account (LJ) → rider (RJM/RJ/RR) → merchant tablet (RM) → admin console. For every screen in that category, compare the sheet's mock and app columns at the same bar as an alignment PR (`CLAUDE.md` → "Pixel parity": structure, geometry, colour, type **and** copy). For every other wired screen outside this week's category, a pass/fail glance at the sheet is enough — detailed prose is reserved for the deep-dive.

## Phase 3 — Explain every mismatch (still no fixes)
For each divergence surfaced in Phase 1 or 2 that is not already an APPROVED deviation: name the concrete structural/visual difference, the root cause in the code (which component or branch renders the divergent shape, and why — a not-yet-migrated legacy component, live data where the mock drew a static value, a feature-flag branch, a field that's genuinely backend-gated), and a specific, actionable recommendation for closing the gap. Do not make the change. A screen with no app target yet (`PENDING`/`BACKEND_GATED` in `tools/parity/parity-status.mjs`) is reported as "not yet renderable for comparison" — never guessed at from the mock alone.

## Phase 4 — Ledger, report, ship (docs-only PR)
Add `docs/KNOWN_BUGS.md` rows — prefix **UIP-**, status **OPEN**, owner "design/alignment — report-only lane, no autonomous fix; see the dated report for the recommendation" — for every fresh or regressed mismatch. Write `docs/UI-PARITY-AUDIT-<date>.md`: a summary table (screen → verdict → evidence source → root cause → recommendation), the Phase 1 regression list, and this run's deep-dive category (so the next run knows which is least-recently reviewed); delete the previous dated report from `main` in the same PR (report-retention convention — git history is the archive). Do **not** touch `docs/PIXEL-PARITY-TRACKER.md` status marks, `docs/DESIGN-DEVIATIONS.md`, or anything under `packages/design/` or `apps/*`/`packages/shared/*` — this PR is docs-only, always. Push, open the PR ready for review with the GitHub MCP tools (`gh` is not available), subscribe to it, and enable auto-merge (squash) — a docs-only PR is green by construction; never merge red regardless. A clean run (no new or regressed mismatches) still ships the report — it is the evidence the week was actually checked.
