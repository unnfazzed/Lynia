# LC steer — 2026-08-03 (Harare low-connectivity replan)

Sprint-cadence steer (2×/day, 05:30/17:30 UTC through the Tue 2026-08-04 23:00 UTC revert). Since
`docs/LC-STEER-2026-08-02b.md`, all four LC lanes fired again and landed real work: LC-A closed its
fourth audit territory (A-T4, wire-bytes profile), LC-B closed its third (B-T3, list/memory audit),
LC-C closed its second (C-T2, rider shift journey), and LC-D fixed its last Day-0 defect (D-D0f)
and then closed its first audit territory (D-T1, admin console sweep). This run found one
in-flight PR sitting green with no reviewers attached and merged it, then re-ranked two lanes'
checklists on the week's evidence and caught one cross-lane duplicate.

## 1. Orient

| PR | Lane | What | Status |
|---|---|---|---|
| #494 | LC-C | C-T2 audited (rider shift journey) — reference-quality, one new optimization item (C-O7/LC-C09) | merged |
| #495 | LC-B | B-T3 audited (list/memory) — 2 defects fixed (LC-B07 unbounded restaurant list, LC-B08 rider sentOffers growth), 6 optimization items appended (B-O10..B-O15); tooling misconfig again ran the wallet lane, surfacing 4 off-lane money-path findings (LC-B-SIB-1..4) | merged |
| #496 | LC-D | D-D0f fixed (admin rider wallet ledger truncation, LC-D07) | merged |
| #497 | LC-A | A-T4 audited (wire-bytes profile) — zero same-run defects, 4 new optimization items (A-O14..A-O17), §2 session-data budgets moved from provisional to evidence-baselined | merged |
| #498 | (RF) | Refactoring-lane work, unrelated to LC | merged |
| #501 | LC-D | D-T1 audited (admin console sweep) — 3 defects fixed (LC-D08 missing `loading.tsx`, LC-D09 merchant ledger/disputes truncation, LC-D10 unbounded funnel query) | **found open, green, unreviewed — merged this run** (see §2) |

## 2. Loop health

**All four lanes are healthy — no diagnosis, no prompt repair needed.** Every lane produced a
merged, checklist-ticking PR today; no lane forked a second branch while one was open; no lane
re-did another's work.

One action taken: PR #501 (LC-D's D-T1 sweep) was sitting `open`, `mergeable_state: clean`, CI
green (`CI` success, `CodeQL` success, no review comments/threads) since 04:50 UTC with nobody
watching it — LC-D's own next firing (07:00 UTC nominal, or its next `:45 */3` sprint slot) would
have babysat it eventually, but the universal merge-on-green policy applies to any Claude-authored
PR the moment it's green, not only to the lane that opened it. Squash-merged it
(`07017f7bca47c1dea84dcd3e3a723287bb82ce3f`) rather than leave it idle. `docs/KNOWN_BUGS.md`
(LC-D08/09/10) and the program doc's D-T1 box were already correctly updated in that PR.

**Tooling note (unchanged from both prior steers):** `list_triggers`/`update_trigger` are still not
available in this session (re-confirmed via `ToolSearch` — only `CronCreate`/`CronList`/
`CronDelete` session-local reminders exist here, not the account's Routines triggers). Didn't need
them this run (no lane stalled or complete), but recording it again so a future steer that *does*
need a trigger edit knows to expect the same gap and either run in a session with those tools or
hand the edit to the founder via the claude.ai Routines UI.

**Recurring tooling bug worth a founder note:** the `lane-bug-hunt` workflow's custom-lane `args`
has now silently fallen back to the hardcoded `wallet` lane THREE times (LC-B-SIB-1/2 originally,
then again per B-T3's own report, and D-T1's first attempt before it patched `resolveLane()` inline
per-run). Each lane is patching its own copy of the generated script rather than the shared
workflow definition, so the bug keeps recurring. Out of scope for this steer to fix (it's workflow
tooling, not program content), but worth flagging plainly: if a lane doesn't happen to notice the
mis-run and re-patch, the whole firing's audit territory silently gets skipped in favor of a wallet
scan. Founder/tooling-owner should fix `resolveLane()` upstream once, not per-firing.

## 3. Budget trend

`apps/mobile/size-budget.json` unchanged: `androidExportTotalBytes: 7,850,000`,
`androidHermesBundleBytes: 6,455,000`. No fresh bundle-byte measurement landed this week (A-T2/
A-T3/A-T4 were dependency/asset/wire-profile audits, not re-measurements) — last real number is
still the Day-0 baseline (7,476,013 / 6,431,313 B → 4.8% / 0.4% headroom). No regression: every PR
since Day-0 that touched `apps/mobile`/`packages/shared` passed the required `mobile-bundle-size`
CI gate, and merge-on-green means a violation would have blocked the merge, not slipped through
quietly.

**No ratchet this run.** The ratchet-down bar is "2+ weeks of headroom" — this program is 2 days
old and Hermes headroom is 0.4% (23.7 KB), the opposite of headroom to spend. That's why A-O11/
A-O12 (the only two items on any checklist that shrink the Hermes bundle itself) have sat pinned at
#1/#2 of Lane A's optimization queue since the prior steer, and A-T4 explicitly re-confirmed the
same tight number this week rather than found a new one. Nothing to file as a budget defect either
— no regression found.

Session-data budgets (§2's other row): A-T4 did the real work here this week — replaced the
"provisional" ≤150/≤300 KB targets with evidence-baselined ones (parcel journey ≤120 KB, food
journey ≤150 KB, rider hour ≤200 KB/h) from a full field-by-field wire trace, already landed in the
same PR. Nothing further to ratchet until A-O6/A-O9 (the two levers A-T4 identifies as necessary to
hit those targets) actually land.

## 4. Re-rank

**Lane B optimization checklist — B-O11 promoted (was #11, now #5, ahead of B-O7).** B-O11 (KYC/
pickup-photo preview rendering the undownscaled original instead of the already-produced
downscaled asset) is S-effort with a confirmed root cause and a one-line-per-site fix, and it
serves Lane B's core "stay alive on 1-2GB RAM" mandate directly in what its own report calls "the
single most OOM-sensitive flow in the app." B-O7 (cold-boot request prioritization), same S-effort,
remains explicitly "impact unconfirmed without an on-device 2G trace." By impact/effort with the
week's evidence, B-O11 is the stronger next pick. B-O7 moves to #6; nothing else in Lane B's order
changes (B-O3/B-O5/B-O6's hardware-blocked reasoning from the prior steer still holds, B-O10/B-O12
-15 stay appended in their existing order — none of that batch has fresh evidence arguing for a
reorder among themselves).

**Lane C optimization checklist — C-O5/C-O6/C-O7 promoted ahead of C-O1/C-O2/C-O4 (was #1-4, now
#4-6); C-O3 struck as a duplicate.** C-O5/C-O6/C-O7 are concrete, S-effort fixes from this week's
completed C-T1/C-T2 traces (durable markers, 409-reconciliation alignment) with named file:line
evidence and clear implementations. C-O1/C-O2/C-O4 are broader M-effort backlog items seeded before
the traces ran, with no fresh measurement behind them this week — by (evidenced impact)/effort the
concrete items win. Separately: **C-O3** ("share one Socket.IO connection across realtime hooks")
turns out to be the identical fix as Lane A's **A-O17** (LC-A07, found by this week's A-T4 trace) —
same three call sites (`socket.ts:12-13`, board/job/location hooks), same multiplex-to-one-
connection remedy, just approached from two angles (C-O3: fewer reconnect handshakes; A-O17: one
keepalive stream instead of three). Struck C-O3 through with a pointer to A-O17 rather than
silently dropping it — Lane A owns the implementation since it has the fresher, more specific
evidence; the resilience benefit ships automatically when A-O17 lands, no separate C-side work
needed.

**Lane A, Lane D: no reorder.** Lane A's checklist was re-ranked by A-T4 itself in the same PR that
produced the evidence (A-O9/A-O6 promoted to #4/#5 for the [data] byte-cost reasons already in the
doc) — nothing this steer found argues for moving it further. Lane D's two-item optimization
checklist (D-O1/D-O2) has no fresh evidence pointing to a reorder; D-T1 just closed with 3 new
defects already fixed in the same PR (no optimization deferrals from that audit).

**Not moved onto any lane's checklist: LC-B-SIB-1..4.** These four money/wallet-path findings
(commission miscalc dormant at 0% rate, rider wallet-ledger mobile-client truncation, commission
receipt text/amount mismatch, admin KPI + rider earnings-tab `orderType` scope gaps) surfaced
incidentally from this week's B-T3 tooling misconfiguration (see §2). They're correctly OPEN in
`docs/KNOWN_BUGS.md`, explicitly dual-routed to "Lane D or the wallet & data-lifecycle audit
routine, whichever fires next" — real money-correctness bugs, not low-connectivity/low-end-device
issues, so they sit outside all four LC lanes' actual mandate (§1) even though Lane D's journey
sweep is one plausible owner. Forcing them onto Lane D's checklist now would extend the program's
scope by fiat rather than have the next routine that reads the ledger (Phase-0, per the standard
dedup protocol) pick the first one up as designed. Leaving them as visible OPEN ledger rows is the
correct call — flagging here so they're not mistaken for silently dropped.

**No newly discovered work to append beyond what each lane's own audit PR already appended.**

## 5. Completion calls

No lane qualifies. Status: LC-A has 1 audit territory (A-T5) + 17 optimization items remaining;
LC-B has 1 audit territory (B-T4) + 14 remaining (post B-O4 strike, post this steer's reorder);
LC-C has 3 audit territories (C-T3/T4/T5) + 6 remaining (post C-O3 strike); LC-D has 4 audit
territories (D-T2..T5) + 2 remaining. The four-lanes-disabled program-close condition (§8) is
nowhere close, and — per §2's tooling note — this session could not have flipped a trigger even if
one had qualified.

## 6. Ledger / doc hygiene

- `docs/KNOWN_BUGS.md` is current: LC-D08/09/10 already landed as **FIXED** in #501 with correct
  report references; LC-B-SIB-1..4 correctly OPEN and dual-routed (§4); LC-A06/07/08/09,
  LC-C07/08/09 correctly OPEN against their owning lane's optimization item. Nothing to reconcile.
- **Report retention cleanup** (the mirror's rule — one most-recent dated report per lane,
  previous deleted in the same PR — had fallen behind): deleted `docs/LC-A-REPORT-2026-08-02.md`,
  `docs/LC-A-REPORT-2026-08-02b.md` (superseded by `-2026-08-03.md`); `docs/LC-B-REPORT-2026-08-02.md`
  (superseded by `-2026-08-03.md`); `docs/LC-D-REPORT-2026-08-02.md`, `-2026-08-02b.md`,
  `-2026-08-03.md` (superseded by `-2026-08-03b.md`). `docs/LC-C-REPORT-2026-08-03.md` was already
  the sole Lane C report (prior steer's cleanup held). This report replaces
  `docs/LC-STEER-2026-08-02b.md` (deleted in this PR) per the same rule.
- Program doc (`docs/plans/2026-08-01-low-connectivity-program.md`) §5 edited in this PR: Lane B
  and Lane C re-ranks + the C-O3 strike-through (§4 above). No other section changed.

## Next steer

Fires again at ~05:30 UTC tomorrow or ~17:30 UTC today (whichever the grid lands next), still 2×/
day through the Tue 2026-08-04 23:00 UTC revert. By then: check whether the `lane-bug-hunt`
wallet-lane fallback bug (§2) has recurred a fourth time (worth escalating harder if so), whether
any LC-B-SIB item got picked up by Lane D or the wallet routine, and whether A-O11/A-O12 have
landed yet — they're the single biggest lever on this program's tightest constraint (0.4% Hermes
headroom) and the longer they sit at #1/#2 unactioned, the more any unrelated mobile-touching PR
risks tipping `mobile-bundle-size` red before Lane A gets a turn.
