// force-update needs no providers or props — it's a self-contained blocking screen. This fixture
// exists so the app-target can name one; defaults render <ForceUpdateScreen/> as-is.
export default {};
