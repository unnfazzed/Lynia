import * as React from "react";
export interface SubProps { children?: React.ReactNode; style?: React.CSSProperties; }
/**
 * 14px muted sub-heading under a Heading.
 * @dsCard group="Components"
 */
export declare function Sub(props: SubProps): React.ReactElement;
