# EXPORT-CHANGELOG — since the 2026-08-05 export

Baseline: 2026-08-05 export, anchored on `explorations/journey/All Screens Gallery.html`
(md5 beginning `8e9c6238`). Current export date: 2026-08-10.

## Design changes: NONE

**No screen, state, token, component, kit, or document changed since the baseline.** Explicitly,
per registry: LJ — no changes · RC — no changes · RJ — no changes · RJM — no changes · RR — no
changes · RM — no changes · Admin (A1–A7) — no changes · new-flows sheet — no changes ·
safety-flows sheet — no changes · support kit — no changes · tokens/ — no changes ·
components/ — no changes.

Evidence:
1. `All Screens Gallery.html` currently fingerprints to
   **md5 `8e9c62380c72af325dcf538f88fd5cd3`** — matching the baseline's `8e9c6238` prefix,
   i.e. the LOOK-authority index is byte-identical.
2. Every authored source file's last-modified stamp in the design tool predates 2026-07-29;
   no design-editing session touched this project between 2026-08-05 and this export.

## Added in this export (new files, at the root)

- `EXPORT-README.md` — authority rules, canonical viewports, deprecation list, offline caveat.
- `EXPORT-MANIFEST.txt` — every file with byte size + SHA-256, plus the full screen inventory.
- `EXPORT-CHANGELOG.md` — this file.

These three are the only additions; fold them into `packages/design/` alongside everything else.

## Removed

Nothing.

## Generated files — may differ byte-wise without design meaning

`_ds_bundle.js`, `_ds_manifest.json`, `_adherence.oxlintrc.json`, and the `.thumbnail` files are
(re)generated by the design tooling; the bundle's stamp shows a routine regeneration after
2026-08-05 with no authored-source change behind it. If your diff shows deltas ONLY in these
generated files (and the three EXPORT-* additions), the export matches the baseline exactly on
everything designed. Use the SHA-256 values in `EXPORT-MANIFEST.txt` to confirm.
